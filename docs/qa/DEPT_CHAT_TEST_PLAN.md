# Bravo Secure — Departmental Chat v2: Full Frontend Test Plan

> Manual SQA checklist for Departmental Chat v2 (provider-org <-> managed-CPO workspace): dept channels + E2EE provisioning, attendance check-in/out with FACE CAPTURE + geofence, incident reporting + queue + dispute, shift management, PDF export, and dept-scoped access control. Code-grounded against the deptchat screens + auth-service backend + the v2 spec.

**Version:** v1.0.96 (vc122) · **Generated:** 2026-07-05 · **Total cases:** 239

**Priority:** P0 80 · P1 107 · P2 52  
**Type:** golden 38 · error 40 · edge 62 · security 71 · regression 26 · perf 2

**Legend — Priority:** P0 = core/security (must pass) · P1 = important · P2 = nice-to-have. **Status:** Pass / Fail / Blocked / N-A. Attendance face-capture (MLKit) requires a native APK build — verify on a real device build, not Expo Go.

---

## 1. Departmental Channels & Navigation (27 cases)

| ID     | Pri | Type       | Scenario                                                                                                                       | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Devices/Roles                                                                                                       | Bug                                                                                                   | Status                       | Notes |
| ------ | --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ---------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| DCN-01 | P0  | golden     | CPO member enters Departmental module via the Dept tab and lands on the Channels list                                          | 1. Observe the CPO bottom tab bar order: On Duty, Mission, Comms, Dept, Me.<br>2. Note the 'Dept' tab icon (an office-building icon) and any red unread badge on it.<br>3. Tap the 'Dept' tab.<br>4. Wait for the module to open full-screen.<br>5. Observe which of the 5 dept tabs is active and which screen renders.                                                                                                                                                                                                                                                                                                                           | The 'Dept' tab sits between Comms and Me and renders ONLY because the flag is on. Its icon is the office-building glyph (NOT forum); the forum icon is the Channels tab INSIDE the module. The tab press does not mount a nested tab (its placeholder never shows) — the listener preventDefaults and navigates to the full-screen 'Departmental' route with {screen:'Channels'}. The module opens with its own 5-tab obsidian footer (Home, Channels, Attend, Incident, Vault) and NO nested double footer, landing on the CHANNELS tab (channel list), not Home. The header reads 'Department Channels'. A brief loading spinner may show, then the channel directory renders.                                                                                                                                       | 1 device — ACTIVE CPO member account                                                                                |                                                                                                       |                              |       |
| DCN-02 | P0  | golden     | Channel list renders only dept-scoped channels grouped by type with correct state badges                                       | 1. Let the channel directory load.<br>2. Note the section headers rendered (Board / Department / Incident); empty groups render nothing.<br>3. For each channel row read: name, type icon, sub-preview text, and the state badge.<br>4. Confirm every channel shown belongs to the CPO's own org.<br>5. Note the state badge on a provisioned normal channel vs an admin-owned channel vs an unprovisioned one.                                                                                                                                                                                                                                    | Channels are grouped under 'Board', 'Department', and 'Incident' section labels (a channel with no channel_type defaults to Department). Each row shows the type icon, name, and a preview: the department name, or 'Tap to open' when provisioned, or 'Not yet active' when the Signal group id is null. State badges per rowState(): INACTIVE (no group id, grey) takes precedence, else MANAGERS (incident), READ-ONLY (access=read_only), PRIVATE (access=restricted), ADMIN (my_role=admin), else ACTIVE. Only the viewer's own org channels appear — the server listChannels JOIN on department_channel_members enforces this, so no channel from any other org is listed.                                                                                                                                       | 1 device — ACTIVE CPO member account                                                                                | D1-i                                                                                                  |                              |       |
| DCN-03 | P1  | golden     | Per-channel unread badge and CPO Dept-tab total badge reflect real unread counts from the encrypted store                      | 1. From the manager device, post 2 messages to a channel the CPO is a member of.<br>2. On the CPO device, observe the red 'Dept' bottom-tab badge (CpoNavigator useDeptUnreadTotal).<br>3. Open the Departmental module → Channels tab.<br>4. Observe the per-channel unread pill on that channel's row.<br>5. Post one more message from the manager and watch both badges update without a manual reload.                                                                                                                                                                                                                                        | The Dept tab badge (red #EF4444) shows the SUMMED unread across all dept channels, capped '9+' above 9. The channel row shows a per-channel unread pill capped '99+' above 99. Counts come from the encrypted messenger store (server hardcodes unread_count to 0 in listChannels). New posts increment both badges live. Note the agent-dashboard 'Departmental' row uses a DIFFERENT combined badge (pending-review/incident/attendance cue), not this unread total — verify you are reading the CPO tab badge.                                                                                                                                                                                                                                                                                                      | 2 devices — manager (poster) + ACTIVE CPO viewer                                                                    |                                                                                                       |                              |       |
| DCN-04 | P0  | golden     | Open a channel as viewer, read messages, and confirm opening zeroes the unread badge                                           | 1. Note the channel row's unread pill and the Dept tab total badge.<br>2. Tap the channel row.<br>3. Observe DepartmentChatScreen: header title, sub-label, pinned description notice (if any), message feed with per-sender names/colours + timestamps, and the composer area.<br>4. Read the messages, then go back to the channel list.<br>5. Re-check the channel row pill and the Dept tab total badge.                                                                                                                                                                                                                                       | Tapping opens DepartmentChatScreen showing the channel name and the 'Read-only · E2E encrypted' sub-label for a viewer. Messages render with each sender's stable accent colour + real display name (hydrated from the server roster on focus) and en-GB HH:mm timestamps. A viewer sees the 'You are a viewer' read-only bar (eye icon) instead of a composer. On focus setActiveConversation marks it read; on returning to the list the per-channel pill and the Dept-tab total for that channel drop to 0.                                                                                                                                                                                                                                                                                                         | 1 device — ACTIVE CPO viewer account                                                                                |                                                                                                       |                              |       |
| DCN-05 | P1  | edge       | Empty state renders when an entitled member has no department channels                                                         | 1. Open the Departmental module → Channels tab.<br>2. Let the directory finish loading (spinner clears).<br>3. Observe the body of the screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | After the loading spinner clears, the entitled-but-empty state shows: forum-outline icon, 'No channels yet', and the subtitle 'Your org admin creates department channels from the Ops console.' No section headers or rows render, and the screen does not crash or spin forever. This is DISTINCT from the not-entitled gate (DCN-10).                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 1 device — ACTIVE CPO member with no channels                                                                       |                                                                                                       |                              |       |
| DCN-06 | P1  | golden     | Navigate Home ↔ Channels ↔ chat; Home quick actions deep-link correctly                                                        | 1. Tap the Home tab.<br>2. Confirm the welcome line ('Welcome, <first name>'), org label ('<org> · Workspace'), 'Secure connection' card render (client review vs2 item 10 REMOVED the ANNOUNCEMENTS card — it must NOT appear).<br>3. Confirm NO Broadcast/Announcements card appears at the top of the dashboard.<br>4. Go back, then tap the 'Channels' quick-action card and confirm it opens the Channels tab.<br>5. Tap 'My attendance' (→ Attend tab) and 'Report incident' (→ Incident tab report wizard) quick actions.<br>6. From Channels open a channel and (as a viewer) confirm there is no admin Members button in the chat header. | Home renders the MEMBER layout (welcome, secure card, today's-attendance status card) titled "Channels", with NO announcement card. Quick actions deep-link: Channels→Channels tab, My attendance→Attend, Report incident→Incident{screen:'ReportIncidentCategory'}, Vault→Vault. Navigation is smooth with the single dept footer; a viewer's chat header shows no account-multiple (Members) button.                                                                                                                                                                                                                                                                                                                                                                                                                 | 1 device — ACTIVE CPO member account                                                                                |                                                                                                       |                              |       |
| DCN-07 | P0  | security   | Role-based entry — provider manager Home vs CPO member Home differ, and Manage entry is manager-only                           | 1. On the manager device open the Dept module → Home tab. Note tiles/cards.<br>2. Go to the Channels tab and check the header for a Manage (cog) button.<br>3. On the CPO device open the Dept module → Home tab. Note tiles/cards.<br>4. On the CPO Channels tab check the header for a Manage (cog) button.<br>5. Compare the Attend and Incident tab ROOT screens between the two roles.                                                                                                                                                                                                                                                        | Manager Home shows the 'NEEDS ATTENTION' tiles (Pending review count + Open incidents count) and manager quick actions ('Attendance / Review & approve', 'Incidents / Manage the queue'); the Channels header shows the cog. CPO Home shows the 'Today's attendance' status card (no counts, no incident detail) and member quick actions ('My attendance', 'Report incident'); the CPO Channels header has NO cog. Attend root = AdminAttendance (manager) vs Attendance (member); Incident root = IncidentQueue (manager) vs ReportIncidentCategory (member; My Reports is one tap away, from the row above the category grid). The role branch (is_org_manager, falling back to account_kind heuristic) only picks the first screen; server guards still decide, and no manager-only surface is exposed to the CPO. | 2 devices — provider manager + ACTIVE CPO member                                                                    |                                                                                                       |                              |       |
| DCN-08 | P1  | golden     | Pull-to-refresh on the channel list forces a fresh directory + provisioning-state refresh                                      | 1. On the member device note the current channel rows and any 'Not yet active' rows.<br>2. From the manager side, provision/activate a channel (or create a new one the member is added to).<br>3. On the member channel list, pull down to trigger the RefreshControl.<br>4. Observe the spinner and the updated list.                                                                                                                                                                                                                                                                                                                            | Pull-to-refresh shows the spinner, re-fetches listChannels (and best-effort drains membership intents), and the list updates: a newly activated channel flips from INACTIVE/'Not yet active' to its active badge/'Tap to open', and a new channel appears in its type group. No duplicate rows, no crash, spinner clears. Note focus also reloads (useFocusEffect) so re-entering the tab has the same effect.                                                                                                                                                                                                                                                                                                                                                                                                         | 2 devices — member + manager (or Ops console)                                                                       | D5-a                                                                                                  |                              |       |
| DCN-09 | P0  | security   | Dept scoping / no cross-org leak — CPO sees only own-org channels and no incident/restricted channel they aren't seeded into   | 1. As the Org A CPO, open the Dept module → Channels tab.<br>2. Enumerate every channel row shown.<br>3. Confirm no Org B channel appears.<br>4. Confirm the Org A incident/restricted managers-only channel does NOT appear.<br>5. If possible, attempt to deep-link/navigate to a foreign channel id and confirm no plaintext, roster, or unread is exposed.                                                                                                                                                                                                                                                                                     | Only channels the CPO is a member of appear — the server listChannels JOIN on department_channel_members is the enforcement, not any client-side hide. No Org B channel and no Org A managers-only incident/restricted channel the CPO wasn't seeded into. A foreign channelId yields no content: listMembers throws 403 (memberRole), the group has no master key on this device, and unread is 0 — access stays server-authoritative with no cross-org leak.                                                                                                                                                                                                                                                                                                                                                         | 2+ accounts across 2 orgs — Org A CPO (subject), Org B members, Org A manager (to create the managers-only channel) |                                                                                                       |                              |       |
| DCN-10 | P0  | security   | Revoked/suspended membership — a de-activated CPO hits the org-membership gate, not silent access                              | 1. As the revoked/non-member user, open the Dept module → Channels tab.<br>2. Observe whether channels load or the gate is shown.<br>3. Read the gate copy and confirm no channel rows/unread leak through.<br>4. Confirm the global recheckMembership / AccessEnded path is honored (no silent 403 crash).                                                                                                                                                                                                                                                                                                                                        | Client entitlement requires membership_status==='active' (mirrors DeptChatAccessGuard's org_members.status='active' and the company status='ACTIVE' path). A revoked CPO is NOT shown the directory — they see the gate screen (forum-outline icon, 'Department Channels', copy about a service-provider organisation workspace, 3 feature bullets, 'Ask your organisation admin for access.') with no real channel data. Separately, CpoNavigator's foreground recheckMembership fires endCpoAccess for a suspended guard, routing to the global AccessEnded flow rather than a leaked list or a raw 403.                                                                                                                                                                                                             | 1 device — revoked/non-member account                                                                               |                                                                                                       |                              |       |
| DCN-11 | P1  | error      | Offline / directory fetch failure — channel list degrades gracefully without crash                                             | 1. Open the Channels tab and let it load once online.<br>2. Enable airplane mode (fully offline).<br>3. Pull-to-refresh the channel list.<br>4. Observe list behavior and any error surface.<br>5. Re-enable connectivity and pull-to-refresh again.                                                                                                                                                                                                                                                                                                                                                                                               | On a failed listChannels the catch sets an empty list and the spinner clears (loading=false) — no infinite spinner, no unhandled error dialog, no stack trace shown to the user. Cached per-channel unread from the messenger store still renders where rows exist. Restoring connectivity + refresh repopulates the directory. (Note: an empty result after a failed fetch may briefly look like the empty state — acceptable, it self-heals on the next successful load.)                                                                                                                                                                                                                                                                                                                                            | 1 device — ACTIVE member account                                                                                    |                                                                                                       |                              |       |
| DCN-12 | P1  | edge       | Unprovisioned channel — INACTIVE badge, 'Not yet active' preview, and honest not-active chat state that clears on provisioning | 1. Open the Channels tab and locate the unprovisioned channel row.<br>2. Read its badge and preview text.<br>3. Tap the row to open the chat as a viewer.<br>4. Observe the chat body and composer/viewer bar.<br>5. Have an admin provision the channel (open it on their device), then WITHOUT leaving the member's still-open thread, refocus it.                                                                                                                                                                                                                                                                                               | Row badge = INACTIVE and preview = 'Not yet active' — badge and preview agree (D1-i, previously showed ACTIVE/ADMIN). Opening as a viewer shows the 'Channel not yet active' state (lock-clock icon + explanation) with no feed, no composer, and no viewer bar. Once an admin provisions the group, the second useFocusEffect re-reads listChannels and sets groupConversationId, so the not-active state clears in place without leaving the thread (D1-h).                                                                                                                                                                                                                                                                                                                                                          | 2 devices — viewer/member + admin (to provision)                                                                    | D1-i                                                                                                  |                              |       |
| DCN-13 | P0  | security   | Viewer cannot post; an admin downgraded to viewer loses the composer on focus and is blocked at send-time (no client dev-skip) | 1. As the admin user open the channel and confirm the composer (announcement toggle + input + send) and the Members (account-multiple) button are present.<br>2. From the manager side, downgrade that user's role from admin to viewer.<br>3. On the (former admin) device, refocus the channel (leave and return).<br>4. Confirm the composer is replaced by the 'You are a viewer' bar and the Members button is gone.<br>5. If the composer was cached, type a message and press send; observe the result.<br>6. As a pure viewer in another channel, confirm no composer ever shows.                                                          | A viewer never gets a composer — only the 'You are a viewer' bar; the admin-only Members button is hidden. After a downgrade, on next focus the roster re-hydration (listMembers) sets myRole=viewer, hiding the composer + Members button. Even from a stale composer, send() re-verifies via listMembers at send time and, when my_role!=='admin', blocks with 'Read-only — You no longer have permission to post in this channel.' The client role gate is the enforcement (group sends are E2EE/client-fanned) and there is NO dev-skip branch.                                                                                                                                                                                                                                                                    | 2 devices — subject user (admin→viewer) + manager/owner                                                             |                                                                                                       |                              |       |
| DCN-14 | P1  | regression | Exit the Departmental module returns to the correct host shell with no double footer; adjacent screens unaffected              | 1. From the CPO Dept tab, open the module.<br>2. Move across Channels/Attend/Home tabs, land on the Home tab, then tap the header chevron ('exit').<br>3. Confirm you return to the CPO tab shell (its footer visible, prior tab intact).<br>4. Repeat entry from the Agent dashboard 'Departmental' row; go to Home tab, exit, and confirm you return to the Agent dashboard.<br>5. While in the module, confirm only ONE footer (the dept 5-tab bar) is visible at a time.<br>6. After exiting, open an adjacent screen (Comms/Messenger home, dashboard) and confirm no regression.                                                             | The module is pushed full-screen (RootStack.Screen 'Departmental') so only its obsidian 5-tab footer shows — no stacked/double footer from the host tabs. The exit chevron lives on the HOME tab and calls navigation.getParent().goBack(), returning to the exact host shell it was opened from (CPO tabs or Agent dashboard). Adjacent screens render normally afterward; dept-channel groups do not leak into the general Messenger/Groups lists.                                                                                                                                                                                                                                                                                                                                                                   | 2 accounts — CPO (CpoNavigator entry) + provider/agent (AgentNavigator entry)                                       |                                                                                                       |                              |       |
| DCN-15 | P2  | edge       | Dept tab total unread updates live when a channel's encrypted group first materialises later in the session                    | 1. Log the CPO in while a channel of theirs is still unprovisioned (no group id) — note the Dept tab badge.<br>2. Have the admin provision that channel and add the CPO if needed; the CPO's messenger store gains a new group/ops_channel conversation slot.<br>3. From the manager, post a message to that channel.<br>4. Without remounting the app, observe the Dept tab badge.                                                                                                                                                                                                                                                                | useDeptUnreadTotal re-fetches the directory whenever the SET of group/ops_channel conversation slots changes (keyed by a stable sorted signature), so a channel whose group id materialised after mount now contributes to the badge. The Dept total increments for the new post without a full app remount (guards the F17 stale-once-fetch regression). A transient listChannels error keeps the prior id set (no badge blink to 0).                                                                                                                                                                                                                                                                                                                                                                                 | 2 devices — CPO member + manager/admin                                                                              | F17                                                                                                   |                              |       |
| DCN-16 | P1  | golden     | Manager Manage Channels screen — active/archived sections, member counts, activation hint, and New channel entry               | 1. As the manager, tap the Manage (cog) button in the Channels header.<br>2. Observe the ObHeader pill count and the CHANNELS section.<br>3. Read a row's sub-line: type · department · member count, plus any 'not active' suffix.<br>4. Scroll to confirm an ARCHIVED section lists archived channels separately.<br>5. Tap an existing row and confirm it opens the Channel editor pre-filled.<br>6. Tap the 'New channel' footer button and confirm it opens an empty ChannelEditor.<br>7. Pull-to-refresh and confirm the list reloads.                                                                                                       | Manage Channels opens (manager-only). The header pill shows the ACTIVE channel count. Active channels list under 'CHANNELS' with icon, name, and 'Type · Department · N member(s)' (singular/plural correct) plus a ' · not active' suffix when provisioned=false; archived channels appear under a separate 'ARCHIVED' section only if any exist. Empty active set shows 'No channels yet. Create the first one below.' Tapping a row opens ChannelEditor with {channel} pre-filled; 'New channel' opens an empty ChannelEditor; pull-to-refresh reloads listManagedChannels.                                                                                                                                                                                                                                         | 1 device — provider manager account                                                                                 |                                                                                                       |                              |       |
| DCN-17 | P0  | regression | Flag OFF — every Dept Chat v2 entry point is hidden and the module is unreachable                                              | 1. Boot as the CPO and inspect the bottom tab bar.<br>2. Confirm there are exactly 4 tabs (On Duty, Mission, Comms, Me) and NO 'Dept' tab.<br>3. Boot as the provider/agent and open the Agent dashboard; scan the action rows.<br>4. Confirm there is NO 'Departmental' row and no dept badges (all dept endpoints 404 → caught, badges stay empty).<br>5. Confirm the On-Duty home Departmental card (if any) is absent.                                                                                                                                                                                                                         | With the flag OFF, CpoNavigator renders only the four guard tabs (the source-scan capability test still sees exactly four Tab.Screen entries) — no CpoDept tab, so useDeptUnreadTotal badge is moot. The Agent dashboard omits the 'dept' row entirely and the dept-badge effect early-returns. There is no navigable path to the 'Departmental' route. This gates the whole area behind the flag (matches the fixed 'CPO Dept tab not flag-gated' bug).                                                                                                                                                                                                                                                                                                                                                               | 2 accounts — CPO + provider/agent, on a flag-OFF build                                                              |                                                                                                       |                              |       |
| DCN-18 | P1  | golden     | Admin first-open bootstraps an unprovisioned channel's encrypted group; needs-members path is honest                           | 1. As the admin, open the Channels tab and tap the unprovisioned channel row.<br>2. Observe the provisioning spinner replacing the row's unread slot (row disabled while provisioning === c.id).<br>3. Variant (a): with other members present, let ensureChannelProvisioned complete.<br>4. Variant (b): with no other reachable members, observe the alert.<br>5. After (a) succeeds, confirm the row updates and the chat opens with a live composer.                                                                                                                                                                                           | On admin open of a null-group channel, ensureChannelProvisioned runs (setProvisioning shows the row spinner and disables the row). Variant (a): status 'ok'/'already' registers the group (registerGroup, first-writer-wins COALESCE), the row's group_conversation_id is patched locally, and DepartmentChat opens with the admin composer. Variant (b): status 'needs_members' shows the alert 'Channel not active yet — Add a member to this channel first…' and does NOT navigate. A generic failure shows 'Could not open channel' with the real message. No plaintext/key is logged.                                                                                                                                                                                                                             | 1-2 devices — admin (subject); a 2nd member for variant (a)                                                         |                                                                                                       |                              |       |
| DCN-19 | P2  | edge       | Owner reactivates an ORPHANED channel that lost its master key on this device                                                  | 1. As the owner, open the Channels tab and tap the affected channel.<br>2. Observe the 'Reactivate channel?' alert.<br>3. Tap Cancel first and confirm nothing changes.<br>4. Reopen and tap 'Reactivate'.<br>5. Watch the provisioning spinner and the outcome; then send a test message.                                                                                                                                                                                                                                                                                                                                                         | Because the owner IS the key source, a re-share is impossible, so openChannel offers 'Reactivate channel?' (creates a fresh encrypted group + re-keys members; earlier messages stay unreadable). Cancel is a no-op. Reactivate calls departmentApi.resetGroup then ensureChannelProvisioned: on 'ok' the row's group id is patched and the chat opens as owner/admin able to send; 'needs_members' shows 'Channel reactivated — add a member to start messaging'; 'failed' shows 'Could not reactivate' with the message. Re-keyed members can decrypt new messages (fixes the 'explicit peer address'/empty-thread class).                                                                                                                                                                                           | 2 devices — owner (subject) + a member to re-key                                                                    |                                                                                                       |                              |       |
| DCN-20 | P0  | regression | First-writer-wins provisioning race — two admin devices open the same unprovisioned channel                                    | 1. On both admin devices, tap the same unprovisioned channel row at (near) the same time so both call ensureChannelProvisioned.<br>2. Let both registerGroup calls land.<br>3. On each device, send a message.<br>4. On a third member device, open the channel and read both messages.                                                                                                                                                                                                                                                                                                                                                            | registerGroup's COALESCE(group_conversation_id, $2) makes the first writer win: the losing device receives the already-registered id back (adopted=true) and ADOPTS it instead of its freshly minted group. Both admins and the member converge on ONE group/master key — no split into two keys, no 'CPO can't see messages' key divergence (B-35 class). Both messages are readable by all members.                                                                                                                                                                                                                                                                                                                                                                                                                  | 3 devices — two admins (racers) + one member reader                                                                 | B-35                                                                                                  |                              |       |
| DCN-21 | P1  | regression | Role resolution — a user who is a CPO of one org but a manager of another gets the MANAGER surface                             | 1. Log in as this dual-role user.<br>2. Open the Dept module → Home tab and note whether manager tiles show.<br>3. Open the Channels tab and check for the Manage (cog) button.<br>4. Open the Attend and Incident tabs and note the root screens.<br>5. Simulate a pre-flag cached session (is_org_manager undefined) and confirm the fallback heuristic still resolves sensibly.                                                                                                                                                                                                                                                                 | useIsManager / the isManager derivation PREFERS the server-resolved is_org_manager flag (mirrors OrgManagerGuard), so this user sees the MANAGER surface: NEEDS ATTENTION tiles on Home, the Manage cog on Channels, AdminAttendance + IncidentQueue roots — not the under-privileged member surface. When is_org_manager is undefined (stale cached session), it falls back to (role==='service_provider'                                                                                                                                                                                                                                                                                                                                                                                                             |                                                                                                                     | account_kind==='agency'). Authorization is still server-side; the branch only picks the first screen. | 1 device — dual-role account |       |                                                                                                                                                                                                                            |     |
| DCN-22 | P0  | security   | Member cannot enumerate managed channels even if the ManageChannels route is forced                                            | 1. As the member, confirm the Channels header has no Manage cog.<br>2. Force navigation to the 'ManageChannels' screen (e.g. deep link / debug nav).<br>3. Observe the list body and any error.<br>4. Attempt the underlying GET /department/manage/channels directly with the member's token.                                                                                                                                                                                                                                                                                                                                                     | The screen has no member entry point (cog is manager-only). If the route is reached, listManagedChannels calls GET /department/manage/channels which is OrgManagerGuard-protected → 403 org_manager_access_required; the screen's catch sets an empty list (no org channel metadata, member counts, or archived channels leak) and shows 'No channels yet…'. Visibility of the manage surface is server-authoritative, not just a hidden button.                                                                                                                                                                                                                                                                                                                                                                       | 1 device — ACTIVE CPO member account                                                                                |                                                                                                       |                              |       |
| DCN-23 | P1  | regression | Archived channel drops out of the member directory and the Dept-tab total but shows under manager ARCHIVED                     | 1. As the member, note the channel row in the Channels list and its contribution to the Dept tab total badge.<br>2. From the manager, archive the channel (POST /department/channels/:id/archive).<br>3. On the member device, pull-to-refresh the channel list (or refocus).<br>4. Observe the row and the Dept tab badge.<br>5. On the manager device open Manage Channels and find the channel.                                                                                                                                                                                                                                                 | listChannels filters c.archived_at IS NULL, so after refresh the archived channel disappears from the member directory; useDeptUnreadTotal's next directory fetch drops its group id, so its unread no longer counts toward the Dept tab total. On the manager's Manage Channels screen the channel now appears under the separate 'ARCHIVED' section (not counted in the active pill). No crash, no orphaned row.                                                                                                                                                                                                                                                                                                                                                                                                     | 2 devices — CPO member + manager                                                                                    |                                                                                                       |                              |       |
| DCN-24 | P1  | security   | Offline send-time role re-check degrades to the cached role — document the downgrade-while-offline window                      | 1. As the admin user, open the channel (composer present) and go fully offline (airplane mode).<br>2. From the manager side (online), downgrade the user to viewer.<br>3. On the still-offline user device, type a message and press send.<br>4. Observe whether the post is queued or blocked.<br>5. Restore connectivity, refocus the channel, and re-attempt a post.                                                                                                                                                                                                                                                                            | At send time send() tries listMembers to re-verify; OFFLINE that call throws and the catch falls back to the CACHED role gate (myRole still 'admin'), so the message is durably QUEUED rather than blocked — a known, documented window: the downgrade only takes effect once the device is back online. After reconnect + refocus, the focus roster re-hydration sets myRole=viewer (composer replaced by the viewer bar) and any further send is blocked with the read-only alert. Verify no bypass persists past reconnection and no dev-skip branch exists.                                                                                                                                                                                                                                                        | 2 devices — subject (admin→viewer, offline) + manager (online)                                                      |                                                                                                       |                              |       |
| DCN-25 | P2  | regression | App killed mid-thread — relaunch returns to a clean host shell with consistent unread, no double footer                        | 1. Open the Dept module and enter a channel thread.<br>2. Hard-kill the app (swipe from recents / force stop) while inside the thread.<br>3. Relaunch the app and log back in if required.<br>4. Observe where the app lands and whether any nested/double footer appears.<br>5. Open the Dept module again and check the channel's unread and the Dept tab total.                                                                                                                                                                                                                                                                                 | Relaunch lands on the normal CPO shell (not restored into a broken deep push with a double footer). Re-entering the module and the channel behaves normally; setActiveConversation/markRead on focus keeps unread consistent (the previously-read thread is not spuriously re-badged, and any messages that arrived while killed are counted). No crash from a torn-down store (the blur cleanup guards against it). recheckMembership on relaunch/foreground still gates access.                                                                                                                                                                                                                                                                                                                                      | 1 device — ACTIVE CPO member account                                                                                |                                                                                                       |                              |       |
| DCN-26 | P2  | edge       | Timezone — chat timestamps and Home 'today's attendance' chip use device-local time                                            | 1. Note a message's displayed time and the Home 'Today's attendance' chip.<br>2. Change the device timezone by several hours (crossing local midnight for a near-midnight message/shift).<br>3. Reopen the channel and the Home tab.<br>4. Compare the message time and the 'today' derivation before/after.                                                                                                                                                                                                                                                                                                                                       | Message timestamps render via toLocaleTimeString('en-GB', HH:mm) in DEVICE-LOCAL time (not raw server UTC) and shift with the timezone. The Home 'today' chip uses local isToday() (getFullYear/Month/Date), so a session that is 'today' locally is reflected; crossing local midnight can reclassify a borderline session as a prior day — verify it changes consistently with the device clock and never crashes or shows an invalid time. No off-by-one that mislabels a clearly-today session as 'No shift today' within the same local day.                                                                                                                                                                                                                                                                      | 1 device — ACTIVE CPO member account (timezone-changeable)                                                          |                                                                                                       |                              |       |
| DCN-27 | P2  | edge       | [RETIRED] Home announcement deep-link when the board channel is unprovisioned opens the honest not-active state                | 1. Open the Dept module → Home tab.<br>2. Confirm the ANNOUNCEMENTS card renders for the unprovisioned board channel and shows NO unread badge (chevron instead).<br>3. Tap the announcement card.<br>4. Observe the chat screen state.                                                                                                                                                                                                                                                                                                                                                                                                            | The announce card selects the board (else read_only) channel even when unprovisioned; announceUnread is 0 (guarded on null group id) so it shows the chevron, not a badge. Tapping deep-links to Channels→DepartmentChat with a null groupConversationId, which renders the 'Channel not yet active' state (lock-clock) rather than a fake feed; for a viewer no composer shows. If an admin later provisions it, the D1-h focus re-read clears the state in place.                                                                                                                                                                                                                                                                                                                                                    | 1 device — ACTIVE CPO member account                                                                                |                                                                                                       |                              |       | — RETIRED by client review vs2 item 10 — the Home announcement card no longer exists, so this case has no entry point. Superseded by the Channels directory row for the same channel (DCN-08 covers the not-active badge). |

## 2. Departmental Chat Messaging & E2EE Channel Provisioning (31 cases)

| ID      | Pri | Type       | Scenario                                                                                                | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Devices/Roles                                                                     | Bug  | Status | Notes |
| ------- | --- | ---------- | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------- | ---- | ------ | ----- |
| DMSG-01 | P0  | golden     | Golden path: manager posts to a provisioned dept channel and CPO receives it                            | 1. On the manager device open the 5-tab dept nav > Channels tab.<br>2. Under the 'Department' section tap the 'Operations' row (badge should read ADMIN, preview 'Tap to open').<br>3. Header shows 'Operations channel · E2E encrypted'; composer is visible.<br>4. Type 'Morning briefing 0800' and tap the send button.<br>5. On the CPO device open Channels > Operations.                                                                                                          | On the manager device the message appears immediately as an own (right-aligned, primary-colour) bubble with a HH:MM timestamp; send spinner clears. On the CPO device the message appears as a left-aligned bubble with the manager's real display name in a stable accent colour and the same body text. The relay never carried plaintext (E2E label present). No 'not yet active' state on either side.                                                                                                                                 | Provider manager device + CPO device (2 accounts, 2 devices)                      | D3-a |        |       |
| DMSG-02 | P1  | golden     | Member roster shows only correct dept members with real names and roles                                 | 1. Open the Operations channel as the manager.<br>2. Tap the members (account-multiple) icon in the header to open ChannelMembers.<br>3. Review the MEMBERS list and the header count pill.<br>4. Send a message from each CPO in turn and return to the chat thread.                                                                                                                                                                                                                   | ChannelMembers lists exactly the 3 seeded members (count pill = 3), obsidian-themed cards: each shows display name, initial avatar, and role line 'Manager · can post' / 'CPO · read only'. Own row is tagged '(you)' with no action buttons. In the chat thread each incoming bubble's sender label resolves to the real display name (not an 8-char userId prefix) in that sender's stable colour. No members from other departments/orgs appear.                                                                                        | Provider manager + 2 CPOs                                                         |      |        |       |
| DMSG-03 | P0  | security   | E2EE provisioning of a brand-new channel: agency device owns rekey, server never gets the key           | 1. Open Channels; confirm the new channel row shows the INACTIVE badge and 'Not yet active' preview.<br>2. Tap the new channel row (as admin).<br>3. Observe the per-row provisioning spinner while ensureChannelProvisioned runs.<br>4. Wait for it to resolve and the chat screen to open.<br>5. Return to Channels and pull-to-refresh.                                                                                                                                              | On tap the row shows a spinner, then the channel opens with a live composer (no 'not yet active'). The row badge flips from INACTIVE to ADMIN/ACTIVE and preview to 'Tap to open'. The Signal group is created on THIS device (master key held locally); only the group_conversation_id is registered server-side (first-writer-wins) — verify via backend/network that no key material is sent. A second admin device opening the same channel adopts the canonical id rather than forking.                                               | Provider manager device (+ optional 2nd admin device to check canonical adoption) | D1-a |        |       |
| DMSG-04 | P0  | security   | Forward secrecy: newly added CPO gets messages from join point forward only                             | 1. As manager open the channel and post 'Message BEFORE add'.<br>2. Open ChannelMembers > Add member, pick the 2nd CPO from the picker; wait for the row spinner to fully clear (drain-on-add awaited).<br>3. Without leaving the channel, post 'Message AFTER add'.<br>4. Log in as the 2nd CPO and open the channel.                                                                                                                                                                  | The 2nd CPO's thread shows ONLY 'Message AFTER add' (and any later posts) — the 3 pre-existing posts and 'Message BEFORE add' are NOT delivered (no historical backfill; sealed-sender has no replay = forward secrecy by design). The add row stays busy until the rekey broadcast completes, so 'Message AFTER add' is sealed to the new member and IS received.                                                                                                                                                                         | Provider manager + CPO(existing) + CPO(new)                                       | D2-b |        |       |
| DMSG-05 | P0  | security   | Removed CPO is rekeyed out and cannot decrypt subsequent posts                                          | 1. As manager open ChannelMembers, tap remove (account-remove) on the CPO, confirm the 'They are rekeyed out' alert.<br>2. Return to the channel list once (focus triggers drainMembershipIntents) so the remove intent drains and rekeys.<br>3. Re-open the channel and post 'After removal — secret'.<br>4. On the CPO device open the channel.                                                                                                                                       | The removed CPO no longer appears in the roster. After the rekey drains, the post 'After removal — secret' is sealed to the NEW epoch/member set only; the removed CPO cannot decrypt it (thread shows no new message / access ended). The server performed no rekey itself — the manager (admin) device drove planRemoveAndRekey.                                                                                                                                                                                                         | Provider manager + CPO                                                            |      |        |       |
| DMSG-06 | P1  | edge       | Message ordering preserved across mixed senders                                                         | 1. Manager posts A, then B in quick succession.<br>2. CPO(admin) posts C immediately after.<br>3. Manager posts D.<br>4. Compare the thread order on both devices; scroll to bottom.                                                                                                                                                                                                                                                                                                    | Both devices render the bubbles bottom-anchored in creation order A, B, C, D (feed justifies to flex-end and auto-scrolls to end on new content). No reordering, duplication, or interleaving anomaly; each sender keeps its stable accent colour and correct name.                                                                                                                                                                                                                                                                        | Provider manager + CPO promoted to admin                                          |      |        |       |
| DMSG-07 | P0  | security   | Viewer (read-only CPO) sees no composer and cannot post                                                 | 1. As the CPO open the channel.<br>2. Inspect the header subtitle and the bottom bar.<br>3. Attempt to find any way to type/send.                                                                                                                                                                                                                                                                                                                                                       | Header subtitle reads 'Read-only · E2E encrypted'. Bottom shows the viewer bar ('eye' icon + 'You are a viewer') instead of a composer. No TextInput, send, announcement toggle, or members icon is rendered. The viewer cannot post by any UI path.                                                                                                                                                                                                                                                                                       | CPO (viewer role)                                                                 |      |        |       |
| DMSG-08 | P0  | security   | Mid-session downgrade to viewer is blocked at send time                                                 | 1. Keep the promoted CPO on the open chat screen with a draft typed.<br>2. From the manager device open ChannelMembers and tap 'Make viewer' on that CPO.<br>3. On the CPO device tap send on the typed draft (do NOT re-focus/leave the screen first).                                                                                                                                                                                                                                 | At send time the client re-fetches listMembers, detects my_role !== 'admin', aborts the send, and shows the 'Read-only — You no longer have permission to post in this channel.' alert. The message is NOT broadcast. On next focus the composer is replaced by the viewer bar. This client gate is the enforcement (group sends have no server plaintext gate).                                                                                                                                                                           | Provider manager + CPO (being downgraded)                                         |      |        |       |
| DMSG-09 | P1  | error      | Offline send: draft preserved, error surfaced, no silent loss                                           | 1. Open the channel, type 'Offline test message'.<br>2. Enable airplane mode (no network).<br>3. Tap send.<br>4. Observe the alert and the composer.<br>5. Re-enable network and re-send.                                                                                                                                                                                                                                                                                               | If the send fan-out throws, a 'Could not post' alert shows the real reason and the draft text is restored into the composer (not lost). No fake 'delivered' bubble. After reconnecting, re-sending the restored draft succeeds and the message appears for recipients. (A merely-queued/durable offline post must not be thrown away.)                                                                                                                                                                                                     | Provider manager device                                                           |      |        |       |
| DMSG-10 | P1  | edge       | Banner / honest empty state when the channel is not yet provisioned                                     | 1. As the CPO open the not-yet-active channel from the list.<br>2. Read the empty state.<br>3. Keep the screen and have an admin provision it on another device.<br>4. Observe the CPO screen on next focus / pull-to-refresh from the list.                                                                                                                                                                                                                                            | Viewer sees the lock-clock 'Channel not yet active' state with the explanatory subtitle and NO composer (honest, not a fake feed). Once an admin provisions it, the D1-h focus re-read clears the state: the group id fills in and messages flow without the viewer having to leave and re-enter. The list badge shows INACTIVE until provisioned, then flips.                                                                                                                                                                             | CPO (viewer) + provider manager (to provision)                                    | D1-h |        |       |
| DMSG-11 | P0  | edge       | Master key missing on receiver: self-heal key resync fills the thread                                   | 1. Reinstall or clear the CPO app and log back in.<br>2. Open the dept channel.<br>3. Observe the thread while the on-focus hydration + requestGroupKeyResync runs.<br>4. Have the admin post a new message.                                                                                                                                                                                                                                                                            | On open the screen hydrates local participants from the server-authoritative departmentApi.listMembers (metadata only, never the key), giving the resync peers to ask. Because groups[gid].masterKeyB64 is absent, requestGroupKeyResync fires (rate-limited). Once the owner reshares, previously-undecryptable posts + the new post decrypt and render. No plaintext or key bytes are logged.                                                                                                                                            | CPO (reinstalled) + provider owner/admin device                                   | D2-d |        |       |
| DMSG-12 | P1  | error      | Owner reactivation of an orphaned channel (has group id, lost local key)                                | 1. As the owner open Channels and tap the orphaned channel row.<br>2. Read the 'Reactivate channel?' alert.<br>3. Tap Cancel first and confirm nothing changes.<br>4. Tap the row again and choose Reactivate.<br>5. After it completes, post a message and confirm current members receive it.                                                                                                                                                                                         | Tapping shows the consent alert warning 'Earlier messages stay unreadable.' Cancel leaves the channel untouched (no reset). Reactivate calls resetGroup + ensureChannelProvisioned: mints a fresh Signal group, re-registers the canonical id, re-keys current members, then opens the chat. New posts send/receive again. If no other member is reachable, the 'Channel reactivated — add a member' guidance shows instead of a dead composer. Only the OWNER path (created_by OR org account) offers this (non-owners are not prompted). | Provider owner device (+ a member device to confirm receipt)                      | D1-d |        |       |
| DMSG-13 | P1  | error      | Solo default channel with no other members shows 'needs members', not a dead composer                   | 1. As the company admin open Channels and tap the seeded 'Operations' default channel.<br>2. Observe the provisioning attempt result.                                                                                                                                                                                                                                                                                                                                                   | ensureChannelProvisioned returns 'needs_members' (createGroupChat has no other reachable member), and the UI shows the 'Channel not active yet — Add a member to this channel first…' alert and does NOT navigate into a dead-composer thread. No permanent silent 'not yet active'; no orphan key re-forged on each open (BUG-3 class).                                                                                                                                                                                                   | Provider company/admin account (solo org, no CPOs)                                | D3-c |        |       |
| DMSG-14 | P0  | security   | Tenant scoping: admin cannot add a cross-org / non-org user into the E2EE group                         | 1. As org-A manager open ChannelMembers > Add member.<br>2. Confirm the CPO picker (orgApi.listCpos) lists ONLY org-A active CPOs — no org-B users appear.<br>3. Attempt (via any available path) to add an org-B / non-org user.<br>4. Observe the server response.                                                                                                                                                                                                                    | The picker is scoped to org-A's active roster only; org-B users are never selectable. Any attempt to add a user not in the channel's org is rejected server-side ('member_not_in_org') and no membership row, intent, or rekey is created — the foreign user is never keyed into the E2EE group (DeptChatAccessGuard/tenant scoping upheld). No cross-org message leak.                                                                                                                                                                    | Org-A manager + (reference) org-B account                                         | D4-a |        |       |
| DMSG-15 | P2  | golden     | Announcement + @mention posts ride inside the E2EE body and render distinctly                           | 1. As manager tap the megaphone toggle (or type '/announce ') and post 'All hands 1500'.<br>2. Type '@' and pick the CPO from the autocomplete, complete the message 'please ack', send.<br>3. On the CPO device open the channel.                                                                                                                                                                                                                                                      | The announcement bubble shows the amber ANNOUNCEMENT header + megaphone and distinct styling; the leading zero-width guard prefix is not visible as text. The @mention renders as a styled token; on the CPO device the bubble where they are mentioned is highlighted (mentionSelf amber). Both ride inside the already-E2EE body — the relay sees only ciphertext and no parsed mention/announcement token is logged.                                                                                                                    | Provider manager + CPO                                                            |      |        |       |
| DMSG-16 | P2  | regression | Obsidian theming + no dev-skip on guards (visual/security regression sweep)                             | 1. Open ChannelMembers and verify the obsidian palette (OB.bg dark, AmbientBg, accentSoft cobalt) across header, cards, avatars, footer, and the add-member bottom sheet.<br>2. Confirm the 'Delete channel' footer action only appears for isOwner.<br>3. As a non-owner admin confirm delete is absent.<br>4. Sanity-check that role/tenant/provision guards have no 'skip in dev' branch (composer still hidden for viewers, add still tenant-checked).                              | ChannelMembers renders fully in the obsidian theme (matches design-system master) with no light-theme bleed or unstyled fallback. 'Delete channel' shows for the creator/owner only; a non-owner admin sees no delete affordance. No guard is bypassed in a dev/staging build — viewer composer stays hidden and cross-org add stays blocked (no weakened transition).                                                                                                                                                                     | Provider owner admin + non-owner manager                                          |      |        |       |
| DMSG-17 | P0  | security   | Cross-department isolation: CPO never sees or reaches a managers-only incident/restricted channel       | 1. As the CPO open Channels and scan every section (Board / Department / Incident).<br>2. Confirm the managers-only incident channel row is absent (listChannels is a membership JOIN, no client-side hide).<br>3. With the CPO token, directly call GET /department/channels/{incidentChannelId}/members.<br>4. Also try POST /department/channels/{incidentChannelId}/group and /members with the CPO token.                                                                          | The CPO never sees the incident/restricted row in the hub. The direct members probe is rejected 403 'not_a_channel_member' (memberRole throws for a non-member); registerGroup/addMember probes are rejected before any state change. No membership row, group id, or intent is created and no channel metadata for that channel leaks to the CPO. Enforcement is server-side, not merely presentation.                                                                                                                                    | CPO (non-member) + reference manager/admin                                        | D4-a |        |       |
| DMSG-18 | P0  | security   | Revoked/suspended access: de-activated CPO and suspended company lose the dept-chat workspace           | 1. As the CPO confirm the Channels hub loads real rows.<br>2. From ops, set the CPO's org_members.status to inactive/revoked.<br>3. On the CPO device pull-to-refresh / re-focus the Channels hub and open a previously-visible channel.<br>4. Repeat the spirit of the test for the company account: move agents.status off 'ACTIVE' and re-open the workspace.                                                                                                                        | After revocation the mobile entitlement gate (requires membership_status==='active') flips to the org-gate empty state, and the server DeptChatAccessGuard rejects every /department call with 403 'dept_chat_org_membership_required' — the client falls through to the global recheckMembership / AccessEnded flow, not a silent white screen. A suspended company (status<>'ACTIVE') is likewise denied (D4-c). No channel content is served after access ends.                                                                         | CPO device + ops console + provider company device                                | D4-c |        |       |
| DMSG-19 | P0  | security   | Server-side admin/owner guards are the real enforcement (defense-in-depth, no dev-skip)                 | 1. With the VIEWER token call: POST /channels/:id/group (register), POST /channels/:id/members (add), DELETE /channels/:id/members/:userId (remove), PATCH /channels/:id/members/:userId/role.<br>2. With the ADMIN token call DELETE /channels/:id/members/:selfId (remove self).<br>3. With a NON-OWNER token call DELETE /channels/:id (delete) and POST /channels/:id/reset-group.<br>4. With a NON-manager token call POST /channels (create) and PATCH /channels/:id (configure). | Every privileged call is rejected server-side even though the UI hides the control: viewer → 'only_admin_can_register_group' / 'only_admin_can_manage_members'; self-remove → 'cannot_remove_self'; non-owner delete → 'only_creator_can_delete'; non-owner reset → 'only_owner_can_reset'; non-manager create/configure → OrgManagerGuard 403. No 'skip in dev/staging' branch weakens any check; no state mutates on a rejected call.                                                                                                    | Provider admin + viewer CPO + non-owner manager (API-level)                       | D4-b |        |       |
| DMSG-20 | P0  | security   | Tightening a standard channel to managers-only rekeys CPOs out and aborts on partial failure            | 1. As manager change the channel access to 'restricted' (or type to 'incident').<br>2. Confirm each CPO viewer is removed via the removeMember path (a remove+rekey intent is enqueued per CPO) and drained by an admin device.<br>3. Post a message and confirm no former CPO viewer can decrypt it.<br>4. Failure leg: force one CPO's removal to fail during the tighten and re-run.<br>5. Loosen the channel back to 'standard' and confirm CPO viewers are re-seeded.              | Tightening does NOT bare-flip the access column: each CPO viewer is removed + rekeyed out (Step-12 seam), so a de-scoped CPO loses the master key and cannot read subsequent posts. If any removal fails, configureChannel throws 'channel_tighten_incomplete' and does NOT flip access — no CPO is left holding the old key while shown as excluded. Loosening back re-adds each CPO via addMember (add+rekey intent, D7-b) so the key is redelivered — not a bare membership insert.                                                     | Provider manager + CPO viewers                                                    | D7-b |        |       |
| DMSG-21 | P0  | edge       | Concurrent provisioning race: two admin devices converge on one canonical group (no key split)          | 1. On both admin devices open the same not-yet-active channel at (near) the same instant so both run ensureChannelProvisioned.<br>2. Each device mints a Signal group and calls registerGroup.<br>3. Add a CPO and have the CPO open the channel.<br>4. From each admin device post a message.                                                                                                                                                                                          | The server COALESCE (first-writer-wins) keeps ONE canonical group_conversation_id; the losing device re-fetches listChannels and ADOPTS the canonical id instead of navigating into its own fork (adopted=true). Members are not split across two master keys (B-35 key-divergence class): the CPO decrypts posts from BOTH admin devices. No 'CPO can't see messages' from a diverged epoch.                                                                                                                                              | 2 admin devices + 1 CPO                                                           | D1-f |        |       |
| DMSG-22 | P1  | error      | Provisioning hard-failure surfaces an honest error, not a silent permanent 'not yet active'             | 1. As admin tap the not-yet-active channel row.<br>2. Observe the row spinner and the outcome when ensureChannelProvisioned returns status 'failed'.<br>3. Re-tap the row a few times.                                                                                                                                                                                                                                                                                                  | The per-row spinner stops and an 'Could not open channel' alert shows the REAL failure message (not swallowed). The channel is NOT entered with a dead composer and is NOT marked provisioned. Re-tapping does not silently re-forge a fresh master key on every open (D1-a/f/g) — it retries the same honest path. No permanent silent 'not yet active' with no explanation.                                                                                                                                                              | Provider admin device                                                             | D1-a |        |       |
| DMSG-23 | P1  | edge       | App killed mid-provision recovers to a single canonical group                                           | 1. As admin tap the channel to provision; force-kill the app immediately after the local group is created but before/while registerGroup runs.<br>2. Relaunch and re-open the channel.<br>3. Have the CPO open the channel and exchange a message both directions.                                                                                                                                                                                                                      | On relaunch the channel resolves to a single canonical group: if registerGroup had committed, the id is adopted; if not, ensureChannelProvisioned re-registers (first-writer-wins keeps one id). The channel does not stay stuck 'not yet active' and members are not split across two master keys. The CPO can decrypt subsequent posts; no orphaned key that re-forges on each open.                                                                                                                                                     | Provider admin device + CPO                                                       | D1-d |        |       |
| DMSG-24 | P2  | edge       | Timezone / device-clock: local HH:MM per device, server-order stable                                    | 1. Manager posts 'Timestamp check' at a known instant.<br>2. Compare the timestamp shown on each device.<br>3. Send several interleaved posts from both devices, including from the clock-skewed device.<br>4. Compare thread ORDER on both devices.                                                                                                                                                                                                                                    | formatTime renders each device's LOCAL HH:MM (toLocaleTimeString en-GB, hour+minute only, no date), so the two devices may show different clock labels for the SAME post — expected, not a bug. Thread ORDER is identical on both devices because it follows the server-assigned created_at, not the device clock; a skewed local clock does NOT reorder or misplace messages. No 'stale message' artifact from clock drift.                                                                                                               | 2 devices in different timezones (one clock-skewed)                               |      |        |       |
| DMSG-25 | P2  | edge       | Announcement false-positive guard: a user-typed megaphone is not styled as an announcement              | 1. As admin, WITHOUT the megaphone toggle or /announce, type a normal message beginning with a literal '📣 ' (megaphone + space), e.g. '📣 lunch is ready', and send.<br>2. Separately send a real announcement via the toggle.<br>3. On the recipient device inspect both bubbles.                                                                                                                                                                                                     | The user-typed '📣 lunch is ready' renders as an ORDINARY bubble — no amber ANNOUNCEMENT header/border — because the renderer keys off the zero-width-space-guarded ANNOUNCE_PREFIX, not a bare emoji. Only the toggle/'/announce' path produces the announcement styling. The megaphone the user typed shows as normal body text.                                                                                                                                                                                                         | Provider admin + recipient                                                        |      |        |       |
| DMSG-26 | P2  | error      | Empty / whitespace / bare-command send is a no-op                                                       | 1. With an empty composer, observe the send button state.<br>2. Type only spaces/newlines and try to send.<br>3. Turn on the announce toggle, type only '/announce' with no body, and send.<br>4. Type real text and send.                                                                                                                                                                                                                                                              | The send button is disabled while the trimmed draft is empty; a whitespace-only draft cannot be sent (no empty bubble is created). A bare '/announce' with no body sends nothing and the announce toggle remains ON until an actual announcement is posted. Only the real-text send produces a bubble and clears/resets the composer and toggle.                                                                                                                                                                                           | Provider admin device                                                             |      |        |       |
| DMSG-27 | P2  | edge       | Sender who left the channel: label degrades gracefully, no crash                                        | 1. Have a CPO post a couple of messages, then remove that CPO (roster no longer maps their name).<br>2. As a remaining member open the thread and scroll to the former member's old bubbles.<br>3. Confirm your own historical bubbles still right-align.<br>4. Have the removed CPO attempt to post again after the rekey.                                                                                                                                                             | The former member's old bubbles fall back to a stable-coloured short userId prefix (memberNames no longer has the name) rather than crashing or showing a blank sender. Own historical bubbles still resolve via sender_id==='self'/myId and right-align. The removed CPO's FUTURE posts do not arrive (they were rekeyed out). No error thrown, no key/plaintext logged.                                                                                                                                                                  | Remaining member + removed CPO                                                    |      |        |       |
| DMSG-28 | P1  | regression | Log-audit regression: no plaintext body or key bytes in logs on provision/send/receive                  | 1. Attach logcat and enable app verbose logs.<br>2. Provision a channel, send a message (including an announcement + @mention), and receive it on the second device.<br>3. Trigger the '[dept.chat] pull skipped' path (e.g. transient runtime error) and the key-resync path.<br>4. Grep the captured logs for the message body text, master key, and ArrayBuffer key bytes.                                                                                                           | No decrypted message body, media key, or master key material appears in any log line. '[dept.chat] pull skipped' logs only an Error.message; provisioning/rekey log metadata only (ids, counts). The static logAudit test (packages/messenger-core **tests** and legacy mobile test) still passes and cannot be bypassed by variable renaming. Announcement/mention parsing is never logged.                                                                                                                                               | 2 devices + logcat host                                                           |      |        |       |
| DMSG-29 | P1  | golden     | Unread badge lifecycle: increments off-screen, zeroes on open                                           | 1. With the CPO NOT on the channel, have the manager post 2 messages.<br>2. Observe the per-channel unread badge on the Channels hub row and the CPO Dept tab badge.<br>3. Open the channel as the CPO.<br>4. Return to the hub.                                                                                                                                                                                                                                                        | The unread badge (ChannelUnread from messengerStore) shows the count on the channel row and the CpoNavigator tab badge increments; unread is a purely local concept (the relay held only ciphertext). Opening the channel (markRead ack + setActiveConversation) zeroes the per-channel unread; on returning to the hub the badge is cleared. 99+ cap renders for large counts.                                                                                                                                                            | Provider manager + CPO                                                            | D5-a |        |       |
| DMSG-30 | P1  | error      | Delete channel (creator-only) propagates removal to all members                                         | 1. As the creator open ChannelMembers and use the 'Delete channel' footer action; confirm the 'removes it for everyone' alert.<br>2. On the CPO device pull-to-refresh / re-focus the Channels hub.<br>3. With a NON-owner member's token, directly call DELETE /department/channels/:id.                                                                                                                                                                                               | The creator's delete cascades (members + membership intents removed via FK) and navigates back. On the CPO's next list focus/refresh the channel row is gone. The non-owner direct DELETE is rejected 403 'only_creator_can_delete' and nothing is deleted. A non-owner never sees the 'Delete channel' affordance in the UI.                                                                                                                                                                                                              | Provider owner + CPO + (API) non-owner                                            |      |        |       |
| DMSG-31 | P1  | edge       | Role toggle is instant metadata with NO rekey (demoted admin keeps decrypt, promoted viewer gains post) | 1. As manager open ChannelMembers and tap 'Allow post' on the CPO (viewer→admin).<br>2. On the CPO device re-focus the channel and post a message.<br>3. As manager tap 'Make viewer' on that CPO (admin→viewer).<br>4. On the CPO device re-focus the channel and inspect the composer; confirm they can still READ existing/new posts.                                                                                                                                                | 'Allow post' is a metadata-only updateMemberRole (no drain/rekey intent needed): after the CPO's next focus the composer appears and they can post. 'Make viewer' removes their composer but they still DECRYPT existing and new posts — by design they retain the master key (contrast with remove/DCM-05 which DOES rekey them out). No key redistribution or thread breakage occurs on either toggle.                                                                                                                                   | Provider manager + CPO                                                            |      |        |       |

## 3. Channel Administration (Create / Edit / Members) (33 cases)

| ID      | Pri | Type       | Scenario                                                                                                         | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Devices/Roles                                         | Bug                                                                                                                                                                                                                                                                                                                                                                                                                        | Status                             | Notes |
| ------- | --- | ---------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ----- | --- | --- |
| DADM-01 | P0  | golden     | Create a standard department channel (name + department + type + access)                                         | 1. Open Channels hub, tap the manage/gear entry to open Manage Channels.<br>2. Tap 'New channel'.<br>3. Enter Name 'Operations East'.<br>4. Enter Department 'Operations'.<br>5. Leave Type = Department, Access = Standard.<br>6. Tap 'Create channel'.                                                                                                                                                                                                                                                           | Primary button shows a busy spinner then the editor pops back to Manage Channels. New channel appears in CHANNELS (active) with icon, name, subtitle 'Department · Operations · N members' and a state badge. createChannel seeds the org account as admin PLUS every active org member (managers as admin/'Manager', CPOs as viewer/'CPO') via seedChannelMembers, so member_count = org + managers + CPO viewers. group_conversation_id is NULL at create; ensureChannelProvisioned fires lazily, so the row may read '· not active' until an admin device bootstraps the Signal group. A channel.create audit row is written. | Provider manager (1 device)                           |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-02 | P1  | error      | Empty / whitespace-only channel name is rejected client-side                                                     | 1. Open 'New channel'.<br>2. Leave Name blank (or type only spaces).<br>3. Tap 'Create channel'.                                                                                                                                                                                                                                                                                                                                                                                                                   | Alert 'Channel — Give the channel a name.' is shown (name.trim() empty guard). No network request, no navigation, no channel created. Entering a valid trimmed name then saving succeeds. Defence-in-depth: even a crafted empty body is rejected server-side by CreateChannelDto @Length(1,80).                                                                                                                                                                                                                                                                                                                                 | Provider manager (1 device)                           |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-03 | P1  | golden     | Edit an existing channel's name / department / access and save                                                   | 1. Manage Channels -> tap an existing channel row to open Edit Channel.<br>2. Fields pre-populate with current name, department, type, access.<br>3. Change Name to 'Operations West' and switch Access to 'Read only'.<br>4. Tap 'Save changes'.<br>5. Reopen the channel row.                                                                                                                                                                                                                                    | Save issues PATCH /department/channels/:id; the service applies COALESCE for name/type/access (only changed fields move). List row and re-opened editor reflect the new name and Read-only badge. No duplicate row is created. Department clears when emptied: the client sends department:null but note the SERVER clear sentinel is '' (empty string) — verify the client-null path still leaves the column unchanged vs the '' explicit-clear path (D7-c) actually nulls it. A channel.configure audit row is written.                                                                                                        | Provider manager (1 device)                           |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-04 | P0  | golden     | Add a member triggers E2EE rekey; Add row stays busy until the awaited drain lands                               | 1. Manage Channels -> open channel -> 'Members' -> 'Add member'.<br>2. In the picker (orgApi.listCpos roster; active-only, excludes existing members), tap an addable CPO.<br>3. Observe the row spinner.<br>4. Wait for the sheet to close and the member list to refresh.<br>5. As the manager, post a message in the channel.<br>6. On the CPO device, open the channel.                                                                                                                                        | Picked row shows a spinner and stays busy while addMember (upsert membership + enqueueIntent) then AWAITED drainMembershipIntents (add + rekey broadcast) complete BEFORE control returns (audit D2-b). New member appears as 'CPO · read only'. The subsequent manager post is readable by the newly added CPO (no 'waiting for encryption key' banner), proving the rekey landed before the post. Note the picker also lists managers (member_role==='manager' -> added as admin/'Manager').                                                                                                                                   | Provider manager + CPO (2 devices)                    | B-35                                                                                                                                                                                                                                                                                                                                                                                                                       |                                    |       |
| DADM-05 | P0  | golden     | Remove a member — confirm dialog, member rekeyed out, cannot read later posts                                    | 1. Members screen -> tap the account-remove icon on the CPO row.<br>2. Confirm dialog 'Remove <name> ... They are rekeyed out.' -> tap 'Remove'.<br>3. Wait for the list to refresh.<br>4. As the manager, post a new message.<br>5. On the removed CPO device, attempt to view the channel.                                                                                                                                                                                                                       | DELETE /members/:userId deletes the membership row (RETURNING) and enqueues a remove+rekey intent; the admin device broadcasts the rekey. Note: unlike add, doRemove does NOT await drainMembershipIntents — the rekey broadcast rides the next intent drain (on Members/hub focus). CPO disappears from Members. New posts after the rekey lands are NOT decryptable by the removed CPO; the channel drops off their hub (membership JOIN no longer returns it). member_not_found on a double-remove is treated benign/idempotent.                                                                                              | Provider manager + CPO (2 devices)                    | B-35                                                                                                                                                                                                                                                                                                                                                                                                                       |                                    |       |
| DADM-06 | P1  | golden     | Toggle member access viewer <-> admin (Allow post / Make viewer) is metadata-only, no rekey                      | 1. Members screen -> on a CPO 'read only' row tap 'Allow post'.<br>2. Observe row spinner then refresh.<br>3. Tap 'Make viewer' to revert.                                                                                                                                                                                                                                                                                                                                                                         | PATCH /members/:userId/role flips role admin<->viewer (updateMemberRole). Row subtitle updates between 'can post' and 'read only'. NO intent is enqueued and NO rekey — the member keeps decrypting existing content (they already hold the key). On the CPO device the compose box becomes enabled/disabled to match, with no key-rotation banner.                                                                                                                                                                                                                                                                              | Provider manager + CPO (2 devices)                    |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-07 | P0  | security   | Tighten a standard channel to Restricted rekeys all CPO viewers out                                              | 1. Edit Channel -> change Access from Standard to 'Restricted'.<br>2. Tap 'Save changes'.<br>3. Re-open Members.<br>4. As the manager, post a message.<br>5. On a de-scoped CPO device, open the channel.                                                                                                                                                                                                                                                                                                          | configureChannel detects !wasManagersOnly && nowManagersOnly, selects all role='viewer' rows and removes each via removeMember (each gets a remove+rekey intent) BEFORE the access column flips. Members list now shows managers/admins only; CPOs gone. New posts not decryptable by removed CPOs; channel drops off their hub. No CPO retains the old group master key (§0.3 / Step-12 seam).                                                                                                                                                                                                                                  | Provider manager + CPO (2 devices)                    | B-35                                                                                                                                                                                                                                                                                                                                                                                                                       |                                    |       |
| DADM-08 | P0  | error      | Tighten aborts (channel_tighten_incomplete) if a CPO rekey-out fails — no bare access flip                       | 1. Edit Channel -> switch Access to Restricted while one viewer removal will fail.<br>2. Tap 'Save changes'.<br>3. Observe the error.<br>4. Re-open the channel editor and Members and re-check Access + roster.                                                                                                                                                                                                                                                                                                   | Save returns BadRequest 'channel_tighten_incomplete'; the editor surfaces the message in an alert and does NOT navigate back. The access column is NOT flipped to restricted. IMPORTANT partial-state check: the CPOs whose removeMember DID succeed are ALREADY gone from the roster (and hold pending remove+rekey intents) even though the channel is still 'standard' — verify this transient inconsistency and that a retry after connectivity restores converges (already-removed members -> member_not_found -> benign/idempotent).                                                                                       | Provider manager (1 device) + network fault injection | B-35                                                                                                                                                                                                                                                                                                                                                                                                                       |                                    |       |
| DADM-09 | P1  | edge       | Loosen a managers-only channel back to Standard re-seeds CPO viewers with a fresh key                            | 1. Edit Channel -> switch Access from Restricted back to 'Standard'.<br>2. Tap 'Save changes'.<br>3. Open Members.<br>4. On a re-added CPO device, open the channel and read a new manager post.                                                                                                                                                                                                                                                                                                                   | configureChannel detects wasManagersOnly && !nowManagersOnly and re-adds each active org CPO via addMember (add+rekey intent so the admin device redelivers the master key). Members list shows the CPOs again as viewers. The re-added CPO can decrypt NEW posts (a bare membership insert would NOT redeliver the key; this path does). Managers are skipped (member_role==='manager' continue). Re-add failures are logged and non-fatal (best-effort loop).                                                                                                                                                                  | Provider manager + CPO (2 devices)                    |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-10 | P0  | security   | Non-manager (plain CPO/member) cannot open Manage Channels or create/edit — 403                                  | 1. As the CPO, attempt to reach Manage Channels (the manager-only entry should not be exposed; if reachable, load /department/manage/channels).<br>2. If any create/edit/archive request can be crafted, attempt POST /department/channels or PATCH /department/channels/:id.                                                                                                                                                                                                                                      | listManagedChannels/createChannel/configureChannel/archiveChannel are rejected by OrgManagerGuard -> 403 org_manager_access_required (the guard RE-READS agents/org_members from the DB, so a stale JWT can't fabricate manager identity). The member CAN still list+read channels they belong to (DeptChatAccessGuard admits active org members) but cannot administer them. There is NO dev-skip branch bypassing the guard.                                                                                                                                                                                                   | Plain CPO/member (1 device)                           |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-11 | P0  | security   | Cross-org scoping — a manager cannot edit/archive another org's channel (org_scope_violation)                    | 1. Log in as the Org A manager.<br>2. Attempt PATCH /department/channels/:idB or POST /department/channels/:idB/archive targeting Org B's channel id.<br>3. Observe the response.                                                                                                                                                                                                                                                                                                                                  | assertManagesChannel loads the channel and asserts ch.org_id === the caller's org_user_id; mismatch throws 403 org_scope_violation. Org A manager cannot rename, reconfigure, or archive Org B's channel. listOrgChannels is WHERE org_id = caller's org, so Org B channels never appear in Org A's Manage list.                                                                                                                                                                                                                                                                                                                 | Org A manager + Org B channel (cross-tenant)          |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-12 | P0  | security   | Add-member rejects a user outside the channel's org (no cross-org rekey into E2EE group)                         | 1. As the Org A channel admin, craft POST /department/channels/:id/members with a user_id outside Org A.<br>2. Observe the response.                                                                                                                                                                                                                                                                                                                                                                               | addMember re-validates the target belongs to THIS channel's org (memberUserId === ch.org_id OR an ACTIVE org_members row); otherwise 403 member_not_in_org (audit D4-a). The cross-org / non-org user is NOT inserted and NOT rekeyed into the group, so DeptChatAccessGuard's org entitlement cannot be bypassed via membership. The picker itself only lists active org roster entries (orgApi.listCpos), so this stays defence-in-depth.                                                                                                                                                                                      | Org A channel admin (1 device) + crafted request      |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-13 | P1  | golden     | Member list accuracy — roster, roles, counts, and 'you' marker match server state                                | 1. Open Members.<br>2. Compare rendered rows to the expected roster.<br>3. Note the header pill count.<br>4. Locate your own row.                                                                                                                                                                                                                                                                                                                                                                                  | Members screen lists exactly the department_channel_members rows (display_name from users JOIN, role_label 'Manager'/'CPO' or fallback 'Admin'/'CPO', and 'can post' vs 'read only' derived from role==='admin'). Header pill equals members.length. Your own row shows '(you)' and has NO remove/access buttons (isMe gate). Add-member picker excludes users already in the channel (memberIds set) and any non-active roster entries (status==='active' filter).                                                                                                                                                              | Provider manager (1 device)                           |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-14 | P1  | golden     | Archive channel — confirm dialog, moves to ARCHIVED, members stop seeing it                                      | 1. Edit Channel -> tap 'Archive channel'.<br>2. Confirm the destructive dialog.<br>3. Return to Manage Channels.<br>4. On a CPO device, refresh the Channels hub.                                                                                                                                                                                                                                                                                                                                                  | POST /department/channels/:id/archive sets archived_at=NOW(); editor pops back. In Manage Channels the channel renders under ARCHIVED (ORDER BY archived_at flag then created_at; not counted in the active pill). listChannels filters c.archived_at IS NULL, so the CPO no longer sees it in their hub. Member rows are NOT deleted (archive != delete). The Archive button is hidden once archived (!editing.archived guard). A channel.archive audit row is written. Re-activation is only via re-config, not this screen.                                                                                                   | Provider manager + CPO (2 devices)                    |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-15 | P1  | security   | Delete channel is creator-only; Delete button hidden unless Members opened as owner                              | 1. As the creator, open the channel from the Channels hub (DepartmentChannelsScreen sets isOwner = created_by === my id) -> chat header 'Members' passes isOwner:true -> confirm 'Delete channel' button is present; tap it and confirm to delete.<br>2. As a non-creator manager, reach Members via the Edit-Channel 'Members' ghost button (which passes NO isOwner) and confirm the Delete button is hidden; then craft DELETE /department/channels/:id.<br>3. Note the two navigation entry points to Members. | deleteChannel enforces created_by === caller -> a non-creator (even a manager, even the org account if not creator) gets 403 only_creator_can_delete. UI-wise the Delete button renders only when isOwner===true; the Edit-Channel 'Members' path intentionally omits isOwner so Delete is hidden there. Deletion cascades member + intent rows via FK and pops back to the list; a channel.delete audit row is written.                                                                                                                                                                                                         | Creator manager + non-creator manager (2 accounts)    |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-16 | P1  | error      | Save/network failure and offline — error alert shown, no navigation, no partial create                           | 1. Fill a valid channel name.<br>2. Enable airplane mode (or force the request to fail).<br>3. Tap 'Create channel' / 'Save changes'.<br>4. Observe the busy state and result.<br>5. Restore connectivity and retry.                                                                                                                                                                                                                                                                                               | Button shows busy then the request fails; alert 'Channel — <server message or Could not save. Please try again.>' shows. Editor does NOT navigate back; no channel row appears (no phantom create). busy resets to false so the button is tappable again; the in-flight double-tap is blocked by the `if (busy) return` guard. Retrying online succeeds exactly once (no duplicate from the failed attempt).                                                                                                                                                                                                                     | Provider manager (1 device) + offline/fault injection |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-17 | P2  | error      | Cannot remove self from a channel (cannot_remove_self)                                                           | 1. Open Members.<br>2. Confirm your own row has no remove/access controls.<br>3. Craft DELETE /department/channels/:id/members/:ownId.                                                                                                                                                                                                                                                                                                                                                                             | UI never renders remove/access buttons on the self row (isMe gate). At the API, removeMember rejects memberUserId === adminUserId with 403 cannot_remove_self, so an admin cannot orphan the channel of its last admin via self-removal.                                                                                                                                                                                                                                                                                                                                                                                         | Provider manager (1 device)                           |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-18 | P2  | edge       | Incident-type channel is treated managers-only regardless of chosen access                                       | 1. New channel -> set Type = 'Incident', Access = 'Standard'.<br>2. Observe the note under the Access list.<br>3. Save, then check the seeded roster.<br>4. Verify a CPO's hub does not show the incident channel.                                                                                                                                                                                                                                                                                                 | Amber note 'Incident channels are managers-only regardless of access.' appears whenever type==='incident' && access!=='restricted'. On create, seedChannelMembers computes managersOnly = access==='restricted'                                                                                                                                                                                                                                                                                                                                                                                                                  |                                                       | channel_type==='incident', so CPOs are never seeded and the incident channel never surfaces on a CPO's hub (membership JOIN). Managers are seeded as admin.                                                                                                                                                                                                                                                                | Provider manager + CPO (2 devices) |       |     |     |
| DADM-19 | P1  | security   | Manually adding a CPO viewer to an incident/restricted channel is NOT blocked (managers-only invariant gap)      | 1. Open the incident channel -> Members -> Add member.<br>2. Pick a plain CPO from the roster (the picker lists all active org roster entries, not just managers).<br>3. Observe the result and whether the CPO can now see/read the incident channel.                                                                                                                                                                                                                                                             | EXPECTED-BY-DESIGN CHECK / likely defect: addMember only validates org membership + admin caller — it does NOT re-apply the managers-only rule that seedChannelMembers enforces. So a manager CAN insert a CPO viewer into an incident/restricted channel and (once the rekey intent drains) that CPO can read incident content, contradicting the Step-12 'CPOs never see incident channels' invariant and the seeding logic. Verify actual behavior; if the CPO gains read access, log as a finding (managers-only should be enforced on add, not just on seed/tighten).                                                       | Provider manager + CPO (2 devices)                    |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-20 | P1  | edge       | App killed mid add-member (before rekey drain acks) — intent persists, converges on next focus, no duplicate row | 1. Add member -> while the awaited drainMembershipIntents is in flight (row still busy), force-kill the app.<br>2. Relaunch and reopen Members (or the Channels hub).<br>3. On the CPO device open the channel and read a post the manager sends after relaunch.                                                                                                                                                                                                                                                   | The membership row (ON CONFLICT upsert) and the pending 'add' intent are already durable server-side. On relaunch, the admin device re-drains listMembershipIntents on Members/hub focus and broadcasts the add+rekey; the CPO then decrypts subsequent posts. No duplicate membership row (upsert), and the intent flips to 'done' only after ackMembershipIntent. A post sent BETWEEN the interrupted add and the successful re-drain may be undecryptable to the CPO (sealed-sender has no replay) — verify only post-rekey messages arrive.                                                                                  | Provider manager + CPO (2 devices)                    | B-35                                                                                                                                                                                                                                                                                                                                                                                                                       |                                    |       |
| DADM-21 | P2  | edge       | Add member to a not-yet-provisioned channel (no group id) — membership + intent queued, no error, rekey deferred | 1. From Manage Channels confirm the channel shows '· not active'.<br>2. Open Members -> Add member -> pick a CPO.<br>3. Observe the result.<br>4. Later, have an admin device open the channel to bootstrap the Signal group.                                                                                                                                                                                                                                                                                      | addMember writes the membership row + 'add' intent successfully (no group id needed to enqueue). drainMembershipIntents is best-effort and skips channels with a null group_conversation_id (nothing to rekey yet) without surfacing an error. When an admin device later bootstraps + registers the group, the seeded roster and the pending intents converge so the CPO ends up keyed in. The Add flow shows no failure alert.                                                                                                                                                                                                 | Provider manager + CPO (2 devices)                    |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-22 | P1  | edge       | Concurrent group provisioning — registerGroup is first-writer-wins; loser adopts the canonical id                | 1. On device A and device B, open the un-provisioned channel so each mints a group and calls POST /department/channels/:id/group.<br>2. Compare the responses.<br>3. Have both devices and a CPO post/read.                                                                                                                                                                                                                                                                                                        | registerGroup uses COALESCE(group_conversation_id, $2) in a single atomic UPDATE, so the FIRST writer's group id is kept. The RETURNING value is the canonical id; the losing device receives adopted:true when effective !== its own id and MUST adopt the returned group (not keep its freshly-minted one). This prevents split master keys / 'CPO can't see messages' (B-35 class). Verify all members end up on ONE group id and messages from both admins are mutually decryptable.                                                                                                                                         | 2 admin devices + CPO (3 devices)                     | B-35                                                                                                                                                                                                                                                                                                                                                                                                                       |                                    |       |
| DADM-23 | P0  | security   | Membership-intent list/ack is admin-scoped — non-admins cannot drain or ack another channel's intents            | 1. As a viewer/non-admin, GET /department/membership-intents.<br>2. Craft POST /department/membership-intents/:intentId/ack for an intent belonging to a channel where the caller is not admin.                                                                                                                                                                                                                                                                                                                    | listMembershipIntents JOINs department_channel_members with m.user_id=caller AND m.role='admin', so a viewer/foreign-admin sees NONE of the target channel's intents. ackMembershipIntent updates only WHERE an EXISTS admin membership check passes; otherwise 404 intent_not_found_or_not_admin. A non-admin therefore cannot mark a rekey as done (which would strand a removed CPO holding the key) nor discover pending membership changes.                                                                                                                                                                                 | Viewer/foreign-admin (1 device) + crafted request     |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-24 | P0  | security   | Suspended/deactivated company account loses BOTH manage and list access (status must be ACTIVE)                  | 1. As the suspended company account, attempt GET /department/channels (list) and GET /department/manage/channels + POST /department/channels.                                                                                                                                                                                                                                                                                                                                                                      | Both DeptChatAccessGuard and OrgManagerGuard gate the company path on type='company' AND status='ACTIVE' (D4-c). A non-ACTIVE company is NOT admitted by DeptChatAccessGuard (403 dept_chat_org_membership_required) unless it still has an active org_members row, and is NOT admitted by OrgManagerGuard (403 org_manager_access_required). Confirm suspension actually severs access rather than relying only on JWT expiry.                                                                                                                                                                                                  | Suspended company account (1 device)                  |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-25 | P1  | regression | Loosening access on an INCIDENT-type channel does NOT re-seed CPOs (type keeps it managers-only)                 | 1. Edit the incident channel -> change Access from Restricted to Standard (leave Type = Incident).<br>2. Save.<br>3. Open Members and check a CPO's hub.                                                                                                                                                                                                                                                                                                                                                           | nowManagersOnly = newAccess==='restricted'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |                                                       | newType==='incident' stays TRUE because type is incident, so the wasManagersOnly && !nowManagersOnly loosen branch does NOT fire — no CPO re-seed. Members remains managers-only; the incident channel never surfaces on a CPO hub even at 'standard' access. Confirms access can't accidentally expose an incident channel to CPOs.                                                                                       | Provider manager + CPO (2 devices) |       |     |     |
| DADM-26 | P1  | regression | Tighten race (D4-d) — verify-before-flip guards the common case but there is no full row lock                    | 1. Begin PATCH access=restricted (tighten) on the channel.<br>2. In the same window, concurrently add a new CPO viewer (second manager device or a crafted concurrent addMember).<br>3. After both settle, re-open Members and post as a manager; on the late-added CPO device try to read.                                                                                                                                                                                                                        | Documented residual (D4-d is PARTIAL): configureChannel snapshots the viewer list, removes each, and aborts the flip on any failure, but it does NOT row-lock the channel for the whole tighten transaction. A CPO inserted AFTER the viewer snapshot but BEFORE the access flip can be left in a restricted channel still holding the (about-to-rotate or rotated) master key. Verify whether the late CPO can read post-tighten messages; if so, log as the known D4-d TOCTOU (needs SELECT ... FOR UPDATE serialization). No CPO should retain read access after tighten.                                                     | 2 manager devices + CPO (3 devices)                   | B-35                                                                                                                                                                                                                                                                                                                                                                                                                       |                                    |       |
| DADM-27 | P2  | security   | updateMemberRole has NO self-guard — an admin can demote themselves to viewer via crafted PATCH                  | 1. Open Members (self row shows no controls).<br>2. Craft PATCH /department/channels/:id/members/:ownId/role with role='viewer'.<br>3. Then attempt an admin-only action (add/remove member) as the same user.                                                                                                                                                                                                                                                                                                     | EXPECTED-BY-DESIGN CHECK / likely defect: updateMemberRole checks the CALLER is admin but does NOT block memberUserId === adminUserId (contrast removeMember's cannot_remove_self). A crafted self-demote to 'viewer' succeeds and immediately strips the caller's own admin rights (subsequent memberRole check fails -> only_admin_can_manage_members), potentially leaving a channel with no reachable admin if this was the last one. UI hides the control (isMe) so normal flows are safe, but log the missing API-level self-guard as a finding.                                                                           | Provider manager (1 device) + crafted request         |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-28 | P2  | error      | Add-member DTO validation — non-UUID user_id / invalid role / oversized label rejected (400)                     | 1. POST /department/channels/:id/members with user_id='not-a-uuid'.<br>2. Repeat with role='owner' (not in ['admin','viewer']).<br>3. Repeat with role_label of 121 chars.                                                                                                                                                                                                                                                                                                                                         | AddMemberDto rejects each before service logic: @IsUUID -> 400 on a non-UUID user_id; @IsIn(MEMBER_ROLES) -> 400 on an invalid role; @MaxLength(60) -> 400 on an over-long role_label. This is the D4-e hardening (previously unvalidated inline @Body). No membership row or intent is created on a rejected request.                                                                                                                                                                                                                                                                                                           | Channel admin (1 device) + crafted request            |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-29 | P2  | edge       | Name / department length bounds enforced server-side even past the client cap                                    | 1. Craft POST /department/channels with name of 81 chars.<br>2. Craft PATCH /department/channels/:id with name='' (empty) and separately department of 81 chars.<br>3. In the normal UI, type a long name and confirm the field stops at 80.                                                                                                                                                                                                                                                                       | CreateChannelDto @Length(1,80) and ConfigureChannelDto name @Length(1,80) / department @MaxLength(80) reject the over-long/empty values with 400. ConfigureChannel department allows '' (explicit-clear sentinel, MaxLength only), unlike CreateChannel. The mobile TextInput maxLength={80} caps both name and department client-side, so the UI can't normally exceed — this is a regression guard on the DTO bounds.                                                                                                                                                                                                          | Provider manager (1 device) + crafted request         |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-30 | P2  | edge       | Read-only (board/standard-access) channel includes CPOs as viewers; compose disabled, managers post              | 1. Create/edit a channel with Access='Read only'.<br>2. Confirm the seeded roster includes CPOs.<br>3. On the CPO device open the channel and attempt to post.<br>4. As a manager, post a message.                                                                                                                                                                                                                                                                                                                 | managersOnly = access==='restricted'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |                                                       | type==='incident' — 'read_only' is NOT managers-only, so seedChannelMembers still seeds CPOs (as viewers) and the channel DOES appear on the CPO hub (distinct from restricted, where CPOs are excluded). CPO can read but the compose box is disabled for viewers (role !== 'admin'); manager (admin) posts land and are readable. Confirms read_only differs from restricted in visibility while both block CPO posting. | Provider manager + CPO (2 devices) |       |     |     |
| DADM-31 | P2  | regression | Delegated department-scoped manager still governs the WHOLE org's channels (channel admin is org-scoped)         | 1. Log in as the department-scoped manager.<br>2. Open Manage Channels and list every channel.<br>3. Edit/archive a channel that belongs to a DIFFERENT department than the manager's scope.                                                                                                                                                                                                                                                                                                                       | listOrgChannels / createChannel / configureChannel / archiveChannel are ORG-scoped (WHERE org_id = manager.org_user_id) and do NOT apply OrgManagerContext.department — unlike attendance/incident queues which force the department filter. So a department-scoped manager sees and can administer ALL of the org's channels. Document this as intended-for-now so a future department-scoping change to channel admin doesn't silently regress (or, if product wants channel scoping too, log the gap).                                                                                                                        | Department-scoped manager (1 device)                  |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-32 | P2  | edge       | Empty org (no active members) — create seeds only the org admin; picker shows 'everyone active is a member'      | 1. As the company account, create a standard channel.<br>2. Open Manage Channels and read the member count.<br>3. Open Members -> Add member and inspect the picker.                                                                                                                                                                                                                                                                                                                                               | createChannel seeds only the org account as admin (activeOrgMembers returns none), so member_count = 1. Provisioning returns 'needs_members' (a single-member group) until a CPO is added, so the row reads '· not active'. The Add-member picker (addable = active roster minus existing members) is empty and shows 'Everyone active is already a member.' No crash on the empty roster.                                                                                                                                                                                                                                       | Company account (1 device)                            |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |
| DADM-33 | P0  | security   | Group-linkage authorization — registerGroup admin-only, resetGroup owner-only; viewer/CPO gets 403               | 1. As a CPO viewer, craft POST /department/channels/:id/group (registerGroup) with a fabricated group id.<br>2. As a non-creator manager (not the org account), craft POST /department/channels/:id/reset-group.<br>3. As the creator/org account, call reset-group legitimately, then re-provision.                                                                                                                                                                                                               | registerGroup requires memberRole==='admin' -> 403 only_admin_can_register_group for a viewer, so a CPO cannot hijack the E2EE group linkage or overwrite it (also blocked by first-writer-wins COALESCE). resetGroup requires created_by===caller OR org_id===caller -> 403 only_owner_can_reset for a non-owner, so a random manager cannot wipe the linkage. The legitimate owner reset clears group_conversation_id, writes a channel.reset_group audit row, and lets the owner device mint + register a fresh group (server never holds a key) — orphaned-channel recovery (D4-b).                                          | CPO viewer + non-creator manager + owner (3 accounts) |                                                                                                                                                                                                                                                                                                                                                                                                                            |                                    |       |

## 4. Attendance — Check-in / Check-out + Face Verification + Geofence (26 cases)

| ID     | Pri | Type       | Scenario                                                                                                                           | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Devices/Roles                                                                                       | Bug                                                   | Status | Notes |
| ------ | --- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------ | ----- |
| ATT-01 | P0  | golden     | Golden verified check-in: face pass + inside geofence → Present                                                                    | 1. Open the Departmental Attend tab as the CPO; open the Attendance screen.<br>2. Confirm the status hero shows the assigned shift (site label, time window, 'Within N m of site') and the sticky button reads 'Verify & Check In'.<br>3. Tap 'Verify & Check In'.<br>4. On Verify Attendance, allow the camera permission prompt if shown; wait for the live front-camera preview and the scan dot to turn green (camera ready).<br>5. Position a single face in the ring, allow the GPS prompt, tap 'Confirm & Check In'.                    | Navigates to Attendance Result showing 'Checked In' with a green 'Present' status chip (or 'Late' if now is >10 min past shift start), no Reason row. Time row shows the check-in time; Site row shows the shift site. Back on the Attendance screen (focus reload) the hero flips to 'On shift / Checked in {time}', pill shows 'ON SHIFT', and the sticky button becomes 'Check Out'.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 1 device: managed CPO (member). Requires a provider manager to have pre-created+assigned the shift. |                                                       |        |       |
| ATT-02 | P0  | golden     | Golden verified check-out: face + location re-verified → Checked Out                                                               | 1. On the Attendance screen (pill 'ON SHIFT'), tap 'Check Out'.<br>2. Verify Check-Out screen opens with the CHECK-OUT pill and live camera; wait for camera-ready.<br>3. Position face, tap 'Confirm & Check Out'.                                                                                                                                                                                                                                                                                                                            | clockOut is sent with face_ok=true + lat/lng. Navigates to Attendance Result titled 'Checked Out' with the retained status (Present/Late) — NOT flipped to Pending Review and NOT early_checkout (checkout is within end-grace). Attendance screen hero returns to 'Off shift', the today session chip shows the final status, and the session in Recent shifts shows '→ {check-out time}'.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 1 device: managed CPO with an open shift.                                                           |                                                       |        |       |
| ATT-03 | P0  | error      | Camera permission DENIED → check-in still allowed as Pending Review (camera_unavailable)                                           | 1. Tap 'Verify & Check In'.<br>2. At the camera permission prompt, tap Deny.<br>3. Observe the Verify screen shows the red camera-off frame, 'Camera access needed', and a footer 'Allow Camera' button plus a 'Check In without camera (Pending Review)' link.<br>4. Tap 'Check In without camera (Pending Review)'.<br>5. Allow the GPS prompt.                                                                                                                                                                                              | clockIn is sent with face_ok=false and face_unavailable=true (with valid lat/lng). Server deriveCheckIn returns pending_review reason=camera_unavailable. Attendance Result shows 'Pending Review' (amber shield-alert badge) with Reason = 'Camera unavailable' (distinct from a face mismatch), and copy stating the check-in was recorded and the CPO has NOT been marked absent. NOTE: server check order is permission_denied BEFORE camera_unavailable — if GPS is also denied the reason becomes permission_denied, so GPS must resolve for camera_unavailable to surface.                                                                                                                                                                                                                                                                                              | 1 device: managed CPO. Camera permission revoked/denied.                                            | D6-e (camera_unavailable distinct from face_mismatch) |        |       |
| ATT-04 | P1  | error      | Face NOT detected / multiple faces (MLKit build) → face_mismatch Pending Review                                                    | 1. Tap 'Verify & Check In'; grant camera; wait camera-ready.<br>2. Point the front camera at a blank wall (no face) OR at two people.<br>3. Tap 'Confirm & Check In'; allow GPS.                                                                                                                                                                                                                                                                                                                                                               | On-device MLKit returns count!=1 → face_ok=false, confidenceBucket 'no_face' (count 0) or 'multiple_faces' (count 2). Server records reason=face_mismatch (both variants map to the same reason/label 'Face check failed'), attendance_status=pending_review. No photo/frame is uploaded (only the boolean + scalar face_meta with faceCount). Captured time/location are still stored. The temp frame is deleted from cache.                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 1 device with MLKit-enabled build: managed CPO.                                                     |                                                       |        |       |
| ATT-05 | P0  | error      | Out-of-geofence check-in blocked from Present → Pending Review (out_of_radius), never silent Absent                                | 1. Tap 'Verify & Check In'; grant camera; wait ready.<br>2. Grant GPS (returns an out-of-radius fix).<br>3. Tap 'Confirm & Check In'.                                                                                                                                                                                                                                                                                                                                                                                                          | Server computes haversine distance > approved_radius_m (within_radius=false; boundary is inclusive, distance == radius still passes) and — because face/coords passed first — returns attendance_status=pending_review, reason=out_of_radius, with within_radius=false and distance_m stored. Attendance Result shows 'Pending Review' with Reason 'Outside site radius'. The record is NOT marked Absent — it enters the manager's Pending Review queue for a decision.                                                                                                                                                                                                                                                                                                                                                                                                       | 1 device with mock/real out-of-radius GPS: managed CPO.                                             |                                                       |        |       |
| ATT-06 | P0  | error      | GPS permission denied → no-coords check-in becomes Pending Review (permission_denied)                                              | 1. Tap 'Verify & Check In'; grant camera; wait ready; face in frame.<br>2. Tap 'Confirm & Check In'.<br>3. At the location prompt, Deny (or leave GPS off so the 8s fix times out).                                                                                                                                                                                                                                                                                                                                                            | getGeo resolves null → clockIn sent with no lat/lng. Server (hasCoords=false) returns pending_review reason=permission_denied; within_radius stays null (distance uncomputable) even though the CPO is physically inside. Attendance Result shows 'Pending Review' with Reason 'Location unavailable'. Flow never crashes on a denied fix. NOTE server check order: no-coords is evaluated BEFORE the face check, so the reason is permission_denied even though the face passed.                                                                                                                                                                                                                                                                                                                                                                                              | 1 device with location denied: managed CPO.                                                         |                                                       |        |       |
| ATT-07 | P1  | error      | Duplicate check-in while a shift is already open → blocked with friendly alert                                                     | 1. Force the Verify & Check In flow again (e.g. deep-navigate to Verify Attendance while a shift is open, or via a second entry point) and tap 'Confirm & Check In'.<br>2. Observe the response.                                                                                                                                                                                                                                                                                                                                               | Server rejects with 'shift_already_open'. This open-shift guard is checked FIRST — before resolveOrg / myTodayShift — so it fires even for a CPO with no currently-active assigned shift. App shows Alert 'You already have an open shift.' and navigates back. No second open session is created (partial unique index on status='open' is the real backstop).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 1 device: managed CPO with an open shift.                                                           |                                                       |        |       |
| ATT-08 | P1  | error      | Check-out without an open shift → blocked with 'no open shift' alert                                                               | 1. Trigger a check-out (Verify Check-Out → 'Confirm & Check Out', or legacy Check Out) while no session is open.<br>2. Observe the response.                                                                                                                                                                                                                                                                                                                                                                                                   | The clockOut UPDATE matches zero 'open' rows and returns 'no_open_shift'. App shows Alert 'You have no open shift to check out of.' and navigates back. No session row is modified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 1 device: managed CPO with no open shift.                                                           |                                                       |        |       |
| ATT-09 | P1  | golden     | Attendance day status/hero reflects checked-in and checked-out states correctly                                                    | 1. Before check-in: open Attendance — confirm hero 'Off shift' / "Today's shift is ready — verify to check in", pill 'OFF', button 'Verify & Check In'.<br>2. Check in (ATT-01). Return to Attendance (pull-to-refresh / focus reload).<br>3. Confirm hero 'On shift / Checked in {time}', pill 'ON SHIFT', button 'Check Out', today's status chip present.<br>4. Check out (ATT-02). Return to Attendance and refresh.<br>5. Confirm hero returns to 'Off shift' and the today session shows final status + '→ {out time}' in Recent shifts. | Each transition updates the hero, pill, sticky button, and status chip to match the session state. A pending session shows its review reason under the recent-shift row. No stale state after refresh (useFocusEffect reloads myShifts + myTodayShift on focus).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 1 device: managed CPO.                                                                              |                                                       |        |       |
| ATT-10 | P1  | edge       | No active shift assigned → check-in disabled / server rejects no_active_shift_assigned                                             | 1. Open Attendance as the CPO.<br>2. Observe hero and sticky button.<br>3. If a Verify flow is force-reached, tap Confirm and observe.                                                                                                                                                                                                                                                                                                                                                                                                         | myTodayShift returns null → hero shows 'No active shift / No shift assigned to you today', pill 'NO SHIFT', and the sticky button is 'No Active Shift' (disabled). If check-in is somehow forced, server returns 'no_active_shift_assigned' (raised AFTER the open-shift guard) and the app alerts 'No active shift is assigned to you right now.' then goes back. A shift that already ended (end_at < now) or starts >12h out is excluded — verifies check-in cannot be created without a currently-checkable assigned shift.                                                                                                                                                                                                                                                                                                                                                | 1 device: managed CPO with no assignment.                                                           | D6-d (12h forward window, tz-independent gating)      |        |       |
| ATT-11 | P0  | security   | SECURITY: face frame never leaves device — only boolean result + scalar meta on the wire                                           | 1. Attach a proxy (e.g. mitm) to the device or tail logcat.<br>2. Perform a normal verified check-in (ATT-01).<br>3. Inspect the clock-in HTTP request body and the app logs.                                                                                                                                                                                                                                                                                                                                                                  | The clock-in body contains only whitelisted ClockInDto fields: shift_id, lat/lng/accuracy_m, face_ok (boolean), optional face_unavailable, and face_meta that is SCALARS ONLY (e.g. {model, version, confidenceBucket, faceCount}). No image bytes, data URI, base64 frame, face geometry, landmarks, or descriptor arrays are sent. No frame URI or face bytes appear in logs. The captured photo is deleted from cache in faceCheck's finally block (pass or fail). Defence-in-depth: even if an array/object is smuggled inside face_meta, server sanitizeFaceMeta strips non-scalar keys (and caps to 12 keys / 120 chars); the global whitelist ValidationPipe strips any unknown top-level keys.                                                                                                                                                                         | 1 device + traffic inspection: managed CPO.                                                         |                                                       |        |       |
| ATT-12 | P0  | security   | SECURITY: cross-org scoping — a CPO cannot check into another org's shift, cross-org assignment rejected                           | 1. As Org A's manager, attempt to assign CPO-B (Org B) to shift SA.<br>2. As CPO-B, attempt to check in (verify flow) sending Org A's shift id in the clock-in body.<br>3. As CPO-A, confirm myTodayShift only ever returns Org A shifts and check-in resolves the org from CPO-A's own active org_members row.                                                                                                                                                                                                                                | Step 1: server rejects with 'cpo_not_active_member_of_org' — no assignment created (active-membership count must equal requested id count). Step 2: shift_id in the body is ADVISORY — the server ignores it and calls myTodayShift(CPO-B) which is scoped by assignment, returns null → check-in rejected 'no_active_shift_assigned'; no session under Org A. Step 3: clockIn resolves org via resolveOrg(user.sub) from org_members; sessions are org-scoped to the CPO's own org — no cross-org leak of shifts/sessions in any list. Clock-in/out are scoped to the authenticated user.sub only.                                                                                                                                                                                                                                                                            | 2 devices/accounts: Org A manager + CPO-A, Org B CPO-B.                                             |                                                       |        |       |
| ATT-13 | P1  | edge       | Offline check-in: NO client queue exists — request fails with error alert (spec gap)                                               | 1. Open 'Verify & Check In'; grant camera; enable airplane mode.<br>2. Tap 'Confirm & Check In'.<br>3. Observe behavior.<br>4. Restore connectivity and re-open Attendance.                                                                                                                                                                                                                                                                                                                                                                    | The clockIn axios call fails (network error). The Verify screen catch shows a generic 'Attendance' alert with the error message; it does NOT navigate to a Pending Review result. IMPORTANT GAP: there is no local offline queue and no auto-sync — VerifyAttendanceScreen never sets ClockInBody.offline, so the check-in is simply lost until the CPO retries online. Log this discrepancy against the spec's 'offline queue + sync' intent (server deriveCheckIn supports an 'offline' pending reason, but the client never sends it).                                                                                                                                                                                                                                                                                                                                      | 1 device offline: managed CPO.                                                                      |                                                       |        |       |
| ATT-14 | P1  | edge       | Clock/timezone (UTC) correctness for late/early thresholds and day-boundary shift gating                                           | 1. Assign a shift starting shortly and check in >10 min after start; verify the status.<br>2. Check out >10 min before shift end; verify the status.<br>3. Near local midnight (but a different UTC date), confirm a shift active/starting within 12h is still offered for check-in.<br>4. Compare displayed times on Result/History against the actual local wall-clock time.                                                                                                                                                                 | Server stores clock_in_at/clock_out_at in UTC (NOW()). Late = check-in > start_at+10min (strict) → 'Late'; early check-out < end_at-10min → 'early_checkout'. Displayed times use device locale/timezone (toLocaleString/toLocaleTimeString) and match local wall-clock. myTodayShift uses end_at >= NOW() AND start_at <= NOW()+12h (forward window, NOT UTC date_trunc), so a shift stays checkable across the local day boundary for non-UTC orgs — no mis-gated 'No active shift' at midnight.                                                                                                                                                                                                                                                                                                                                                                             | 1 device with non-UTC timezone: managed CPO.                                                        | D6-d (tz-independent day boundary)                    |        |       |
| ATT-15 | P2  | regression | MLKit module ABSENT (no APK rebuild) → face degrades to capture-presence PASS (assurance gap)                                      | 1. Tap 'Verify & Check In'; grant camera; wait ready.<br>2. Point camera anywhere (even no face) and tap 'Confirm & Check In'.<br>3. Observe result and check the face_meta bucket if inspectable.                                                                                                                                                                                                                                                                                                                                             | With no detector (require throws → loadDetector null), runFaceCheck returns face_ok=true with confidenceBucket 'capture_only' (model 'presence-capture', a frame was captured but not analyzed). Check-in succeeds as Present (assuming in-geofence) — it does NOT false-fail. VERIFY the assurance limitation: in this build face verification is effectively presence-of-capture only, not real detection; confirm the audit bucket records 'capture_only' so managers can distinguish it. Flag that real MLKit detection requires the rebuilt APK.                                                                                                                                                                                                                                                                                                                          | 1 device on a pre-MLKit build: managed CPO.                                                         |                                                       |        |       |
| ATT-16 | P1  | edge       | Early check-out on a clean shift → status flips to Early Checkout                                                                  | 1. With an open present/late shift, tap 'Check Out'.<br>2. Complete face + location on the Verify Check-Out screen well before shift end.<br>3. Observe the Result and history.                                                                                                                                                                                                                                                                                                                                                                | Because the check-out verification is clean (deriveCheckOut review_reason null) but clock_out_at < end_at-10min, attendance_status becomes 'early_checkout' (not pending_review). Result shows 'Checked Out' with an Early Checkout chip. The captured check-in time/location remain immutable (only status moves). Contrast with ATT-26: a FAILED check-out verification runs AFTER the early-checkout update and overrides the status to Pending Review — verify these two branches don't cross.                                                                                                                                                                                                                                                                                                                                                                             | 1 device: managed CPO with an open shift.                                                           |                                                       |        |       |
| ATT-17 | P2  | regression | Result screen 'Pending Review' reassurance copy — user not marked absent                                                           | 1. Produce any Pending Review check-in (e.g. deny camera, ATT-03).<br>2. Read the Attendance Result screen copy and badge.<br>3. Tap 'Done'.                                                                                                                                                                                                                                                                                                                                                                                                   | Title 'Pending Review' with amber shield-alert badge, a Reason row naming the specific cause, and body copy explicitly stating the check-in/out was recorded and sent to an admin and 'You have NOT been marked absent.' 'Done' returns to the tab root via popToTop — not a hard-coded target that could dead-end.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 1 device: managed CPO.                                                                              |                                                       |        |       |
| ATT-18 | P2  | edge       | App killed / backgrounded mid check-in (after Confirm, before response) → no duplicate open shift on retry                         | 1. Tap 'Verify & Check In'; grant camera; wait ready; tap 'Confirm & Check In'.<br>2. Immediately force-kill the app (or kill the process) before the AttendanceResult screen appears — while the clockIn request may or may not have committed server-side.<br>3. Relaunch, open Attendance, observe state.<br>4. If the hero shows 'Off shift' (request did not commit), retry the check-in. If it shows 'On shift', do NOT retry.                                                                                                           | There is no client idempotency key, so the safety net is the DB. If the first request committed, the relaunched Attendance hero shows 'On shift' and the sticky button is 'Check Out' — no orphaned/duplicate row. If the CPO retries anyway (or the request half-committed), the second clockIn hits the open-shift guard and returns 'shift_already_open' → friendly alert, no second open session. The captured clock_in_at/geo of the first (committed) session stay intact.                                                                                                                                                                                                                                                                                                                                                                                               | 1 device: managed CPO.                                                                              |                                                       |        |       |
| ATT-19 | P2  | edge       | Concurrent double check-in race → partial unique index may surface an unmapped 23505 instead of shift_already_open                 | 1. Fire two POST /attendance/clock-in requests for the same CPO at the same instant.<br>2. Inspect both responses.                                                                                                                                                                                                                                                                                                                                                                                                                             | One request creates the open session (200). The other should ideally get a friendly 'shift_already_open' (400). RISK TO VERIFY: clockIn does a non-atomic SELECT(status='open')-then-INSERT with no try/catch around the INSERT, so under a true race both can pass the SELECT and the second INSERT violates the partial unique index — surfacing a raw Postgres 23505 as a 500 with an untranslated error message rather than the friendly 400. Confirm exactly one open session exists afterward (data integrity holds) and log the ungraceful-error discrepancy if reproduced.                                                                                                                                                                                                                                                                                             | 2 sessions/devices or scripted API: managed CPO.                                                    |                                                       |        |       |
| ATT-20 | P2  | regression | Verified mode but open session has NULL shift_id → check-out silently skips face verification (assurance gap)                      | 1. Open Attendance; confirm pill 'ON SHIFT' with an open session that has no shift_id.<br>2. Tap 'Check Out'.<br>3. Observe whether the Verify Check-Out (face) screen appears or a direct clock-out happens.                                                                                                                                                                                                                                                                                                                                  | onCheckOut gates the verify screen on `verified && openShift?.shift_id`; with a null shift_id it falls to the legacy branch — a direct attendanceApi.clockOut({lat,lng}) with NO face step and no result screen. The session closes with a geotag only. FLAG this as an assurance gap: a v2 account can complete a check-out without the required face confirmation whenever the open row lacks a shift_id. Also note clockOut's v2 status logic only runs when row.shift_id is set, so no early_checkout/pending derivation happens for this row.                                                                                                                                                                                                                                                                                                                             | 1 device: managed CPO with a null-shift_id open session.                                            |                                                       |        |       |
| ATT-21 | P2  | edge       | Shift with no geofence centre (site_lat/lng NULL) → radius check skipped, check-in Present from anywhere                           | 1. As the CPO with GPS ON but physically far from any nominal site, tap 'Verify & Check In'.<br>2. Complete face + grant GPS; tap 'Confirm & Check In'.                                                                                                                                                                                                                                                                                                                                                                                        | In deriveCheckIn, because shift.site_lat/site_lng are null, within stays null and distance_m null — the `within === false` branch is never taken, so out_of_radius is NOT raised. With face passing and coords present, status is Present/Late (not pending). VERIFY the geofence is effectively unenforced for shifts created without a centre — a manager must set site coords for the radius gate to apply. The Attendance hero still renders 'Within N m of site' from the radius even though no centre exists (cosmetic).                                                                                                                                                                                                                                                                                                                                                 | 1 device: managed CPO on a no-geofence shift.                                                       |                                                       |        |       |
| ATT-22 | P1  | edge       | Member dispute route — closed/reviewed record flagged back to Pending Review; open/pending disputes blocked                        | 1. Open My Attendance. Confirm the 'Dispute' link shows ONLY on rows where status != 'open' AND review_status != 'pending'.<br>2. On a closed reviewed row, tap 'Dispute', enter a note (>=3 chars), Send.<br>3. Attempt (via API) to dispute the OPEN session and the already-pending session.                                                                                                                                                                                                                                                | Step 2: POST /attendance/sessions/:id/dispute sets review_status='pending', review_reason='disputed', dispute_note stored (truncated to 500), audited 'attendance.dispute'; the captured geo/time and prior review columns stay intact except review_status/reason. Alert 'Your dispute was sent to your admin for review.' and the row re-appears in the manager Pending Review queue. Step 3: disputing an already-pending record → 'already_pending_review' (UI maps to 'This record is already waiting for admin review.'); disputing an OPEN session → 'shift_still_open'. The note field enforces Length(3,500) at the DTO.                                                                                                                                                                                                                                              | 1 device: managed CPO with mixed-status history.                                                    |                                                       |        |       |
| ATT-23 | P0  | security   | SECURITY: own-records-only — a CPO cannot read or dispute another CPO's attendance session                                         | 1. As CPO-A, call GET /attendance/me and confirm only CPO-A's own rows return.<br>2. As CPO-A, call POST /attendance/sessions/{SB}/dispute with a note.<br>3. Confirm CPO-B's record is unchanged.                                                                                                                                                                                                                                                                                                                                             | Step 1: myShifts is scoped WHERE cpo_user_id = user.sub — CPO-B's rows never appear. Step 2: disputeSession selects WHERE id=$1 AND cpo_user_id=$2 (the caller) → no match → NotFoundException 'session_not_found'; SB is NOT flagged, no audit row, no cross-user write. Confirms a member cannot view or mutate another member's attendance record (self-scoping enforced server-side, not just hidden in UI).                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 2 accounts: CPO-A, CPO-B (same org).                                                                |                                                       |        |       |
| ATT-24 | P1  | regression | Flag OFF regression — legacy plain clock-in (no shift / no face / no geofence) preserved byte-for-byte                             | 1. Open Attendance. Observe hero and sticky button.<br>2. Tap the primary action and complete it.<br>3. Check out.<br>4. Inspect the created session row.                                                                                                                                                                                                                                                                                                                                                                                      | GET /attendance/my-shift/today is DeptChatV2Guard-gated → 404, so the screen sets verified=false: hero shows 'Tap below to record your attendance', the sticky button is 'Clock In' (not 'Verify & Check In'), and there is no shift card. clockIn takes the legacy branch — inserts an open session with only clock_in_lat/lng/accuracy_m, NO shift_id lookup (no 'no_active_shift_assigned'), NO face fields, NO attendance_status/review derivation. Check-out is the direct geotagged clockOut. The Verify/face screen is never reached and the dispute route (also v2-gated) returns 404. Confirms the flag never weakens or bypasses a guard and legacy behaviour is unchanged.                                                                                                                                                                                          | 1 device: CPO on a flag-OFF account/build.                                                          |                                                       |        |       |
| ATT-25 | P2  | error      | Camera ready but frame capture fails / MLKit detector throws → camera_unavailable (not face_mismatch)                              | 1. Tap 'Verify & Check In'; grant camera; wait camera-ready.<br>2. Trigger a capture failure (a) or a detector runtime error (b).<br>3. Tap 'Confirm & Check In'; allow GPS.                                                                                                                                                                                                                                                                                                                                                                   | Case (a): photo.uri is falsy → face = {face_ok:false, face_unavailable:true, bucket 'capture_failed'}. Case (b): runFaceCheck catch → {face_ok:false, face_unavailable:true, bucket 'detector_error'} and the temp frame is still deleted in finally. Both send face_unavailable=true → server reason=camera_unavailable (Pending Review), DISTINCT from the face_mismatch produced by a genuine no_face/multiple_faces detection. Attendance Result shows 'Pending Review' / Reason 'Camera unavailable'. Verifies a technical camera/detector failure is never a silent pass and never mislabeled as a face mismatch.                                                                                                                                                                                                                                                        | 1 device: managed CPO (fault injection).                                                            | D6-e (camera_unavailable distinct from face_mismatch) |        |       |
| ATT-26 | P1  | error      | Failed check-out verification (out-of-radius / face fail / no coords) → session flips to Pending Review, overriding early_checkout | 1. With an open present/late shift, tap 'Check Out'.<br>2. On Verify Check-Out, induce a failure (out-of-radius GPS, or no face, or deny GPS).<br>3. Tap 'Confirm & Check Out'; observe Result and history.                                                                                                                                                                                                                                                                                                                                    | clockOut closes the row (status='closed', clock_out_at=NOW, geotag stored). deriveCheckOut returns a review_reason (order: no-coords→permission_denied, then camera_unavailable, then face_mismatch, then out_of_radius). Because review_reason is set and the row was not already pending, attendance_status is driven to 'pending_review' with that reason — this runs AFTER the early_checkout update, so even a before-end-grace checkout ends as Pending Review (failure wins over early_checkout, branches don't cross — cf. ATT-16). Result titled 'Checked Out' with a Pending Review chip + Reason row. The captured check-in time/location remain immutable (only status/review columns change). NOTE: if the check-IN was already pending, checkout does NOT re-flag (review_status==='pending' guard) and early_checkout is skipped — status stays pending_review. | 1 device: managed CPO with an open shift.                                                           |                                                       |        |       |

## 5. Attendance — Admin Review & Shift Management (26 cases)

| ID        | Pri | Type       | Scenario                                                                                                            | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Devices/Roles                                                                 | Bug | Status | Notes |
| --------- | --- | ---------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | --- | ------ | ----- |
| ASHIFT-01 | P0  | golden     | Manager approves a Pending Review check-in (with notes) — re-derives Present/Late and clears the queue              | 1. Open the Departmental Attend tab → Admin Attendance.<br>2. Confirm the header pill shows 'N PENDING' in warn/amber tone.<br>3. Scroll to PENDING REVIEW; locate the flagged row (shows clock-in time + reason label).<br>4. Tap 'Approve'.<br>5. In the modal (title 'Approve check-in'), type a short note in the optional Notes field.<br>6. Tap 'Approve' to confirm.                                                                                                                                                                | On confirm, a spinner shows on the Approve button, both buttons disable (busyId), then load() re-runs. The record leaves the Pending Review list; header pending count and pill decrement by one. Approve ALWAYS re-derives the final status from the shift window at check-in time — Present if clock_in ≤ start+10min grace, else Late — regardless of the original flag reason (review_status→approved). The Present (or Late) counter increments after reload. Note is persisted to admin_notes (later visible in CSV export). No error alert.                                                                                                                  | 1 device: provider company-account manager                                    |     |        |       |
| ASHIFT-02 | P0  | golden     | Manager rejects a Pending Review check-in — record becomes terminal Absent, leaves the queue                        | 1. Admin Attendance → PENDING REVIEW.<br>2. Tap 'Reject' on a flagged row.<br>3. Confirm the modal reads 'Reject check-in' / 'Reject this check-in? It stays flagged.'<br>4. Optionally add a note, then tap 'Reject'.<br>5. Pull-to-refresh the screen.                                                                                                                                                                                                                                                                                   | Record disappears from the Pending Review queue (review_status → rejected). It is NOT re-shown as pending. The Absent counter increments by one — reject drives attendance_status to the TERMINAL 'absent' (manager did not vouch the check-in), so it leaves the pending_review bucket in reporting. Pending count/pill decrement. Captured clock_in_at/geotag/face_verified are unchanged in any later view — only review + status columns move.                                                                                                                                                                                                                  | 1 device: provider company-account manager                                    |     |        |       |
| ASHIFT-03 | P0  | golden     | Create a shift with geofence + assign a CPO — unblocks that CPO's check-in end-to-end                               | 1. Admin Attendance → tap the SHIFTS segment → tap 'New Shift'.<br>2. Enter Department (e.g. Operations) and Site (e.g. Main Office).<br>3. Tap 'Use current location' (grant location) → confirm coordinates + radius (150 m default) appear.<br>4. Set Start to now and End to +8h via the date/time chips.<br>5. Under ASSIGN CPOS, tap the target CPO card so its checkbox fills; header shows '1 selected'.<br>6. Tap 'Create & Assign Shift'.<br>7. On the CPO device, reload the attendance dashboard.                              | Editor POSTs createShift then assignCpos, returns to the Shifts list; new shift appears under UPCOMING & ACTIVE with the site·department label, correct window (fmtWindow), '1 assigned' and the '150 m' radius meta. On the CPO device myTodayShift resolves (window covers now / starts within the 12h lead window) so the block state clears and check-in is now allowed.                                                                                                                                                                                                                                                                                        | 2 devices: provider manager + managed CPO                                     |     |        |       |
| ASHIFT-04 | P1  | error      | Shift validation — end time not after start time is blocked before save                                             | 1. Admin Attendance → Shifts segment → New Shift.<br>2. Select at least one CPO.<br>3. Set Start and End to the SAME time (or End earlier than Start).<br>4. Tap 'Create & Assign Shift'.                                                                                                                                                                                                                                                                                                                                                  | An alert titled 'Shift' with message 'The end time must be after the start time.' appears (validateShiftDraft checks start<end FIRST). No shift is created (no new row), no network POST /attendance/shifts is fired (validation runs before the try). Mirrors the server invariant.                                                                                                                                                                                                                                                                                                                                                                                | 1 device: provider manager                                                    |     |        |       |
| ASHIFT-05 | P1  | error      | Shift validation — creating a shift with zero CPOs assigned is blocked                                              | 1. Admin Attendance → Shifts segment → New Shift.<br>2. Set a valid Start/End window.<br>3. Leave ASSIGN CPOS with '0 selected'.<br>4. Tap 'Create & Assign Shift'.                                                                                                                                                                                                                                                                                                                                                                        | Alert 'Select at least one CPO to assign.' No shift created. This ≥1-CPO rule applies to CREATE only; in edit mode the assign section is hidden and validateShiftDraft is passed selectedCount=1, so the same validation must NOT fire when editing.                                                                                                                                                                                                                                                                                                                                                                                                                | 1 device: provider manager                                                    |     |        |       |
| ASHIFT-06 | P1  | edge       | Shift validation — geofence set but radius 0/empty is blocked; no-geofence shift saves                              | 1. New Shift → select a CPO, set a valid window.<br>2. Tap 'Use current location' so coordinates + radius input appear.<br>3. Clear the radius field (leave it blank / 0).<br>4. Tap 'Create & Assign Shift'.<br>5. Then clear the geofence via the x, and save again.                                                                                                                                                                                                                                                                     | Step 4: alert 'Approved radius must be greater than 0 m.' (rule only fires when hasCoords). No shift created. Step 5 inverse: a shift with NO geofence (coords cleared) skips the radius rule and saves — body sends approved_radius_m undefined, server records time-only attendance and the shift row shows 'no geofence'.                                                                                                                                                                                                                                                                                                                                        | 1 device: provider manager                                                    |     |        |       |
| ASHIFT-07 | P1  | edge       | Edit an existing shift — window/site/radius patched, assignments untouched, no ≥1-CPO gate                          | 1. Shifts segment → tap the pencil icon on an UPCOMING & ACTIVE shift.<br>2. Confirm the header title reads 'Edit Shift' and the ASSIGN CPOS section is absent.<br>3. Change the End time and/or Site label and radius.<br>4. Tap 'Save Changes'.<br>5. Return to the Shifts list.                                                                                                                                                                                                                                                         | Prefilled fields match the shift (department/site/coords/radius/window). Save succeeds (PATCH /attendance/shifts/:id → updateShift, COALESCE patch) even though no CPO picker exists. Row reflects the new window/site/radius. Assigned CPO count is unchanged (edit never touches assignments). Change is server-audited attendance.shift.update with before/after metadata. Note: there is NO UI to add/remove CPOs on an existing shift after creation.                                                                                                                                                                                                          | 1 device: provider manager                                                    |     |        |       |
| ASHIFT-08 | P1  | edge       | Archive a shift — confirm dialog, removed from list, assigned CPO loses today's shift                               | 1. Shifts segment → tap the archive icon on the shift.<br>2. In the 'Archive shift' confirm dialog (message names the site + 'Assigned CPOs will no longer see it as today\'s shift.'), tap 'Cancel' first → verify nothing changes.<br>3. Re-open and tap 'Archive'.<br>4. On the assigned CPO device, reload attendance.                                                                                                                                                                                                                 | Cancel is a no-op. Archive → DELETE /attendance/shifts/:id → archiveShift soft delete (archived_at set). The shift disappears from Upcoming/Past lists (listOrgShifts filters archived_at IS NULL). The assigned CPO's dashboard reverts to 'No active shift assigned' (myTodayShift also filters archived) and check-in is blocked again. Action audited attendance.shift.archive.                                                                                                                                                                                                                                                                                 | 2 devices: provider manager + managed CPO                                     |     |        |       |
| ASHIFT-09 | P0  | security   | Department-scoped manager sees ONLY their department's summary, pending queue and export                            | 1. Log in as the Operations-scoped manager and open Admin Attendance.<br>2. Inspect the Present/Late/Absent counters and the PENDING REVIEW list.<br>3. Open the Shifts segment and inspect the shift list / department chips.<br>4. Attempt to widen scope by tapping a 'Logistics' chip (if shown) or hitting GET /attendance/org/summary?department=Logistics and /attendance/org/pending?department=Logistics directly.<br>5. Attempt POST /attendance/org/export?department=Logistics.                                                | Counters, pending queue and export are FORCED to Operations — the controller uses department = manager.department ?? requested, and since manager.department='Operations' (non-null) any requested department is overridden, never widened. Requesting Logistics returns Operations-scoped data, not Logistics. No Logistics session or pending row is ever visible. A scoped manager cannot escape their department.                                                                                                                                                                                                                                               | 1 device: delegated department-scoped manager (plus multi-dept seed data)     |     |        |       |
| ASHIFT-10 | P0  | security   | Cross-org isolation — manager cannot review another org's session; cross-org CPO assign rejected                    | 1. Log in as Org A manager; note Org A's shift IDs and pending session IDs.<br>2. Confirm Admin Attendance shows ONLY Org A data.<br>3. Replay PATCH /attendance/sessions/:id/review with an Org B session id.<br>4. In New Shift, tamper the assignments payload with an Org B CPO's user id assigned to an Org A shift.<br>5. Attempt PATCH /attendance/shifts/:id and DELETE on an Org B shift id.                                                                                                                                      | Org A manager never sees Org B rows. Reviewing an Org B session id → 404 session_not_found_in_org (row scoped by org_user_id in the FOR UPDATE select). Assigning a foreign/inactive CPO → 400 cpo_not_active_member_of_org (active-member count check). Patching/archiving an Org B shift → 404 shift_not_found_in_org. No cross-tenant leak in counts, queue, or shift list.                                                                                                                                                                                                                                                                                      | 2 accounts: Org A manager + Org B manager (data from both orgs)               |     |        |       |
| ASHIFT-11 | P1  | error      | Illegal status transition — approving/rejecting an already-reviewed record is refused                               | 1. Manager A and Manager B both open Admin Attendance showing the same pending record.<br>2. Manager A approves it (record clears).<br>3. Manager B (stale list) taps Approve/Reject on the same record and confirms.<br>4. Alternatively: on one device rapidly double-tap Approve on a single record.                                                                                                                                                                                                                                    | The second review attempt fails with an alert 'Review — not_pending_review' (BadRequestException from reviewSession's review_status !== 'pending' guard, surfaced from response.data.message). The record is not double-processed; its final status/counters are unchanged from the first decision. The busyId guard (if (busyId) return;) prevents a concurrent double-submit on the same device.                                                                                                                                                                                                                                                                  | 2 devices: two provider managers of the same org                              |     |        |       |
| ASHIFT-12 | P2  | edge       | Empty states — no pending reviews and no shifts render clear placeholders                                           | 1. Open Admin Attendance.<br>2. Observe the header pill, stat counters, and PENDING REVIEW section.<br>3. Tap the SHIFTS segment.                                                                                                                                                                                                                                                                                                                                                                                                          | Header pill shows '0 PENDING' in good/green tone (pillTone good when pendingReview===0). Present/Late/Absent all show 0 (orgSummary returns empty counts). Pending section shows the card 'Nothing waiting for review.' Shifts screen shows the empty card: 'No shifts yet' + 'Create a shift and assign CPOs so they can check in.' No spinner stuck, no crash.                                                                                                                                                                                                                                                                                                    | 1 device: provider manager (empty org)                                        |     |        |       |
| ASHIFT-13 | P1  | edge       | Timezone display — shift window and check-in times render device-local; correct at the day boundary                 | 1. Set the device timezone to a non-UTC zone; relaunch the app.<br>2. Shifts segment → open a shift; note the WHEN summary and the row window (fmtWindow).<br>3. Admin Attendance → note a pending row's check-in time (fmtTime).<br>4. Cross-check the same records on the ops-console attendance page / CSV export (which carry ISO/UTC).                                                                                                                                                                                                | Shift start/end and check-in times display in the device's local timezone consistently (date/time chips + fmtWindow + fmtTime agree). A shift starting late-evening local does NOT render on the wrong calendar day. A CPO whose active window straddles UTC midnight still resolves myTodayShift correctly — the query uses a forward lead window (NOW ≤ end AND start ≤ NOW+12h, D6-d fix), NOT date_trunc('day', NOW()) which mis-gated at the UTC day boundary for non-UTC orgs.                                                                                                                                                                                | 1 device (non-UTC tz) + ops-console for cross-check                           |     |        |       |
| ASHIFT-14 | P1  | regression | Auto-absent rollup — assigned CPO who never checks in shows Absent; day-status marker suppresses it; idempotent     | 1. As manager, use 'Set day status' to mark CPO-2 as Leave for the shift date.<br>2. Let the shift window pass and the 5-min rollup sweep run (or invoke sweepOnce).<br>3. Open Admin Attendance and inspect the Absent counter and records for CPO-1/CPO-2.<br>4. Trigger a second sweep and re-check.                                                                                                                                                                                                                                    | CPO-1 gets exactly ONE auto-generated 'absent' record carrying the shift_id (review_status 'none'). A second sweep adds NO duplicate — the WHERE NOT EXISTS (session for this shift+cpo) makes it idempotent. CPO-2 is NOT auto-marked absent: a day-status marker (shift_id NULL, status in leave/sick/off_duty/absent, same date) exists, matched by the second NOT EXISTS (D6-c). CPO-2 shows Leave. Only shifts ended within the last 2 days are scanned; the Redis lock single-fires across replicas.                                                                                                                                                          | 1 device: provider manager (+ backend rollup running)                         |     |        |       |
| ASHIFT-15 | P1  | error      | Offline / backend error during approve — record preserved, actionable alert, controls re-enabled                    | 1. Enable airplane mode (or block the auth-service).<br>2. Tap Approve on a pending record and confirm in the modal.<br>3. Observe the row and any alert.<br>4. Restore connectivity and pull-to-refresh, then approve again.                                                                                                                                                                                                                                                                                                              | The reviewSession call fails; doReview catches and shows alert 'Review — Could not update. Please try again.' (or the server message). Because load() only runs on success, the record REMAINS in the Pending Review queue unchanged; the busy spinner clears and buttons re-enable (busyId reset in finally). After reconnect + refresh the record is still pending and can be approved successfully. No optimistic removal, no partial write.                                                                                                                                                                                                                     | 1 device: provider manager (network-controllable)                             |     |        |       |
| ASHIFT-16 | P0  | security   | Wrong role / flag off — a CPO/member cannot reach admin review or shift management (guard not skippable)            | 1. Log in as the managed CPO and inspect the Departmental Attend tab.<br>2. Confirm no Admin Attendance dashboard (and therefore none of its Shifts / Day status segments) is exposed.<br>3. Directly exercise the manager endpoints with the CPO token: GET /attendance/org/summary, /attendance/org/pending, /attendance/shifts, POST /attendance/shifts, POST /attendance/day-status, PATCH /attendance/sessions/:id/review, POST /attendance/org/export.<br>4. Also confirm behaviour with DEPT_CHAT_V2 OFF for the flag-gated routes. | CPO UI shows only the self attendance surface (My Attendance / check-in). Every manager endpoint returns 403 org_manager_access_required for the CPO token — OrgManagerGuard re-reads the DB (agents type='company' status='ACTIVE', or org_members manager row) and trusts NO manager claim from the JWT; there is no dev/skip bypass branch. With the flag OFF the v2-gated routes 404 via DeptChatV2Guard (invisible) while JWT auth still applies (guard is never replaced by the flag).                                                                                                                                                                        | 1 device: managed CPO (member)                                                |     |        |       |
| ASHIFT-17 | P2  | error      | Capture-location permission denied in Shift editor — graceful fallback, geofence-less save still possible           | 1. New Shift → fill Department/Site, select a CPO, set a valid window.<br>2. Tap 'Use current location'.<br>3. Observe the alert and the geofence state.<br>4. Save the shift without a geofence.                                                                                                                                                                                                                                                                                                                                          | getGeo() returns null → non-crashing alert 'Location — Could not read your location. Grant permission or enter coordinates manually.' The capturing spinner clears (finally). No coordinates are set and no fake coordinates are written. The shift can still be created without a geofence (time-only attendance); the radius rule does not fire (hasCoords false). No silent success.                                                                                                                                                                                                                                                                             | 1 device: provider manager (location denied)                                  |     |        |       |
| ASHIFT-18 | P1  | security   | Export accuracy — CSV is biometric-free, correctly scoped, and audited                                              | 1. On ops-console, log in as the provider manager and open Dept Attendance.<br>2. Apply a date range (and, if company account, a department filter).<br>3. Click Export CSV (and Export PDF).<br>4. Open the file and inspect columns and values.<br>5. Repeat as a department-scoped manager.                                                                                                                                                                                                                                             | CSV header is exactly: Member, Member ID, Department, Site, Check-in, Check-out, Status, Face verified, In radius, Admin notes. Face verified / In radius are yes/no/empty RESULT booleans only — NO face_meta, no biometric image/descriptor (face_meta is never SELECTed). Rows honor the date range and are org-scoped; a department-scoped manager's export is FORCED to their department (manager.department ?? dto.department). An attendance.export audit row (filters + count, format 'csv', no PII narrative) is written BEFORE the body returns. PDF is a client-side print of the same audited data. Counts reconcile with the Admin Attendance summary. | ops-console (web): provider company manager + one department-scoped manager   |     |        |       |
| ASHIFT-19 | P1  | golden     | Set day status — leave/sick/off-duty/absent marker written, re-marking REPLACES (no stacking), foreign CPO rejected | 1. Admin Attendance → 'Set day status'.<br>2. Mark CPO-1 as Leave for today (add an optional note); save.<br>3. Re-open and re-mark the same CPO-1 for the same date as Sick leave; save.<br>4. Open My Attendance as CPO-1 (or the org summary) and inspect the marker.<br>5. Attempt to set a day status for a CPO who is NOT an active member of this org (tamper cpo_user_id).                                                                                                                                                         | Step 2 writes a marker session (shift_id NULL, status='closed', attendance_status='leave', review_status='approved', reviewed_by=manager). Step 3 REPLACES it — setDayStatus first DELETEs any same-CPO/same-date NULL-shift marker then INSERTs, so exactly ONE marker exists (Sick leave), never two stacked rows (D6-f). Audited attendance.day_status. Step 5 → 400 cpo_not_active_member_of_org. The marker also suppresses auto-absent for that date (see ATT-ADM-14).                                                                                                                                                                                        | 1 device: provider manager (+ multi-org seed for the negative case)           |     |        |       |
| ASHIFT-20 | P1  | regression | Check-out verification failure surfaces in the admin queue; clean early check-out shows Early checkout              | 1. On the CPO device, clock OUT from outside the approved radius (or with camera denied).<br>2. On the manager device, open Admin Attendance → PENDING REVIEW.<br>3. Approve that record.<br>4. Separately: have another CPO clock out cleanly but well BEFORE shift end (> grace), and inspect its status (not the queue).                                                                                                                                                                                                                | The failed check-out flags the session Pending Review — deriveCheckOut sets review_reason (out_of_radius / camera_unavailable / permission_denied / face_mismatch) and attendance_status→pending_review (a row already pending is never re-flagged; captured clock_in stays immutable). It appears in the admin queue with the matching reason label. On approve, status is re-derived to Present/Late from clock_in vs shift start (NOTE: this overwrites any early_checkout marker — confirm this is acceptable). The clean early check-out (step 4) sets attendance_status='early_checkout' and does NOT enter the review queue.                                 | 2 devices: provider manager + managed CPO                                     |     |        |       |
| ASHIFT-21 | P1  | regression | Member dispute lands in the admin Pending Review queue and can be resolved                                          | 1. On the CPO device, open My Attendance → Dispute that closed record, add a note, submit.<br>2. On the manager device, refresh Admin Attendance → PENDING REVIEW.<br>3. Inspect the disputed row's reason label.<br>4. Approve (or reject) the dispute.<br>5. Negative: on the CPO device try to dispute a record that is still open, or one already pending.                                                                                                                                                                             | disputeSession flips the record back to review_status='pending', review_reason='disputed', stores dispute_note (truncated 500), audited attendance.dispute. It appears in the manager queue with label 'Disputed by the member' (reviewReasonLabel 'disputed'). Manager resolves via the normal reviewSession flow — approve re-derives Present/Late, reject → terminal Absent. Step 5: disputing an OPEN record → 400 shift_still_open; disputing an already-pending record → 400 already_pending_review.                                                                                                                                                          | 2 devices: provider manager + managed CPO                                     |     |        |       |
| ASHIFT-22 | P1  | edge       | Correct a session's clock times (forgotten clock-out) — original capture preserved, open session stays clockable    | 1. As manager, PATCH /attendance/sessions/:id with a corrected clock_out_at + edit_reason (via the org session edit surface).<br>2. Inspect the org_audit_log row for the edit.<br>3. On an OPEN session, PATCH only clock_in_at (no clock_out_at) with an edit_reason.<br>4. Have that CPO attempt to clock out afterwards.<br>5. Attempt the same PATCH against a session belonging to another org.                                                                                                                                      | editShift is transactional (FOR UPDATE): it writes edited_by/edited_at/edit_reason and records the ORIGINAL clock_in_at/clock_out_at in the attendance.shift.edit audit metadata (before/after), so the pre-edit capture is always recoverable (PDF p.9). Setting clock_out_at closes the session (status→'edited'). Editing only clock_in on an OPEN session PRESERVES status='open' (D6-a), so the CPO can still clock out (clockOut matches status='open') and the open-shift unique guard still blocks a second clock-in. A foreign-org session id → 404 shift_not_found_in_org.                                                                                | 1 device: provider manager (+ managed CPO for the open-session step)          |     |        |       |
| ASHIFT-23 | P1  | edge       | Pending-queue reason labels are correct per failure type and follow the derivation precedence                       | 1. Generate check-ins that fail for each cause: offline, location-not-shared, camera denied, face-not-confirmed, outside radius.<br>2. On the manager device, open PENDING REVIEW and read each row's reason label.<br>3. Generate a check-in that fails MULTIPLE conditions at once (e.g. offline AND out of radius; or camera denied AND out of radius).                                                                                                                                                                                 | Each reason label renders correctly: out_of_radius→'Outside the approved site radius', face_mismatch→'Face check could not be confirmed', camera_unavailable→'Camera unavailable or not allowed', permission_denied→'Location was not shared', offline→'Submitted offline', disputed→'Disputed by the member'. Precedence follows deriveCheckIn's ordered checks: offline > no-coords(permission_denied) > face_unavailable(camera_unavailable) > face_ok===false(face_mismatch) > out_of_radius. So an offline+out-of-radius check-in shows 'Submitted offline', and a camera-denied+out-of-radius shows 'Camera unavailable', not the radius reason.              | 2 devices: provider manager + managed CPO                                     |     |        |       |
| ASHIFT-24 | P1  | security   | Manager access revoked mid-session — suspended company / demoted manager loses admin access immediately             | 1. Confirm the manager can load Admin Attendance and the pending queue.<br>2. Backend: set the company agent status to a non-ACTIVE value (or set the delegated manager's org_members row to status<>'active' / member_role<>'manager').<br>3. WITHOUT re-logging in (same JWT), pull-to-refresh Admin Attendance and retry a review/export call.                                                                                                                                                                                          | The next request 403s org_manager_access_required. OrgManagerGuard RE-READS the DB every call — Path 1 requires agents type='company' AND status='ACTIVE' (D4-c), Path 2 requires an active manager org_members row — so a still-valid JWT cannot fabricate org ownership after the DB state changes. No cached/claim-based bypass.                                                                                                                                                                                                                                                                                                                                 | 1 device: provider manager (+ backend DB access to flip status)               |     |        |       |
| ASHIFT-25 | P2  | edge       | App killed / backgrounded mid-review — no partial write, record and notes not persisted                             | 1. Tap Approve on a pending record; the modal opens.<br>2. Type a note in the modal.<br>3. Force-kill (or hard-background) the app BEFORE tapping the confirm 'Approve' button.<br>4. Relaunch the app and reopen Admin Attendance.                                                                                                                                                                                                                                                                                                        | No reviewSession call was ever fired (the request only fires on confirm). On relaunch the record is STILL pending, unchanged; the typed note was never persisted (notes state is local until confirm). No partial/duplicate write, no stuck busyId, counters unchanged. useFocusEffect reloads a clean, current queue.                                                                                                                                                                                                                                                                                                                                              | 1 device: provider manager                                                    |     |        |       |
| ASHIFT-26 | P2  | edge       | Department-scoped summary/export excludes non-shift day-status markers (leave/sick/off-duty)                        | 1. As the Operations-scoped manager, open Admin Attendance for a range covering the marker date.<br>2. Inspect whether the Leave/Sick/Off-duty count appears in the summary.<br>3. Export CSV and check whether the marker row is present.<br>4. Compare against an unscoped company-account manager's view of the same data.                                                                                                                                                                                                              | Because orgSummary/exportSessions filter department via LEFT JOIN cpo_shifts sh ON sh.id=ses.shift_id then sh.department=$dept, a day-status marker (shift_id NULL → sh.department NULL) does NOT match a department filter — so a department-scoped manager's counts and export OMIT leave/sick/off-duty/absent markers, while the unscoped manager (no forced department) sees them. Confirm whether this reporting omission is intended; flag as a potential defect if day-status markers should surface to the owning department's manager.                                                                                                                     | 1 device: department-scoped manager + 1 company-account manager (shared seed) |     |        |       |

## 6. Incident Reporting (Reporter / CPO side) (38 cases)

| ID     | Pri | Type       | Scenario                                                                                                         | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Devices/Roles                                                                         | Bug                                                                      | Status | Notes |
| ------ | --- | ---------- | ---------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------ | ----- |
| INC-01 | P0  | golden     | Golden path — submit an incident end-to-end (category → severity → description → submit → confirmation)          | 1. Open the Departmental Incident tab → land on the category grid (STEP 1; client review vs2 item 15 made it the member root).<br>2. On STEP 1, tap a category tile (e.g. 'Safety Issue') — tile highlights with glow.<br>3. Tap a severity chip (e.g. 'High') — chip fills with its colour.<br>4. Tap Continue.<br>5. On STEP 2 confirm the summary card shows the chosen category + severity chip.<br>6. Type a clear description in 'What happened'.<br>7. Tap 'Submit Report'. | App navigates (replace) to the Incident Submitted screen showing a check-decagram badge, 'Incident Submitted', a REFERENCE value in INC-YYYY-NNNNN format, and SUBMITTED + severity chips. Tapping Done pops to the tab root and opens My Reports; back from there is the category grid (the root). No plaintext description appears anywhere outside the report.                                                                                                                                                                                                                                                                                                                   | 1 device: CPO agent (reporter) + a provider org with ≥1 active manager                |                                                                          |        |       |
| INC-02 | P1  | error      | Required selection — Continue stays disabled until BOTH category and severity are chosen                         | 1. Land on STEP 1 with nothing selected — observe the Continue button.<br>2. Tap only a category tile (no severity). Observe Continue.<br>3. Deselect is not possible; instead tap only a severity chip on a fresh entry (no category). Observe Continue.<br>4. Now select both a category and a severity.                                                                                                                                                                         | Continue is disabled (dimmed, non-tappable) with zero, category-only, or severity-only selected. It only enables once both a category AND a severity are selected (ready = category!==null && severity!==null). Tapping a disabled Continue does nothing (no navigation).                                                                                                                                                                                                                                                                                                                                                                                                           | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-03 | P1  | error      | Required field — blank / whitespace-only description is rejected on submit                                       | 1. Leave 'What happened' empty and tap 'Submit Report'.<br>2. Dismiss the alert, type only spaces in the description, tap 'Submit Report' again.<br>3. Type a real description and submit.                                                                                                                                                                                                                                                                                         | Steps 1 and 2 show the alert 'Please add a short description of what happened.' and NO network request is sent / no report is created (client trims then checks non-empty). Step 3 submits successfully. Note: server @Length(1,5000) guards empty but NOT whitespace-only (a single space has length 1), so the whitespace block is client-side only — do not rely on the server for it.                                                                                                                                                                                                                                                                                           | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-04 | P2  | edge       | Description length cap — input is clamped at 5000 chars and counter reflects it                                  | 1. Paste or type a very long string (>5000 chars) into 'What happened'.<br>2. Watch the counter under the field.<br>3. Submit.                                                                                                                                                                                                                                                                                                                                                     | The field never exceeds 5000 characters (onChangeText slices to MAX=5000); the counter reads N/5000 and caps at 5000/5000. Submit succeeds and the stored/displayed description is the 5000-char prefix, then trimmed (trailing whitespace removed by submit's .trim()), matching server @Length(1,5000).                                                                                                                                                                                                                                                                                                                                                                           | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-05 | P1  | golden     | Attach location — GPS captured once on tap and shown as a readable address                                       | 1. Tap 'Attach my location'.<br>2. Grant the OS location prompt if shown.<br>3. Wait for the capture (row shows 'Getting location…').<br>4. Observe the attachment row after capture.<br>5. Submit and open the report from My Reports.                                                                                                                                                                                                                                            | Location is captured a single time via getCurrentPosition (no watch/continuous/background tracking). The row flips to 'Location attached' with a map-marker-check icon and a Mapbox reverse-geocoded place name (not raw lat/lng). The submitted report's detail shows the Location label. Coordinates are never logged.                                                                                                                                                                                                                                                                                                                                                            | 1 device: CPO agent (reporter), Location enabled                                      |                                                                          |        |       |
| INC-06 | P1  | error      | Location permission denied — flow degrades gracefully, manual fallback offered, submit still allowed             | 1. Tap 'Attach my location'.<br>2. Deny the OS permission prompt.<br>3. Observe the resulting alert and the screen.<br>4. Enter a site name in the revealed manual field (e.g. 'Main Gate').<br>5. Add a description and submit.                                                                                                                                                                                                                                                   | No crash (getGeo returns null on denial). Alert 'Location was not shared. You can type the site manually, or submit without it.' appears and the manual site input opens automatically (manualOpen=true). The report submits with the manually typed location_label (client-capped 120 chars) and no coordinates. Detail shows the manual label.                                                                                                                                                                                                                                                                                                                                    | 1 device: CPO agent (reporter), location permission denied                            |                                                                          |        |       |
| INC-07 | P2  | edge       | Manual site entry without ever requesting GPS                                                                    | 1. Without tapping 'Attach my location', tap the 'Or enter the site manually' link.<br>2. Type a site label longer than 120 chars.<br>3. Submit and open the report.                                                                                                                                                                                                                                                                                                               | Manual input appears without any OS location prompt. The label is clamped to 120 chars client-side (slice(0,120)). Report submits with only location_label set (no lat/lng); detail shows the label.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-08 | P0  | security   | Photo evidence via camera — encrypted, sealed manager-only, and confirmed on submit                              | 1. Tap 'Add photo evidence' → confirm the safe/lawful advisory alert → tap 'Take photo'.<br>2. Grant camera if prompted and capture a photo.<br>3. Confirm the row flips to 'Photo attached · Encrypted on submit · managers can open it'.<br>4. Add a description and tap 'Submit Report'.<br>5. Land on Incident Submitted (no photo-failure alert). Open the report from My Reports and tap the Evidence item.                                                                  | Photo attaches locally (read by uri, quality 0.7/max 1600px, NOT base64). On submit the report is created first, then bytes read via readUriBytes, encrypted+uploaded to an opaque storage_key, the per-file key sealed (sealOuterTo, device 1) to each recipient (managers + company account + self) and stored as ciphertext; grantMediaAccess runs. Because sealedFor>0 there is NO warning alert — it goes straight to the confirmation. The plaintext photo is never posted to any channel; only the submitter and org managers can decrypt it.                                                                                                                                | 1 device: CPO agent (reporter) + provider org with an active, key-provisioned manager |                                                                          |        |       |
| INC-09 | P2  | golden     | Photo evidence via gallery/library picker                                                                        | 1. Tap 'Add photo evidence' → advisory alert → 'Choose from library'.<br>2. Pick an image.<br>3. Confirm 'Photo attached'. Tap the close (x) on the photo row to remove it, then re-add.<br>4. Submit.                                                                                                                                                                                                                                                                             | System image picker (launchImageLibrary) opens; chosen photo attaches with quality 0.7 / max 1600px applied. The x removes the attachment (setPhoto(null), row reverts to 'Add photo evidence'). Report submits with (or without) the photo depending on final state.                                                                                                                                                                                                                                                                                                                                                                                                               | 1 device: CPO agent (reporter) with gallery images                                    |                                                                          |        |       |
| INC-10 | P1  | error      | Camera / library open failure — error surfaced, report still submittable without a photo                         | 1. Tap 'Add photo evidence' → advisory alert → 'Take photo'.<br>2. Deny the camera permission (or trigger the picker error).<br>3. Observe the result.<br>4. Add a description and submit without a photo.                                                                                                                                                                                                                                                                         | A 'Photo — Could not open the camera or library on this device.' alert appears (runPicker catch); no photo is attached; the screen is intact. The user can still submit the report normally (photo is optional). No crash, no half-attached state.                                                                                                                                                                                                                                                                                                                                                                                                                                  | 1 device: CPO agent (reporter), camera denied/unavailable                             |                                                                          |        |       |
| INC-11 | P1  | error      | Evidence upload fails after report succeeds — submitter is warned (no silent success) and can re-add             | 1. Attach a photo and a description.<br>2. Tap 'Submit Report' and let the report POST succeed but the evidence upload/seal fail.<br>3. Read the resulting alert; tap OK.<br>4. Open the report from My Reports and check the Evidence section.                                                                                                                                                                                                                                    | Because the report already succeeded it is NOT blocked. The failure is NOT hidden: alert 'Your report was sent, but the photo could not be uploaded. You can re-open the report and add it again.' is shown (reason no-runtime/read/upload/attach → attached=false), then navigation proceeds. The report exists in My Reports with correct status; the Evidence section shows no openable photo rather than pretending it attached.                                                                                                                                                                                                                                                | 1 device: CPO agent (reporter), evidence pipeline forced to fail                      |                                                                          |        |       |
| INC-12 | P1  | edge       | Evidence uploaded but sealed for 0 recipients — submitter warned it can't be opened                              | 1. Attach a photo + description and submit.<br>2. Let upload+attach succeed but every seal fail (no recipient keys).<br>3. Read the alert.                                                                                                                                                                                                                                                                                                                                         | Alert 'Your report was sent, but the photo could not be secured for your manager to open. You can re-open the report and add it again.' (attached=true, sealedFor=0, reason no-recipient-keys). Navigation proceeds to confirmation. Server stored the attachment pointer but storeAttachmentKeys was never called, so the manager sees a non-openable evidence pointer — not a leak.                                                                                                                                                                                                                                                                                               | 1 device: CPO agent (reporter) + org manager without provisioned keys                 |                                                                          |        |       |
| INC-13 | P0  | regression | My Reports list reflects the new incident with correct ref, category, severity and status                        | 1. From the Incident Submitted screen tap Done → land on My Reports.<br>2. Verify the just-submitted report appears at the top.<br>3. Pull-to-refresh the list.<br>4. Tap the row to open detail, then go back.                                                                                                                                                                                                                                                                    | The report row shows the correct category label + icon, the ref (INC-YYYY-NNNNN) with submit time, a severity chip (correct colour/label) and a status chip reading SUBMITTED. Newest-first (mine() ORDER BY created_at DESC). Refresh reloads via incidentApi.mine and replaces state (no duplication). Header count pill matches list length.                                                                                                                                                                                                                                                                                                                                     | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-14 | P0  | security   | Member detail is READ-ONLY — no internal manager notes, assignee, or status-transition controls leak             | 1. Open My Reports and tap the report the manager has been working.<br>2. Inspect the entire MyIncidentDetail screen (hero, DETAILS card, Evidence).<br>3. Look for any note text, assignee name, event history, or status-change buttons.<br>4. Pull down / re-open to confirm no manager endpoint is called.                                                                                                                                                                     | MyIncidentDetail is built ONLY from the list DTO param (no call to GET /incidents/:id manager detail). It shows category, current status label, severity, description, location, submitted/updated times, and own evidence. It NEVER renders internal notes, the assignee, the event timeline, or any status-transition button. Current status reflects the last mine() refresh but no internal manager data is exposed.                                                                                                                                                                                                                                                            | 2 accounts: CPO agent (reporter) + provider manager acting on the same incident       |                                                                          |        |       |
| INC-15 | P1  | security   | Submitter can decrypt and view their OWN evidence photo on-device                                                | 1. Open the report from My Reports.<br>2. Scroll to the EVIDENCE section — an 'Encrypted photo · Tap to decrypt & view' row is shown.<br>3. Tap it and wait for the spinner.                                                                                                                                                                                                                                                                                                       | loadEvidenceUri fetches this device's sealed key (getAttachmentKey, device 1), unseals it (openOuterAsSelf), downloads + decrypts the ciphertext (downloadMedia), writes a temp file (writeTempBytes) and renders the image inline. The plaintext is only produced on-device; no plaintext key/photo is fetched from or logged by the server.                                                                                                                                                                                                                                                                                                                                       | 1 device: CPO agent (reporter), same identity that submitted                          |                                                                          |        |       |
| INC-16 | P2  | edge       | Evidence sealed for another device shows a safe 'Can't open on this device' state                                | 1. Open the incident detail with such evidence.<br>2. Tap the evidence row.                                                                                                                                                                                                                                                                                                                                                                                                        | getMyAttachmentKey returns 404 (no_sealed_key_for_this_device) → loadEvidenceUri returns null; the UI shows a disabled row 'Can't open on this device — Sealed for another device, or unavailable' with an image-off icon. No crash, no error toast loop, no plaintext.                                                                                                                                                                                                                                                                                                                                                                                                             | 1 device: CPO agent after reinstall / different device                                |                                                                          |        |       |
| INC-17 | P1  | error      | Cancel mid-report — backing out creates no incident                                                              | 1. On STEP 1 pick a category + severity, tap Continue.<br>2. On STEP 2 type a description and attach a location.<br>3. Tap the header back arrow twice (or use the OS back gesture) to exit the wizard without submitting.<br>4. Return to My Reports and refresh.                                                                                                                                                                                                                 | No POST /incidents is fired; no report, ref, or event row is created. My Reports is unchanged. Re-entering the wizard starts blank (STEP 2 local state is fresh) — the report body is written exactly once, only on Submit.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-18 | P1  | error      | Offline submit — clear error, NO phantom reference, retry works when back online (no local queue exists)         | 1. With the device offline, tap 'Submit Report'.<br>2. Observe the button/spinner and the resulting alert.<br>3. Dismiss the alert; confirm you are still on STEP 2 with your input intact.<br>4. Check My Reports — the incident must NOT be listed.<br>5. Restore connectivity and tap 'Submit Report' again.                                                                                                                                                                    | Submit fails with a 'Could not submit. Please try again.' (or server message) alert; the busy spinner clears (finally) and the form is preserved. NO reference is shown and NO row appears in My Reports (there is intentionally no offline outbox/queue — document as a known limitation, not a passed queue test). After reconnecting, the retry succeeds and produces exactly ONE report.                                                                                                                                                                                                                                                                                        | 1 device: CPO agent (reporter), toggled offline→online                                |                                                                          |        |       |
| INC-19 | P0  | security   | Cross-reporter isolation — a member sees only their OWN reports, never a peer's                                  | 1. On Reporter A's device open My Reports and note A's refs.<br>2. On Reporter B's device open My Reports and note B's refs.<br>3. Compare the two lists.<br>4. (If possible) attempt to open B's incident id / evidence from A's session.                                                                                                                                                                                                                                         | incidentApi.mine is server-scoped to submitter_id: each list contains only that account's own reports. A never sees B's refs/descriptions/evidence and vice-versa, even sharing an org. Any attempt by a non-manager peer to read another's evidence returns 403 (not_authorized_for_incident_evidence).                                                                                                                                                                                                                                                                                                                                                                            | 2 devices: CPO Reporter A + CPO Reporter B, same org                                  |                                                                          |        |       |
| INC-20 | P0  | security   | Cross-org isolation — a reporter's incident is invisible to a DIFFERENT org's managers/members                   | 1. Reporter (Org 1) submits an incident and notes its ref/id.<br>2. On the Org 2 manager device open the incident queue.<br>3. On the Org 2 member device open My Reports.<br>4. Attempt (if the id is known) to open the Org 1 incident detail/evidence from Org 2 accounts.                                                                                                                                                                                                      | The Org 1 incident never appears in Org 2's queue (queue is WHERE org_user_id = manager's org) nor in any Org 2 member's My Reports (scoped to submitter_id). Direct detail returns 404 incident_not_found_in_org; evidence returns 403. No cross-org leak of body, location, or evidence.                                                                                                                                                                                                                                                                                                                                                                                          | 3 accounts: CPO Reporter (Org 1) + manager (Org 2) + member (Org 2)                   |                                                                          |        |       |
| INC-21 | P2  | regression | All 15 categories are selectable and each maps to a backend-accepted value                                       | 1. Verify all 15 category tiles render with a label + icon (Security Concern, Safety Issue, Medical Incident, Suspicious Activity, Access Control, Property Damage, Vehicle Issue, Staff Misconduct, Visitor/Contractor, Equipment Failure, Operational Disruption, Workplace Harassment, Lost Property, Fire Hazard, Other).<br>2. For a representative sample (ideally each), select the category + a severity and submit.<br>3. Confirm each submission is accepted (no 400).   | All 15 client keys (incidentMeta INCIDENT_CATEGORY_META) exactly match the backend allow-list (incident.constants.INCIDENT_CATEGORIES / DTO @IsIn) — verified byte-for-byte in code. Every submission is accepted (HTTP 201) and renders the correct category label in My Reports. A missing/renamed key would surface as a 400 or an unlabeled row.                                                                                                                                                                                                                                                                                                                                | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-22 | P1  | perf       | Large / multi-MB photo does not crash or 'refresh' the in-progress report (memory-pressure guard)                | 1. Type a description.<br>2. Attach a large photo via camera or library.<br>3. Background and foreground the app; rotate; scroll.<br>4. Submit.                                                                                                                                                                                                                                                                                                                                    | The photo is held only as a {uri, mime} in screen state (bytes read via readUriBytes at upload time, NOT base64 in state), so the host activity is not reclaimed and the in-progress report is preserved through backgrounding/memory pressure. Submit encrypts + uploads from the uri without OOM. The report and (on success) evidence complete normally.                                                                                                                                                                                                                                                                                                                         | 1 low-RAM device: CPO agent (reporter)                                                |                                                                          |        |       |
| INC-23 | P2  | edge       | Submit with no evidence and no location — minimal valid report                                                   | 1. Choose category + severity on STEP 1, Continue.<br>2. Enter only a description (no photo, no location, no manual site).<br>3. Submit and open the report.                                                                                                                                                                                                                                                                                                                       | Report submits with a ref; detail shows description, severity, status SUBMITTED, submitted time, and NO Location row (location_label null) and NO Evidence section (EvidenceSection renders null when 0 attachments). No errors about missing attachments.                                                                                                                                                                                                                                                                                                                                                                                                                          | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-24 | P2  | edge       | Location captured but reverse-geocode unavailable — coords still attach with a generic label                     | 1. Tap 'Attach my location' and let the GPS fix succeed.<br>2. Observe the attachment row label after capture.<br>3. Submit and open the report detail.                                                                                                                                                                                                                                                                                                                            | getGeo returns real coords; reverseGeocode returns null (no token / miss / offline) so the label falls back to 'Current location'. The row still shows 'Location attached' with map-marker-check, real lat/lng are sent as location_lat/lng, and location_label = 'Current location'. A geocode miss NEVER blocks the report. No coordinates or address are logged.                                                                                                                                                                                                                                                                                                                 | 1 device: CPO agent (reporter), Mapbox token absent or unreachable                    |                                                                          |        |       |
| INC-25 | P2  | error      | GPS permission granted but the fix times out — manual fallback still offered                                     | 1. Tap 'Attach my location'.<br>2. Wait through 'Getting location…' until it resolves.<br>3. Observe the alert and screen.<br>4. Enter a manual site and submit.                                                                                                                                                                                                                                                                                                                   | getGeo resolves null on the position error/timeout (distinct from permission denial). Same graceful path as INC-R-06: alert offers manual entry, manualOpen=true. locBusy clears (finally) so the row is tappable again. Report submits with the manual label and no coordinates. No spinner stuck, no crash.                                                                                                                                                                                                                                                                                                                                                                       | 1 device: CPO agent (reporter), no GPS fix                                            |                                                                          |        |       |
| INC-26 | P2  | edge       | GPS overrides a manual site entry when both are provided                                                         | 1. Tap 'Or enter the site manually' and type a site (e.g. 'Warehouse B').<br>2. Then tap 'Attach my location' and let a GPS fix succeed.<br>3. Submit and open the report detail.                                                                                                                                                                                                                                                                                                  | On a successful fix, setCoords is set and manualOpen is closed. The submit payload uses coords.label + coords lat/lng (the manual branch only applies when coords is null), so the GPS-derived label wins and the typed manual text is discarded. Detail shows the geocoded location, not 'Warehouse B'. No duplicate/conflicting location fields sent.                                                                                                                                                                                                                                                                                                                             | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-27 | P2  | edge       | Reverse-geocoded place name longer than the server's 200-char location_label cap                                 | 1. Tap 'Attach my location' and let a long-place-name fix attach.<br>2. Add a description and tap 'Submit Report'.<br>3. Observe the result.                                                                                                                                                                                                                                                                                                                                       | LATENT BUG CHECK: the client sends coords.label UNCAPPED (only manual entry is sliced to 120; the GPS label is not sliced), but the server enforces @Length(1,200) on location_label. A >200-char place_name causes the whole POST /incidents to 400, so the report fails and the generic 'Report Incident' error alert shows the server message — the user loses the report. Expected behaviour (once fixed) is the label is clamped client-side and the report still submits. File/confirm as a defect if reproduced.                                                                                                                                                             | 1 device: CPO agent (reporter), long-address location                                 | latent: uncapped GPS location_label vs server @Length(1,200)             |        |       |
| INC-28 | P2  | security   | Member payload data-minimization — incidents/mine must not expose internal manager fields                        | 1. As the reporter, capture the raw JSON of GET /incidents/mine (proxy/network inspector or debug build).<br>2. Inspect each report object's fields.<br>3. Confirm the UI (MyIncidents / MyIncidentDetail) renders none of the internal fields.                                                                                                                                                                                                                                    | UI correctly hides internal data. HOWEVER mine() is a SELECT \* so the response body includes assigned_to (and department) even though they are never displayed — an over-fetch/data-minimization weakness: an assignee's user id rides the member-facing payload. Flag as a hardening finding (project the member DTO to category/severity/status/description/location/timestamps/ref only). No internal NOTE text is present (notes live in incident_events, never returned to mine()).                                                                                                                                                                                           | 1 device: CPO agent (reporter) with network inspection                                | mine() SELECT \* leaks assigned_to in member payload                     |        |       |
| INC-29 | P1  | regression | Department routing gap — CPO-submitted reports never carry a department, so a dept-scoped manager can't see them | 1. Reporter submits an incident from the mobile wizard.<br>2. On the department-scoped manager's device/console open the Incident Queue.<br>3. Attempt to open that incident's detail as the dept-scoped manager.<br>4. Compare with an UNSCOPED (company/org-wide) manager opening the same queue.                                                                                                                                                                                | BUG CHECK: the mobile submit (ReportIncidentDetailsScreen) never sends a `department`, so incident_reports.department is always NULL. The manager queue/detail force `AND ($ IS NULL OR department = $)` with department = manager.department, and `NULL = 'security'` is false — so a department-scoped manager sees an EMPTY queue and gets 404 (incident_not_found_in_org) on detail for every CPO-submitted report. The unscoped manager sees it fine. Push alerts still fan out org-wide (resolveOrgManagers with department null → all managers). Confirm/flag: CPO reports do not reach a department-scoped manager as spec'd (PDF p.11 'routes to the department manager'). | 3 accounts: CPO reporter + department-scoped manager + unscoped manager               | client omits department on submit → dept-scoped manager 404/empty queue  |        |       |
| INC-30 | P1  | error      | Process death between report POST and evidence seal — report persists with silent evidence loss                  | 1. Attach a photo + description and tap 'Submit Report'.<br>2. Force-kill the app in the window AFTER the report POST returns 201 but BEFORE uploadAndSealEvidence finishes (report exists, evidence not yet sealed).<br>3. Relaunch, open My Reports and the just-created report's Evidence section.                                                                                                                                                                              | The report exists in My Reports (created_at, ref, SUBMITTED) because the report POST already committed. The evidence pipeline was interrupted, so there is NO attachment (or an unsealed/partial one) AND the 'could not be uploaded/secured' warning alert NEVER fired (the process died before it). Result: the submitter is unaware the photo did not attach. Confirm the Evidence section shows nothing / non-openable rather than a false 'attached'. Flag the silent-loss window as a gap (no resumable evidence outbox).                                                                                                                                                     | 1 device: CPO agent (reporter), force-killed mid-submit                               | no resumable evidence outbox → silent photo loss on process death        |        |       |
| INC-31 | P2  | security   | Partial seal — some managers provisioned, some not — succeeds with no warning yet some can't open                | 1. Attach a photo + description and submit.<br>2. Let upload+attach succeed and the seal loop partially succeed (sealedFor ≥ 1, sealFails ≥ 1).<br>3. Observe the confirmation (no alert).<br>4. On the UNprovisioned manager's device, open the incident evidence.                                                                                                                                                                                                                | Because sealedFor>0 the submit shows NO warning and lands on the confirmation — even though one or more managers were skipped (seal failed). The skipped managers get a 404 no_sealed_key_for_this_device and see 'Can't open on this device'. Document that partial seal is silent to the submitter (only sealedFor===0 warns); the provisioned recipients + submitter CAN open it, so there is no plaintext leak, but coverage is incomplete.                                                                                                                                                                                                                                     | 2+ accounts: CPO reporter + one provisioned + one unprovisioned manager               |                                                                          |        |       |
| INC-32 | P1  | regression | Ambiguous network failure on submit — lost response causes a duplicate report on retry (no idempotency)          | 1. Tap 'Submit Report'; let the server create the report but the client never receives the 201 (timeout/reset).<br>2. Client shows the error alert; user taps 'Submit Report' again.<br>3. Check My Reports and the manager queue.                                                                                                                                                                                                                                                 | DEFECT CHECK: submit has NO idempotency key. The first (lost-response) attempt already created a report; the retry creates a SECOND report with a new ref. Two near-identical incidents (two INC-YYYY-NNNNN refs) exist for one real event. Confirm/flag as a duplicate-submission risk (recommend a client idempotency token or a short dedupe window). Contrast with INC-R-18 where the request never reached the server (no duplicate).                                                                                                                                                                                                                                          | 1 device: CPO agent (reporter), lost-response injection                               | no idempotency key on POST /incidents → duplicate on lost-response retry |        |       |
| INC-33 | P1  | security   | Feature flag OFF — incident endpoints 404 and the report entry point is not reachable                            | 1. Confirm the Departmental Incident tab / 'Report incident' entry is hidden or gated in the UI.<br>2. If a direct API path is available (debug), call POST /incidents and GET /incidents/mine.<br>3. Observe responses.                                                                                                                                                                                                                                                           | The whole /incidents controller is behind DeptChatV2Guard → every route returns 404 when the flag is off (feature hidden, not just 403). The client offers no reachable Report entry. The guard cannot be skipped from the client; there is no 'dev bypass' branch. No incident can be created while the flag is off.                                                                                                                                                                                                                                                                                                                                                               | 1 device: reporter with Dept Chat v2 flag OFF                                         |                                                                          |        |       |
| INC-34 | P1  | security   | Unauthenticated / expired session cannot submit — no anonymous incident creation                                 | 1. On STEP 2 with valid input, let the access token expire (or force it invalid).<br>2. Tap 'Submit Report'.<br>3. Observe the result / any refresh flow.                                                                                                                                                                                                                                                                                                                          | POST /incidents (JwtAuthGuard) returns 401 for a missing/expired token; no report is created. The client surfaces the error (or silently refreshes then retries with a valid token). There is no path to create an incident without a valid JWT — submitter_id is always taken from user.sub server-side, never from the body.                                                                                                                                                                                                                                                                                                                                                      | 1 device: CPO agent (reporter), expired session                                       |                                                                          |        |       |
| INC-35 | P2  | edge       | Solo self-registered agent with no managing org — submit still works, self is the only evidence recipient        | 1. Submit an incident (with a photo) as the solo agent.<br>2. Open My Reports → the report.<br>3. Open the Evidence and decrypt it.                                                                                                                                                                                                                                                                                                                                                | resolveOrg falls back to submitterId (org_user_id = self), so the report is created and org-scoped to the agent's own id. evidenceRecipients returns just [self]; the photo seals to the submitter's own device (sealedFor=1, no warning). The agent can decrypt their own evidence. No manager exists to alert, which is expected for a solo agent — not an error.                                                                                                                                                                                                                                                                                                                 | 1 device: solo self-registered agent (no org)                                         |                                                                          |        |       |
| INC-36 | P2  | edge       | Timezone / clock — submitted-time renders in device-local time while the ref year is server-UTC                  | 1. Set the device to an extreme timezone (and optionally a wrong clock).<br>2. Submit an incident and note the REFERENCE (INC-YYYY-...).<br>3. Open My Reports and the detail; compare the shown 'Submitted' time to the ref year.<br>4. Change the device timezone and re-open the detail.                                                                                                                                                                                        | The ref year comes from server now() (UTC) via to_char, so it is authoritative and unaffected by the device clock/timezone. fmtTime renders created_at with device-local toLocaleString (no fixed UTC), so the displayed date/time shifts with the device timezone and can, at a year boundary, differ from the ref's UTC year. Verify: no crash, ref is stable, and the local-time display is at least internally consistent. Note the ref-year(UTC) vs shown-date(local) divergence as a display inconsistency vs the 'UTC-everywhere' rule.                                                                                                                                      | 1 device: CPO agent (reporter), extreme timezone/clock                                |                                                                          |        |       |
| INC-37 | P2  | perf       | Double-tap / rapid Submit produces exactly one report                                                            | 1. Rapidly double- or triple-tap 'Submit Report'.<br>2. Watch the button busy state.<br>3. After navigation, check My Reports and the manager queue.                                                                                                                                                                                                                                                                                                                               | The `if (busy) return` guard plus the PrimaryButton busy state block re-entrant submits, so exactly ONE POST /incidents fires and exactly ONE report/ref is created. No duplicate rows from double-tap. (Distinct from INC-R-32 which is a lost-response server-side duplicate, not a client double-fire.)                                                                                                                                                                                                                                                                                                                                                                          | 1 device: CPO agent (reporter)                                                        |                                                                          |        |       |
| INC-38 | P2  | edge       | Large font scale / accessibility — the wizard stays usable and Submit reachable                                  | 1. Set the device font scale to maximum.<br>2. Walk STEP 1 (category grid + severity row) and STEP 2 (description, attachments, Submit).<br>3. Confirm every control is reachable and text isn't clipped to unreadability.                                                                                                                                                                                                                                                         | scaleTextStyles adapts type; category tiles (numberOfLines=2) and chips remain tappable, the description field scrolls, and the sticky 'Submit Report' footer stays visible/tappable above the safe-area inset. No control is pushed off-screen or overlapped so the reporter cannot complete a submission at max font scale.                                                                                                                                                                                                                                                                                                                                                       | 1 device: CPO agent (reporter), max font scale                                        |                                                                          |        |       |

## 7. Incident Queue, Triage & Dispute (Manager / Ops side) (27 cases)

| ID      | Pri | Type       | Scenario                                                                                                      | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Devices/Roles                                                                                                                                                                                                                                                                                                                       | Bug                                                                       | Status | Notes |
| ------- | --- | ---------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------ | ----- | --- |
| IQUE-01 | P0  | golden     | Manager opens Incident Queue — own-org incidents load, Critical/High sorted to top                            | 1. Log in as the provider manager. 2. Open the departmental section and tap the Incident tab / navigate to Incident Queue. 3. Observe the header pill count and the ordering of the incident cards. 4. Read each card: severity bar colour, category label+icon, ref (INC-YYYY-NNNNN), severity tag, status tag, relative updated time.                                                                                                                                                                           | Queue loads (spinner then list). Header pill shows items.length, matching the visible card count. Cards are ordered Critical→High→Medium→Low, and within a severity by updated_at DESC (server ORDER BY CASE severity …, updated_at DESC). Every card shows a valid INC-YYYY-NNNNN ref, correct category label/icon, severity + status tags with correct colours, and a relative time from updated_at. Only incidents where org_user_id = this manager's org appear.                                                                                                                                                                                                                                                           | 1 device — delegated provider manager (department = NULL)                                                                                                                                                                                                                                                                           |                                                                           |        |       |
| IQUE-02 | P0  | golden     | Full incident lifecycle walk: Submitted → Received → Under Review → Action Assigned → Resolved → Closed       | 1. Open Incident Queue, tap the Submitted incident to open detail. 2. Under UPDATE STATUS, tap the single next-status button (Received); on the 'Update status → Move this incident to "Received"?' alert tap Update. 3. When the screen reloads, tap the next button (Under Review) → Update. 4. Continue: Under Review → Action Assigned → Update. 5. Action Assigned → Resolved → Update. 6. Resolved shows two buttons (Closed and Under Review); tap Closed → Update. 7. Scroll the TIMELINE after each hop. | Each transition is confirmed via the alert, applies, and reloads. The status tag updates each hop (Submitted→Received→Under Review→Action Assigned→Resolved→Closed) with the correct label/colour. The TIMELINE gains one 'from → to' entry per hop (swap-horizontal icon, INTERNAL badge, actor timestamp) inserted into incident_events. Once Closed, UPDATE STATUS shows only the 'Under Review' (reopen) button. The Record card (category/severity/description/location) is never altered by any transition — only incident_reports.status + updated_at and an incident_events row change.                                                                                                                                | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-03 | P0  | security   | Reopen closed incident: delegated manager blocked (403), company-admin account succeeds                       | 1. As the delegated manager, open the Closed incident detail. 2. In UPDATE STATUS tap 'Under Review' → on the alert tap Update. 3. Observe the error. 4. Confirm the incident is still Closed and no timeline entry was added. 5. Log out and log in as the company account. 6. Open the same Closed incident, tap 'Under Review' → Update. 7. Observe status + timeline.                                                                                                                                         | Delegated manager: reopen is rejected by the FSM server-side (ForbiddenException invalid_incident_transition:closed->under_review:manager, 403) because actorOf() = 'manager' (user_id != org_user_id). App shows the alert 'Only a company admin can reopen a closed incident.' Status stays Closed; no incident_events row. Company account: actorOf() = 'company_admin' so reopen succeeds, status becomes Under Review, and a Closed → Under Review timeline event is recorded. Proves the reopen gate is enforced by the server FSM per actor, with no client-only guard and no dev-skip branch.                                                                                                                          | 1 device, 2 accounts — delegated provider manager + company (org) account                                                                                                                                                                                                                                                           |                                                                           |        |       |
| IQUE-04 | P0  | security   | Department scoping (read) — dept-scoped manager sees only own-department incidents in queue/detail            | 1. Log in as the Ops-scoped manager and open Incident Queue. 2. Inspect every card; note the count vs the known total org incidents. 3. Apply status/severity filters and re-check. 4. Deep-link / navigate to the known Security-department incident detail (via a stale notification or direct nav param if reachable).                                                                                                                                                                                         | Queue returns ONLY Ops-department incidents (queue() forces department = manager.department; the query param cannot widen it). Security-department incidents never appear, regardless of filter. detail() applies the same forced department filter, so opening the Security incident id returns 'This incident could not be loaded.' (404 incident_not_found_in_org). No cross-department leak of ref, category, description, or evidence via the read paths. NOTE: read paths only — see IQD-17 for the write-path scoping gap.                                                                                                                                                                                              | 1 device — department-scoped provider manager (department='Ops')                                                                                                                                                                                                                                                                    |                                                                           |        |       |
| IQUE-05 | P0  | security   | Cross-org isolation — manager of Org B cannot view or act on Org A's incident                                 | 1. Log in as the Org B manager, open Incident Queue — confirm only Org B incidents show. 2. Attempt to open Org A's incident detail by id (deep link / direct nav). 3. Attempt a status update, an assign, and an add-note against Org A's incident id via the API. 4. Attempt to list Org A's incident evidence and fetch a sealed key.                                                                                                                                                                          | Org A's incident never appears in Org B's queue (org_user_id scoping). detail/updateStatus/assign/addNote all resolve WHERE org_user_id = OrgB → row not found → 404 incident_not_found_in_org (no cross-org read or write). listAttachments / evidenceRecipients / getMyAttachmentKey fail isOrgManager(callerId, OrgA) → 403 not_authorized_for_incident_evidence. No incident data (ref, description, location, evidence, timeline, sealed keys) from Org A is exposed to Org B.                                                                                                                                                                                                                                            | 1 device, 2 accounts — Org A manager (to seed) + Org B manager (attacker)                                                                                                                                                                                                                                                           |                                                                           |        |       |
| IQUE-06 | P0  | security   | Wrong role — CPO member is denied the manager incident queue (403), sees only own reports                     | 1. Log in as the CPO member. 2. Confirm the member UI exposes 'Report Incident' + 'My reports' but NOT the manager Incident Queue tab. 3. Directly call GET /incidents/queue and GET /incidents/:id for their own and another member's incident. 4. Open one of the member's own submitted incidents in 'My reports'.                                                                                                                                                                                             | The member has no manager-queue surface. Any call to /incidents/queue or /incidents/:id is rejected by OrgManagerGuard with 403 org_manager_access_required (server-side, not client hiding). The member-facing view is built from mine() which returns ONLY the member's own incident_reports rows (submitter_id scoped) and NEVER any incident_events — so internal notes, timeline of manager actions, and other members' incidents are never returned. WATCH-ITEM: mine() is SELECT \* so assigned_to/department ride the JSON payload though the UI does not display them — confirm no sensitive field is rendered and consider narrowing the projection.                                                                 | 1 device — CPO member account                                                                                                                                                                                                                                                                                                       |                                                                           |        |       |
| IQUE-07 | P1  | edge       | Only legal next-status buttons rendered; illegal skips not offered; cancel is a no-op                         | 1. Open the Submitted incident — note the UPDATE STATUS buttons. 2. Received incident — note buttons. 3. Under Review incident — note buttons. 4. Resolved incident — note buttons. 5. Closed incident — note buttons. 6. On any incident, tap a next-status button and on the confirm alert tap Cancel. 7. Verify the status is unchanged and no timeline event/network write occurred.                                                                                                                          | Buttons exactly mirror INCIDENT_NEXT / the server FSM: Submitted→[Received]; Received→[Under Review]; Under Review→[Action Assigned]; Action Assigned→[Resolved]; Resolved→[Closed, Under Review] (rework); Closed→[Under Review] (reopen). No button ever offers a skip (e.g. Submitted directly to Resolved). Tapping Cancel on the confirm alert makes no change — status stays, no incident_events row, no PATCH request. There is no UI affordance to reach an illegal transition.                                                                                                                                                                                                                                        | 1 device — company (org) account                                                                                                                                                                                                                                                                                                    |                                                                           |        |       |
| IQUE-08 | P1  | golden     | Assign action owner — 'Assign to me' and reassign to another manager, timeline records it                     | 1. Open the incident detail; note ASSIGNED TO shows 'Unassigned'. 2. Tap the assign card to open the 'Assign owner' sheet. 3. Tap 'Assign to me'. 4. Confirm the card now shows manager A's name with 'Reassign'. 5. Re-open the sheet, pick another manager, confirm reassignment (check mark next to the current owner). 6. Scroll the TIMELINE.                                                                                                                                                                | The sheet lists 'Assign to me' plus other members with member_role='manager' only (roster filtered to managers; no CPO members). Assigning updates incident_reports.assigned_to and ASSIGNED TO flips to 'Reassign', with the current owner check-marked. Each assignment inserts an 'Action owner assigned' incident_events row (from = to = current status, so no status hop) and an incident.assign audit row. Selecting the already-current owner shows the check mark.                                                                                                                                                                                                                                                    | 1 device — provider manager account (org with >=2 managers)                                                                                                                                                                                                                                                                         |                                                                           |        |       |
| IQUE-09 | P1  | error      | Assign blocked on terminal statuses — Resolved/Closed incident cannot be assigned                             | 1. Open the Resolved incident detail. 2. Tap the ASSIGNED TO card, open the sheet, tap 'Assign to me'. 3. Observe the result. 4. Repeat for the Closed incident.                                                                                                                                                                                                                                                                                                                                                  | assign() reads the row FOR UPDATE, sees status resolved/closed, and throws BadRequestException incident_not_assignable (400). The app shows the Assign alert with that message. assigned_to does not change and no 'Action owner assigned' timeline event or audit row is added for either the Resolved or Closed incident. Assignment is only permitted while actively worked (submitted…action_assigned).                                                                                                                                                                                                                                                                                                                    | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-10 | P1  | security   | Internal note — manager adds a note, shows INTERNAL badge in timeline, never visible to the submitting member | 1. As the manager, open the incident detail. 2. In ADD INTERNAL NOTE type a note; paste a >2000-char string to test the cap. 3. Tap Send. 4. Confirm the note appears in TIMELINE with the amber INTERNAL badge and note text; input clears. 5. Log in as the submitting CPO member and open the same incident from 'My reports'.                                                                                                                                                                                 | Manager: addNote posts an incident_events row (from=to=NULL, note_internal=TRUE), it renders in the timeline with the INTERNAL badge and the manager's timestamp; the input is hard-capped at 2000 chars client-side (onChangeText slice) and by the DTO @Length(1,2000). Member: the member view comes from mine() (reports only, no events) so NO internal notes and NO manager-action timeline are ever returned. The org_audit_log incident.note metadata records only {internal: true} — the note text is never audit-logged and never surfaced to the member.                                                                                                                                                            | 1 device, 2 accounts — provider manager + submitting CPO member                                                                                                                                                                                                                                                                     |                                                                           |        |       |
| IQUE-11 | P1  | golden     | Evidence viewing — manager decrypts incident photo evidence on-device; cross-org manager denied               | 1. Open the incident detail as the recipient manager — EVIDENCE section lists the attachment. 2. Tap the encrypted-photo row. 3. Observe the fetch-key → unseal → download → decrypt → render. 4. Confirm no plaintext key or URL is shown in any field. 5. As a cross-org manager, attempt GET /incidents/:id/attachments and .../key for the same incident.                                                                                                                                                     | listAttachments authorises the manager via isOrgManager and returns opaque pointers (storage_key only, never a URL). getMyAttachmentKey returns THIS device's sealed_key (opaque ciphertext); EvidenceSection unseals on-device and the photo renders. No plaintext key/URL appears anywhere. The cross-org manager fails the submitter                                                                                                                                                                                                                                                                                                                                                                                        | org-manager check → 403 not_authorized_for_incident_evidence on both list and key fetch. No key material is logged.                                                                                                                                                                                                                 | 1 device — org manager (recipient device); + 1 cross-org manager account  |        |       |     |
| IQUE-12 | P2  | edge       | Evidence device rotation — sealed key missing for new device degrades gracefully                              | 1. On the manager's new/rotated device, open the incident with existing evidence. 2. Tap the encrypted-photo row to decrypt. 3. Observe the behaviour. 4. Open the same evidence on the manager's original (sealed) device.                                                                                                                                                                                                                                                                                       | getMyAttachmentKey finds no row for this device → 404 no_sealed_key_for_this_device. EvidenceSection catches it (uri === null) and shows the locked/unavailable state ('Can't open on this device' / 'Sealed for another device, or unavailable', image-off icon) rather than crashing or looping; the row is disabled. Opening on the original sealed device still decrypts and renders. No plaintext key material is exposed or logged in either case.                                                                                                                                                                                                                                                                       | 1 device (rotated/new) + original device — same org manager account                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-13 | P1  | golden     | Queue filters and sort — severity chips + status filter row + empty view                                      | 1. Open Incident Queue (defaults: severity 'All', status 'Any status'). 2. Tap severity chip 'Critical' — verify only critical incidents show and the pill updates. 3. Tap 'High', then 'All' to reset. 4. In the horizontal status row tap 'Under Review' — verify only under_review incidents show. 5. Combine 'Critical' severity + 'Resolved' status. 6. Pick a filter combination that matches no incident. 7. Pull-to-refresh under an active filter.                                                       | Each severity chip sends ?severity= and highlights in the severity colour; each status chip sends ?status= server-side and highlights. Combined filters intersect (AND) via the SQL. The header pill (items.length) always matches the visible cards. A no-match combination shows the empty card 'No incidents in this view.' Pull-to-refresh reloads with the active severity+status preserved (load(filter, statusFilter)). Sort order (Critical/High first, then updated_at DESC) holds within every filtered view. NOTE: mobile UI exposes severity+status only — category/date/submitter/department filters exist server-side but are not surfaced.                                                                      | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-14 | P1  | error      | Offline / network-error handling — queue load failure and status-update failure never crash                   | 1. Enable airplane mode. 2. Open (or refresh) Incident Queue. 3. Observe the list + spinner. 4. Re-enable connectivity and pull-to-refresh — confirm incidents return. 5. Open an incident, enable airplane mode, tap a next-status button → Update. 6. Observe the result and the status. 7. Restore connectivity and retry once.                                                                                                                                                                                | Offline queue load fails soft: catch → setItems([]) → the empty card 'No incidents in this view.' renders, finally clears the spinner (no crash, no stuck spinner). Reconnect + refresh restores the real list with filters preserved. Offline status update: doMove catch shows the alert 'Could not update. Please try again.' and the status stays unchanged — load() is only called on success, so there is NO optimistic/phantom local transition and no duplicate timeline event. Retry after reconnect succeeds exactly once.                                                                                                                                                                                           | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-15 | P1  | golden     | Attendance dispute routes into the manager review queue — manager clears it via approve/reject                | 1. As the CPO member, open My Attendance, find the closed record, tap Dispute, enter a note, submit. 2. Verify the record flips to Pending Review on the member's side. 3. Log in as the provider manager, open Admin Attendance / Pending Review queue. 4. Locate the disputed record — confirm reason shows 'disputed' and the member's dispute_note is visible. 5. Tap Approve (or Reject), optionally add manager notes, confirm.                                                                             | disputeSession (own record WHERE cpo_user_id) sets review_status='pending', review_reason='disputed', dispute_note (sliced to 500) and writes an attendance.dispute audit row. The record appears in pendingQueue tagged disputed. The captured geotag/time and prior review columns stay immutable — only review_status/reason/dispute_note change. Manager approve/reject clears it through reviewSession (approve derives present/late; reject leaves it flagged) and is audited. A member cannot dispute an open shift (shift_still_open) or an already-pending record (already_pending_review).                                                                                                                           | 1 device, 2 accounts — CPO member + provider manager                                                                                                                                                                                                                                                                                |                                                                           |        |       |
| IQUE-16 | P1  | regression | Feature-flag gate — with DEPT_CHAT_V2 OFF the whole incidents controller is invisible (404)                   | 1. With the flag OFF, log in as the provider manager. 2. Confirm the Incident tab / dept-chat surfaces are not presented. 3. Hit every incidents endpoint directly: POST /incidents, GET /incidents/mine, GET /incidents/queue, GET /incidents/:id, PATCH /incidents/:id/status, POST /incidents/:id/assign, POST /incidents/:id/note, and the attachments/keys routes. 4. Toggle the flag ON and confirm the surface returns.                                                                                    | The whole IncidentController is wrapped by DeptChatV2Guard, so with the flag OFF every route (member submit/mine AND manager queue/detail/status/assign/note/evidence/keys) returns 404 — the surface is invisible, no partial exposure of incident data. With the flag ON, the full member + manager surface reappears and behaves per the golden cases.                                                                                                                                                                                                                                                                                                                                                                      | 1 device — provider manager account (flag-toggle capable env)                                                                                                                                                                                                                                                                       |                                                                           |        |       |
| IQUE-17 | P0  | security   | Cross-department WRITE bypass — dept-scoped manager mutates a cross-dept incident by id (status/assign/note)  | 1. Confirm GET /incidents/:id for the Security incident returns 404 (detail is dept-scoped). 2. Now call PATCH /incidents/:id/status with a plausible legal next status. 3. Call POST /incidents/:id/assign with the manager's own id. 4. Call POST /incidents/:id/note with an internal note. 5. Re-check the incident as a Security-scoped manager or the company account.                                                                                                                                      | SPEC-INTENDED: writes must be department-scoped exactly like detail()/queue() — a dept-scoped manager acting on a cross-department incident should get 404 incident_not_found_in_org, and no status/assignment/note/audit change should occur. OBSERVED GAP (file a bug): updateStatus(), assign() and addNote() resolve WHERE id=$1 AND org_user_id=$2 with NO department filter (unlike detail/queue), so the Ops-scoped manager CAN move status, assign, and add internal notes on the Security incident despite being unable to open it. Also note updateStatus's FSM error leaks the current status (invalid_incident_transition:from->to). Treat any successful cross-dept write as a CONFIRMED authorization defect.    | 1 device — department-scoped provider manager (department='Ops')                                                                                                                                                                                                                                                                    |                                                                           |        |       |
| IQUE-18 | P1  | error      | Assign owner validation — non-manager assignee and out-of-org assignee rejected server-side                   | 1. Confirm the mobile 'Assign owner' sheet lists only managers + 'Assign to me' (no CPO members). 2. Via the API, POST /incidents/:id/assign with assignee_user_id = the CPO member's id. 3. POST assign with assignee_user_id = a user from a different org. 4. POST assign with assignee_user_id = the org account itself (company). 5. Re-open the incident and check ASSIGNED TO + timeline.                                                                                                                  | The UI never lets you pick a non-manager. Server-side: assigning a non-manager member (not org_members manager/active and != org) → 400 assignee_must_be_manager. Assigning an out-of-org user → also fails the manager-membership check → 400 assignee_must_be_manager. Assigning the company account (assignee === orgUserId) is allowed. No timeline/audit row is written for the rejected attempts; assigned_to is unchanged. UI and server agree on who can own an incident (managers only).                                                                                                                                                                                                                              | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-19 | P1  | security   | Crafted illegal FSM hop / invalid status rejected — guard not skippable, no dev branch                        | 1. Via the API, PATCH /incidents/:id/status to a status that skips the chain (e.g. submitted → resolved, submitted → closed, received → action_assigned). 2. PATCH status with a bogus value not in the enum (e.g. to='archived'). 3. PATCH a legal transition to confirm the endpoint otherwise works. 4. Repeat the illegal skips as a delegated manager.                                                                                                                                                       | Illegal-but-valid-enum skips are rejected by assertIncidentTransition → 403 invalid_incident_transition:<from>-><to>:<actor> (no INCIDENT_TRANSITIONS row matches); no status/timeline/audit change. A bogus status value is rejected by the DTO @IsIn(INCIDENT_STATUSES) → 400 validation before the service runs. The legal transition succeeds. The gate is server-side for every actor with no 'skip in dev' branch — the only privileged edge is the company_admin reopen (IQD-03).                                                                                                                                                                                                                                       | 1 device, 2 accounts — company account + delegated manager                                                                                                                                                                                                                                                                          |                                                                           |        |       |
| IQUE-20 | P1  | edge       | Concurrent / stale transition — second illegal hop is safe but shows a misleading reopen message              | 1. Managers A and B both open the Submitted incident detail (both see the 'Received' button). 2. A taps Received → Update (incident is now Received). 3. WITHOUT B refreshing, B taps Received → Update on the stale screen. 4. Observe B's error copy and the incident's final state. 5. Refresh B and confirm the true state.                                                                                                                                                                                   | updateStatus takes a FOR UPDATE row lock, so writes serialize: A's submitted→received commits once. B's stale submitted→received is now received→received, which no FSM row allows → 403 invalid_incident_transition:received->received:manager; no second status change, no duplicate timeline event, no corruption. BUG TO FILE: the client maps ANY message starting with 'invalid_incident_transition' to 'Only a company admin can reopen a closed incident.' — so B sees a misleading reopen message for a plain stale/concurrent hop. Expected fix: distinguish the closed-reopen case from a generic stale-transition/refresh case. On refresh B sees the real current status (Received) with the correct next button. | 1 device (stale screen) or 2 devices — two provider managers                                                                                                                                                                                                                                                                        |                                                                           |        |       |
| IQUE-21 | P0  | security   | RLS deny-by-default — incident tables unreachable via the anon/authenticated Supabase client                  | 1. Using the anon key, SELECT from incident_reports, incident_events, incident_attachments, incident_attachment_keys, org_audit_log. 2. Repeat using a normal authenticated end-user JWT. 3. Attempt INSERT/UPDATE/DELETE on incident_reports and incident_events directly. 4. Confirm the same data IS reachable only through the backend service (auth-service) which uses the service role.                                                                                                                    | All four incident tables + org_audit_log have RLS ENABLED and FORCED with no permissive policies, so anon and authenticated clients get zero rows on SELECT and are denied on write (deny-by-default). No incident narrative, coordinates, ref, storage_key, sealed_key, or audit metadata leaks to a direct client. Only the backend (service-role, RLS-bypassing) path returns data, and only after the controller/guard authz. Confirms the DB is not a side-channel around OrgManagerGuard/DeptChatV2Guard.                                                                                                                                                                                                                | N/A — direct DB/PostgREST client (anon key + end-user JWT)                                                                                                                                                                                                                                                                          |                                                                           |        |       |
| IQUE-22 | P1  | security   | Audit + log scrub — no narrative, coordinates, note text, storage_key, or key material in audit/logs          | 1. Submit an incident with a distinctive description string and lat/lng. 2. As the manager, run status transitions, an assign, and an internal note. 3. Query org_audit_log for this incident's rows (action = incident.submit / incident.status / incident.assign / incident.note). 4. Grep the auth-service logs for the description string, the coordinates, the note text, the storage_key, and any sealed_key.                                                                                               | org_audit_log metadata holds ONLY scalars: incident.submit → {category, severity}; incident.status → {from, to}; incident.assign → {}; incident.note → {internal}. No description, coordinates, ref, note text, storage_key, or key bytes ever land in metadata (matching the migration's 'NO PII' note). The distinctive description/coordinates/note text/keys never appear in application logs (CLAUDE.md log-audit stop-condition). Nothing bypasses the scrub by renaming variables.                                                                                                                                                                                                                                      | N/A — DB (org_audit_log) + auth-service log inspection                                                                                                                                                                                                                                                                              |                                                                           |        |       |
| IQUE-23 | P2  | edge       | New-incident push routing by department — only the right managers are alerted; submit survives push failure   | 1. As the CPO member, submit an incident with department='Ops'. 2. Observe which manager accounts receive the new-incident push. 3. Submit another incident with no department. 4. (Fault-inject) Make the push bridge fail/time out and submit again; confirm the submit still returns 201 with a ref.                                                                                                                                                                                                           | resolveOrgManagers(org, 'Ops') returns the company account + unscoped managers + Ops-scoped managers only — the Security-scoped manager is NOT alerted for the Ops incident. A no-department incident alerts all active managers + the company account. The push is best-effort (metadata-only FCM): if delivery fails or times out, submit still commits the report + 'submitted' event + audit and returns {id, ref, status, severity} — the push never blocks or throws the submit.                                                                                                                                                                                                                                         | Up to 4 accounts — company + unscoped/Ops/Security managers, + 1 CPO submitter                                                                                                                                                                                                                                                      |                                                                           |        |       |
| IQUE-24 | P2  | edge       | Ref format + updated_at ordering + timezone display consistency                                               | 1. Submit 3 incidents in quick succession; note each ref. 2. In the queue, confirm each card shows a valid ref and the correct relative/updated time. 3. Move the least-recently-updated incident to the next status so its updated_at bumps. 4. Re-open the queue and observe its position within its severity band. 5. Change the device timezone (e.g. Asia/Dubai → UTC) and re-open detail + queue; compare timestamps.                                                                                       | Each ref is INC-YYYY-NNNNN with the current year and a 5-digit zero-padded, strictly increasing sequence (incident_ref_seq), UNIQUE. After a status change bumps updated_at, the incident jumps to the top of its severity band (ORDER BY …, updated_at DESC). Timestamps render consistently regardless of device timezone (created_at/updated_at are timestamptz; display via the shared time helper) — no off-by-hours drift or 'in the future' relative times when the device clock/timezone changes.                                                                                                                                                                                                                      | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-25 | P1  | regression | Manager files their own incident from the queue — submit is JWT-gated, appears in queue, no corruption        | 1. From Incident Queue, tap 'Report an Incident'. 2. Complete the wizard (category, severity, description, optional photo/location) and submit. 3. Return to the queue and pull-to-refresh. 4. Confirm the new incident is present (clear active filters if they exclude it). 5. Open it and confirm it is a normal member-submitted incident owned by the manager's org.                                                                                                                                         | The submit route (POST /incidents) is gated only by JwtAuthGuard + DeptChatV2Guard (not OrgManagerGuard), so a manager can file their own report without dropping to a member surface. resolveOrg maps the manager's submitter_id to their org_user_id, so the incident lands in the same org queue with a fresh ref and status 'submitted'. It appears under matching filters, sorts correctly, and does not disturb existing filters/sort or other incidents.                                                                                                                                                                                                                                                                | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-26 | P2  | edge       | Mid-flow app kill / backgrounding during a status update or note — commit is atomic, exactly-once             | 1. Open the incident, tap a next-status button → Update, and force-kill the app immediately (before or during the network round-trip). 2. Relaunch and re-open the incident. 3. Inspect status + TIMELINE. 4. Repeat the kill while sending an internal note. 5. If the write clearly did NOT reach the server, retry after relaunch.                                                                                                                                                                             | The status write is a single server transaction (UPDATE status + INSERT incident_events + audit under one withTransaction) — so after a kill the incident is EITHER fully unchanged OR fully advanced with exactly one matching timeline event and audit row; never a status change without its event or vice-versa. No duplicate timeline entries appear from a killed-then-retried action (the client only reloads on success, so no phantom local state). Note is likewise all-or-nothing (single insert).                                                                                                                                                                                                                  | 1 device — provider manager account                                                                                                                                                                                                                                                                                                 |                                                                           |        |       |
| IQUE-27 | P1  | security   | Member-to-member evidence isolation — a member cannot attach to or read another member's incident evidence    | 1. As member B, call GET /incidents/:id/attachments for A's incident. 2. Call GET /incidents/:id/recipients and GET /incidents/:id/attachments/:attId/key for A's incident. 3. Call POST /incidents/:id/attachments (attach) and POST /incidents/:id/attachments/:attId/keys (store keys) against A's incident. 4. Confirm member B still sees only their own incidents via GET /incidents/mine.                                                                                                                  | Member B is neither the submitter nor an org manager, so listAttachments / evidenceRecipients / getMyAttachmentKey all fail the submitter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | org-manager check → 403 not_authorized_for_incident_evidence. attach → 403 only_submitter_can_attach; storeAttachmentKeys → 403 only_uploader_can_store_keys. GET /incidents/mine returns only B's own submitter_id-scoped reports. No cross-member evidence, sealed keys, ref, or narrative leaks between members of the same org. | 1 device, 2 accounts — CPO member A (submitter) + CPO member B (attacker) |        |       |     |

## 8. Access Control, Dept-Scoping, PDF Export & Security Stop-Conditions (31 cases)

| ID      | Pri | Type       | Scenario                                                                                                      | Steps (frontend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Expected Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Devices/Roles                                                         | Bug              | Status                                     | Notes |
| ------- | --- | ---------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ---------------- | ------------------------------------------ | ----- | --- | --- |
| DSEC-01 | P0  | golden     | Feature flag ON grants Dept Chat to every org member (company, manager, CPO)                                  | 1. Log in on device A as the provider COMPANY account; from the Agent dashboard open the Departmental module. 2. Confirm the 5-tab shell loads (Home, Channels, Attend, Incident, Vault) and the Channels tab lists the seeded channels. 3. Log in on device B as the delegated MANAGER and open Departmental. 4. Log in on device C as the managed CPO; on the CPO dashboard/home tap the Departmental / Dept Chat entry. 5. On each device open the Channels tab and confirm channels load without a 403/permission error.                                                                                                                 | All three roles reach the Departmental workspace and successfully GET /department/channels. Company + manager see admin surfaces (Manage/Create, Admin Attendance root, Incident Queue root); CPO sees member surfaces (Attendance root, My Incidents root) and only channels it is seeded into. No 'Pro required' or entitlement error for the Lite CPO — access is by org membership (DeptChatAccessGuard admits agents.type='company' AND status='ACTIVE', or an org_members row status='active'), not tier.                                                                                                                                                                          | 3 devices: provider company account + delegated manager + managed CPO |                  |                                            |       |
| DSEC-02 | P0  | security   | Feature flag OFF makes the v2 surface invisible (404, not 403) and hides the CPO tab                          | 1. Log in as the managed CPO on a DEPT_CHAT_V2=false build. 2. Inspect the CPO home / bottom nav for the Dept Chat tab and the on-duty home card. 3. Attempt to reach any attendance-verification / incident-report entry point. 4. As the manager, attempt to hit a v2 attendance route (org/summary, org/pending, org/export, shifts, day-status, sessions/:id/review) and an ops/deptchat/\* route on a backend with the flag off (observe the network response).                                                                                                                                                                         | The CpoDept tab and the v2 entry cards are NOT rendered (CpoNavigator gates on DEPT_CHAT_V2). The v2-gated backend routes respond 404 Not Found (DeptChatV2Guard throws NotFoundException so the feature is invisible, not merely forbidden) — never a 200 and never a 500. Legacy /attendance clock-in/out, /me, /org/sessions, PATCH sessions/:id are NOT 404'd (they carry no DeptChatV2Guard — see AC-16).                                                                                                                                                                                                                                                                           | managed CPO + manager on a flag-OFF build                             |                  |                                            |       |
| DSEC-03 | P0  | security   | Non-org user (individual / random Pro) is denied the Dept Chat workspace                                      | 1. Log in as the non-org user on a DEPT_CHAT_V2=true build. 2. Confirm the app does not surface a Departmental tab for this role. 3. Using the same valid JWT, attempt GET /department/channels directly (or via a deep link that reaches the department controller).                                                                                                                                                                                                                                                                                                                                                                        | Request is rejected 403 'dept_chat_org_membership_required' (DeptChatAccessGuard Path 1 company-match and Path 2 active-member-match both miss). No channel list is returned. Membership — not a JWT claim — is the gate; a valid token alone does not grant access (guard re-reads agents/org_members from the DB).                                                                                                                                                                                                                                                                                                                                                                     | one non-org user account                                              |                  |                                            |       |
| DSEC-04 | P0  | security   | Department-scoped manager sees ONLY their department; cross-dept data hidden and query-param widening blocked | 1. Log in as manager M (dept-scoped) on device A; open Departmental → Attend tab (Admin Attendance root). 2. Load the attendance summary and pending-review queue. 3. Open the Incident tab (Incident Queue root) and load incidents. 4. Attempt to widen scope by sending ?department=Events on org/summary, org/pending and the incident queue. 5. Compare rows against known 'Events' records. 6. Log in as the COMPANY account (department=null) and load the same views.                                                                                                                                                                | Manager M's summary, pending queue, incident queue/detail, and export are FORCED to department='Operations' — the controller passes department: manager.department ?? department, so when manager.department is non-null the query param is IGNORED and cannot widen scope. No 'Events' CPOs/records/incidents appear. The company account / unscoped manager (department=null) sees the WHOLE org (query param may narrow only).                                                                                                                                                                                                                                                        | dept-scoped manager + provider company account                        |                  |                                            |       |
| DSEC-05 | P0  | security   | Cross-org access denied — manager of Org A cannot read Org B                                                  | 1. Log in as Org A's manager. 2. In the Departmental workspace load attendance summary / incident queue (Org A data appears correctly). 3. Attempt a cross-tenant read: the org-manager endpoints scope by manager.org_user_id (resolved server-side from org_members), so craft requests attempting to reference Org B's data. 4. Attempt to add an Org B CPO into an Org A channel via the channel members screen.                                                                                                                                                                                                                         | Every org-manager route derives the org from OrgManagerGuard (manager.org_user_id), never a client-supplied org id, so Org A's manager can only ever read Org A. Org B rows never appear. The addMember cross-org attempt is rejected 'member_not_in_org' (see AC-14). No Org B plaintext, roster, or attendance data leaks into Org A's UI. (assertOrgScope is the service-layer rail for any route that DOES take a target org id.)                                                                                                                                                                                                                                                    | Org A manager (Org B exists as a separate tenant)                     | D4-a             |                                            |       |
| DSEC-06 | P0  | security   | CPO/member capability lockdown — cannot reach any manager/admin route                                         | 1. Log in as the CPO and open Departmental. 2. Confirm the Attend tab opens on the member Attendance root (not Admin Attendance) and the Incident tab opens on My Incidents (not the Queue). 3. Attempt the manager incident queue endpoint with the CPO token. 4. Attempt manager attendance review/summary/pending, create-channel, manage-channels, shift-create, day-status and org/export with the CPO token. 5. In a channel where the CPO is a viewer, confirm no composer is shown and posting is blocked.                                                                                                                           | The CPO never sees admin roots client-side, and every manager-only endpoint returns 403 'org_manager_access_required' (OrgManagerGuard Path 1 and Path 2 both miss for a non-manager member). Incident queue member access is 403. Viewer channels render read-only 'You are a viewer' with no composer. Role is enforced server-side — a CPO who forges a request still gets 403.                                                                                                                                                                                                                                                                                                       | managed CPO                                                           | BUG-3            |                                            |       |
| DSEC-07 | P0  | golden     | PDF export of attendance generates correct, org-scoped, biometric-free report + is audit-logged               | 1. Open the ops-console Attendance Oversight (dept-attendance) page. 2. Enter the target org UUID, set From and To dates, click 'Load summary' and confirm the status counters match known data. 3. Click 'Export PDF'; allow the popup. 4. Inspect the opened report window: h1 'Bravo Secure — Attendance Report', the org id, the range label, generated timestamp, session count, and the table columns (Member, ID, Department, Site, in/out, Status, Face verified, In radius, Admin notes). 5. Use the browser print dialog → Save as PDF. 6. In the backend/ops audit log, confirm an 'attendance.export' row (who/when/org/format). | The PDF report renders the SAME rows as the audited CSV endpoint (exportPdf calls fetchCsv → POST /ops/deptchat/attendance/export, then print-formats it), scoped to the entered org and date range. It contains only the face_verified/in_radius BOOLEANS and admin notes — NO face frames, no biometric images, no raw coordinates. Footer reads 'Biometric-free export · verification results only · this export is audit-logged.' The audit row is 'attendance.export' via recordAdmin (ops_audit + org_audit_log); NOTE the recorded format metadata is 'csv' even for the PDF button because PDF reuses the CSV endpoint — verify this is acceptable/expected, not a mislabel bug. | ops-console browser as HQ SUPERVISOR/ADMIN                            |                  |                                            |       |
| DSEC-08 | P1  | golden     | CSV export downloads scoped, biometric-free data with the same columns                                        | 1. On the dept-attendance page enter the org UUID and date range. 2. Click 'Export CSV'. 3. Open the downloaded attendance-<org>.csv file. 4. Verify header columns and that face data is a boolean only. 5. Confirm the confirmation message 'Export downloaded (biometric-free).' 6. Verify an audit row was written.                                                                                                                                                                                                                                                                                                                      | A CSV named attendance-<first8ofOrg>.csv downloads with the required columns (Member, ID, Department, Site, clock in/out, Status, Face verified, In radius, Admin notes), scoped to org + range, with face represented only as face_verified boolean (face_meta never selected in the query). Audit row 'attendance.export' recorded. No biometric images anywhere.                                                                                                                                                                                                                                                                                                                      | ops-console browser as HQ SUPERVISOR/ADMIN                            |                  |                                            |       |
| DSEC-09 | P0  | security   | Export blocked for insufficient role (non-SUPERVISOR/ADMIN ops user; CPO on mobile)                           | 1. Log into ops-console as the lower-privilege ops user, open dept-attendance, enter an org UUID, click 'Export CSV' then 'Export PDF'. 2. Observe the on-screen message. 3. On mobile, as a CPO, attempt to trigger any attendance export path (POST /attendance/org/export).                                                                                                                                                                                                                                                                                                                                                               | The ops export requests return 403 (RequireRoles('SUPERVISOR','ADMIN') on POST /ops/deptchat/attendance/export) and the page shows 'Export failed: … — export requires Supervisor/Admin.' No file downloads and no report window with real data opens. The CPO org/export call is 403 'org_manager_access_required' (OrgManagerGuard). No partial data leaks in the error body.                                                                                                                                                                                                                                                                                                          | lower-privilege ops user + managed CPO                                |                  |                                            |       |
| DSEC-10 | P0  | security   | Suspended/removed CPO loses Dept Chat access immediately                                                      | 1. As the CPO, confirm the Departmental workspace and channel list load. 2. Have an admin set the CPO's org_members.status to non-active (suspend/revoke). 3. On the CPO device, pull-to-refresh / re-open the Channels tab (or re-authenticate). 4. Attempt GET /department/channels and any attendance/incident action.                                                                                                                                                                                                                                                                                                                    | Once status<>'active', DeptChatAccessGuard Path 2 (member … status='active') no longer matches → 403 'dept_chat_org_membership_required'. The CPO can no longer list channels or perform attendance/incident actions. Because the guard RE-READS the DB (not the JWT), the existing unexpired token cannot keep access alive.                                                                                                                                                                                                                                                                                                                                                            | managed CPO + admin to flip status                                    | D4-c             |                                            |       |
| DSEC-11 | P0  | security   | Suspended/deactivated company account loses dept-chat AND manager access                                      | 1. As the company account, confirm dept-chat + manager surfaces (create channel, admin attendance, incident queue) load. 2. Set the company agent status to a non-ACTIVE value (suspended/deactivated). 3. On the company device re-open the Departmental workspace. 4. Attempt to list channels, load admin attendance, and create a channel.                                                                                                                                                                                                                                                                                               | With status<>'ACTIVE', both DeptChatAccessGuard Path 1 and OrgManagerGuard Path 1 (…type='company' AND status='ACTIVE') fail → 403. The suspended company loses BOTH workspace access and manager privileges. The status check is part of the entitlement, not just the account type (D4-c fix).                                                                                                                                                                                                                                                                                                                                                                                         | provider company account + admin to flip status                       | D4-c             |                                            |       |
| DSEC-12 | P0  | security   | No plaintext / biometric / key material leaks in logs or exports                                              | 1. With logcat capturing, on device: open a channel and send/receive a message, perform a face+location check-in, and submit an incident with a photo. 2. Grep the captured logcat for message body text, the incident narrative, face-frame bytes/base64, coordinates, and any group master-key / ArrayBuffer key material. 3. Run a PDF and CSV export and inspect the output for biometric images or raw key/coordinate leakage.                                                                                                                                                                                                          | Logcat contains NO plaintext message bodies, no incident narrative, no face frames, and no key bytes/ArrayBuffers (logAudit stop-condition holds; faceCheck deletes the frame immediately after on-device MLKit detection). Exports carry only face_verified/in_radius booleans and admin notes — never images or key material. If any secret appears in logs or export, fail hard.                                                                                                                                                                                                                                                                                                      | 1 device with ADB logcat + ops-console                                |                  |                                            |       |
| DSEC-13 | P0  | security   | Guard chain always applied — no dev-skip / bypass branch on any dept-chat route                               | 1. From an unauthenticated state (no/expired JWT), attempt /department/channels, the attendance verify/review endpoints, and /ops/deptchat/\* — via the app after logout or a crafted request. 2. With a valid non-manager token, hit the manager-only and ops-only routes. 3. With the feature flag off, hit the v2 routes. 4. Confirm there is no 'dev mode' query param, header, or build variant that skips JwtAuthGuard, DeptChatAccessGuard, OrgManagerGuard, AdminGuard, or DeptChatV2Guard.                                                                                                                                          | Unauthenticated → 403/401 'not_authenticated' (JwtAuthGuard/entitlement guard runs FIRST, before the flag guard, so flag-off still yields an auth error not a 404 for an unauthenticated caller); wrong role → 403; flag off (authenticated) → 404. Every dept-chat route runs the full guard chain layered (JwtAuthGuard first, then entitlement/role/feature guards — never substituted). No skip-in-dev branch; no header/param bypass. The ops surface is AdminGuard (HQ tier) and is never reachable by an OrgManagerGuard-only manager token, and vice-versa.                                                                                                                      | any device + crafted requests                                         |                  |                                            |       |
| DSEC-14 | P1  | security   | addMember rejects cross-org / non-org targets (no rekey of a foreign user into the E2EE group)                | 1. As the Org A channel admin, open a channel's Members screen. 2. Attempt to add the foreign/non-org user id as a member. 3. Observe the result and confirm no membership-intent rekey is enqueued for that user. 4. Re-open the channel members list to confirm the foreign user is absent.                                                                                                                                                                                                                                                                                                                                                | The add is rejected with 'member_not_in_org' — a channel admin cannot add an arbitrary cross-org or non-org user and rekey them into the E2EE group (D4-a fix; the org account itself, memberUserId === ch.org_id, is the ONLY exemption). No channel_membership_intents 'add' row is created for the foreign user; the E2EE group master key is never sealed to them.                                                                                                                                                                                                                                                                                                                   | Org A channel admin (foreign user id known)                           | D4-a             |                                            |       |
| DSEC-15 | P1  | edge       | PDF export edge cases — empty range renders gracefully; popup-blocked is surfaced                             | 1. Enter an org UUID and a date range known to contain zero sessions; click 'Export PDF'. 2. Observe the generated report window. 3. Block popups in the browser, then click 'Export PDF' again. 4. Read the on-screen status message. 5. Click 'Export CSV' for the empty range and open the file.                                                                                                                                                                                                                                                                                                                                          | For an empty range the report opens with the header row, '0 session(s)' in the meta line, and an empty table body — no crash. With popups blocked the page shows 'Popup blocked — allow popups to generate the PDF report.' and no half-rendered window (window.open returned null → early return). The empty CSV contains the header row only. No unhandled exception; the Export buttons stay disabled while orgId is blank (disabled={busy                                                                                                                                                                                                                                            |                                                                       | !orgId.trim()}). | ops-console browser as HQ SUPERVISOR/ADMIN |       |     |     |
| DSEC-16 | P1  | regression | Legacy /attendance surface stays reachable and unchanged when v2 flag is OFF (per-method gating regression)   | 1. On a flag-OFF backend, as the CPO/agent, perform a legacy clock-in and clock-out via the existing (non-v2) attendance path. 2. Load 'my attendance' (/attendance/me) and org sessions (/attendance/org/sessions), and PATCH /attendance/sessions/:id. 3. Confirm none of these calls return 404. 4. Separately confirm the v2-only additions (shifts, my-shift/today, org/summary, org/pending, org/export, sessions/:id/review, day-status, sessions/:id/dispute, incident, ops/deptchat) DO return 404 on the same flag-off backend.                                                                                                    | The legacy /attendance clock-in/out, /me, /org/sessions and PATCH sessions/:id carry NO DeptChatV2Guard and behave exactly as before — not 404'd. Only the v2 additions (which each add DeptChatV2Guard per-method) are invisible (404) when the flag is off. DeptChatV2Guard is applied per-method, never on the whole controller class. No regression to the pre-existing clock-in/out flow.                                                                                                                                                                                                                                                                                           | CPO/agent on a flag-OFF build                                         | D1-b             |                                            |       |
| DSEC-17 | P0  | edge       | Out-of-geofence check-in → Pending Review, NEVER Absent                                                       | 1. As the CPO with a valid today shift, open Attend → check-in and complete the face+location verify from OUTSIDE the site radius. 2. Observe the result screen. 3. As the manager, open the Pending queue and confirm the record with its reason. 4. Confirm the record is NOT shown as 'absent' anywhere in My Attendance.                                                                                                                                                                                                                                                                                                                 | Server deriveCheckIn haversine vs approved_radius_m flags the check-in review_status='pending' with an out-of-radius reason and in_radius=false; the result screen shows 'Pending Review' and the copy 'You have NOT been marked absent.' The captured geotag/time are immutable. Manager sees it in the pending queue with the reason; a reject (not the system) is what can later drive 'absent'.                                                                                                                                                                                                                                                                                      | managed CPO + manager to review                                       |                  |                                            |       |
| DSEC-18 | P0  | edge       | Camera denied / face not detected → Pending Review with camera_unavailable reason; frame never persisted      | 1. Deny the camera permission at the verify screen and confirm check-in. 2. In a second run, grant camera but present no face (point away) so MLKit detects zero faces. 3. Observe each result screen. 4. As the manager, open the pending record and read the failure reason label. 5. Grep logcat during both runs for face-frame bytes/base64.                                                                                                                                                                                                                                                                                            | Camera denial sends face_unavailable → server records review_status='pending' with reason surfaced as 'camera_unavailable' (fixed 2026-07-02: ClockInDto.face_unavailable + reviewReasonLabel). Face-absent likewise resolves to Pending Review, never Absent. faceCheck deletes the captured frame immediately on pass/fail/crash — logcat shows NO frame bytes. Manager sees the specific reason label, not a generic failure.                                                                                                                                                                                                                                                         | managed CPO + manager + ADB logcat                                    | SPEC-04-#11      |                                            |       |
| DSEC-19 | P1  | error      | No active shift → check-in hard-blocked with a clear state (not silently absent)                              | 1. As the CPO open Attend. 2. Confirm the 'No active shift assigned' block state and that the check-in action is disabled. 3. Force a POST /attendance/clock-in anyway (crafted request).                                                                                                                                                                                                                                                                                                                                                                                                                                                    | myTodayShift returns null → UI renders the no-shift block and disables check-in. A forced clock-in is rejected server-side with 'no_active_shift_assigned' (400). The CPO is not marked present or absent — attendance simply cannot start without a shift.                                                                                                                                                                                                                                                                                                                                                                                                                              | managed CPO (no shift)                                                |                  |                                            |       |
| DSEC-20 | P1  | edge       | App killed mid-verify (after capture, before submit) leaves no orphan/duplicate session and no retained frame | 1. Start check-in, complete the face+location capture, and force-stop the app the instant before/while the clock-in request is sent. 2. Relaunch, re-open Attend. 3. Check My Attendance and the manager pending queue for a partial/duplicate session. 4. Re-run check-in to completion. 5. Grep logcat for a lingering face frame.                                                                                                                                                                                                                                                                                                         | No orphaned or duplicate cpo_shift_sessions row is created (the row is written only on the server-side clock-in transaction). The re-run produces exactly one session. The captured frame was deleted on the client at capture teardown — logcat shows no retained frame bytes. No crash on relaunch.                                                                                                                                                                                                                                                                                                                                                                                    | managed CPO + manager + ADB logcat                                    |                  |                                            |       |
| DSEC-21 | P1  | regression | Timezone / day-boundary: check-in gating is tz-independent for a non-UTC org (D6-d regression)                | 1. Assign a shift active in local time but on the other side of the UTC date line. 2. As the CPO at a time the shift is active locally, open Attend and confirm today's shift shows and check-in is enabled. 3. Also verify a shift starting within the next 12h is checkable, and one ended/older is not.                                                                                                                                                                                                                                                                                                                                   | myTodayShift bounds by NOW-relative window (s.end_at >= NOW() AND s.start_at <= NOW() + INTERVAL '12 hours'), NOT date_trunc('day', NOW()) in server UTC — so the shift resolves correctly for the non-UTC org and check-in is enabled while active or within the 12h lead. The old UTC-day mis-gating at the boundary does not recur.                                                                                                                                                                                                                                                                                                                                                   | managed CPO in a non-UTC org                                          | D6-d             |                                            |       |
| DSEC-22 | P1  | edge       | Offline / network loss during check-in fails gracefully; no background location tracking                      | 1. Go offline, then attempt a check-in through the verify flow. 2. Observe the error surfaced to the user. 3. Confirm no attendance row was created server-side. 4. Restore connectivity and retry — confirm a clean single check-in. 5. Throughout, confirm location is requested only single-shot at action time (no watch/background tracking).                                                                                                                                                                                                                                                                                           | The offline check-in surfaces a clear network error and does NOT fabricate a present/absent status or a phantom session. Retry after reconnect succeeds once. Location uses single-shot getCurrentPosition at action time only — no background watcher, no continuous tracking in logcat (matches the no-background-tracking constraint).                                                                                                                                                                                                                                                                                                                                                | managed CPO + ADB logcat                                              |                  |                                            |       |
| DSEC-23 | P1  | error      | CPO dispute route — own closed record disputable; not-own / still-open / already-pending rejected             | 1. As the CPO, POST /attendance/sessions/:id/dispute with a note on the own closed record. 2. Confirm it returns to the manager pending queue with reason 'disputed' and the note. 3. Attempt to dispute the still-open session. 4. Attempt to dispute the already-pending session. 5. Attempt to dispute ANOTHER CPO's session id. 6. With the flag OFF, attempt any dispute.                                                                                                                                                                                                                                                               | Own closed record: review_status→'pending', review_reason='disputed', dispute_note stored (≤500 chars), audited 'attendance.dispute'; manager clears it via normal review. Still-open → 400 'shift_still_open'. Already-pending → 400 'already_pending_review'. Another CPO's id → 404 'session_not_found' (scoped by cpo_user_id, so it is invisible, not editable). Flag OFF → 404 (DeptChatV2Guard on the dispute method).                                                                                                                                                                                                                                                            | managed CPO + manager                                                 |                  |                                            |       |
| DSEC-24 | P0  | security   | Dept-scoped manager org/export (mobile) is forced to their department and records all filters in the audit    | 1. As the dept-scoped manager, POST /attendance/org/export with no department (and separately with department='Events' in the body). 2. Open the CSV and confirm only 'Operations' rows appear in both cases. 3. Confirm the export audit row records the applied filters (from/to/department/shift/cpo where set). 4. As the company account, export with department='Operations' and confirm it narrows correctly.                                                                                                                                                                                                                         | The controller passes department: manager.department ?? dto.department, so a scoped manager's export is FORCED to 'Operations' regardless of the body's department field — no 'Events' rows leak. The export is biometric-free (face_verified boolean only). Filters are recorded in the export audit metadata. The company/unscoped account (department=null) can narrow via the body department but defaults to whole-org.                                                                                                                                                                                                                                                             | dept-scoped manager + company account                                 |                  |                                            |       |
| DSEC-25 | P1  | security   | Delegated (non-company) manager cannot reopen a closed incident — company_admin only                          | 1. As the delegated manager, open the closed incident and attempt the reopen transition. 2. Observe the result. 3. As the company account, attempt the same reopen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | The delegated manager's reopen is rejected 403 — the incident FSM reopen gate is actorRole='company_admin' only (company account, where manager.user_id === manager.org_user_id). The company account reopen succeeds and writes a timeline event (actor/timestamp/note). A delegated manager governs the queue but cannot reopen a closed case.                                                                                                                                                                                                                                                                                                                                         | delegated manager + company account                                   | SPEC-13-#24      |                                            |       |
| DSEC-26 | P0  | security   | Incident routing + queue are department-scoped — cross-dept managers not alerted and cannot read              | 1. As the 'Operations' CPO, submit an incident tagged department='Operations'. 2. Check which managers receive the push alert. 3. As Manager E ('Events'), load the incident queue and attempt to open the 'Operations' incident detail. 4. As Manager O and as the company/unscoped manager, load the queue.                                                                                                                                                                                                                                                                                                                                | resolveOrgManagers alerts only that department's managers PLUS org-wide (department IS NULL) managers — Manager E ('Events') is NOT alerted. Manager E's queue/detail is forced to 'Events' (department = $ filter) so the 'Operations' incident is absent/unreadable. Manager O and the unscoped/company manager see it. No cross-department incident narrative leaks.                                                                                                                                                                                                                                                                                                                  | dept-scoped managers O + E + company account                          |                  |                                            |       |
| DSEC-27 | P1  | security   | addMember requires channel-admin; non-admin/viewer rejected; company-account exemption honored                | 1. As a VIEWER member of the channel, attempt POST /department/channels/:id/members. 2. As the channel ADMIN, add a valid active org member. 3. As the channel admin, add the org (company) account id itself (not in org_members). 4. Attempt to add a foreign user (covered by AC-14) for contrast.                                                                                                                                                                                                                                                                                                                                        | Viewer/non-admin add → 403 'only_admin_can_manage_members' (memberRole check runs BEFORE the org-membership check). Admin adding a valid active org member → ok, membership row + 'add' rekey intent enqueued. Admin adding the org account id → ok via the memberUserId === ch.org_id exemption (the org account has no org_members row but is legitimately part of its own org). Foreign user → 'member_not_in_org' (AC-14).                                                                                                                                                                                                                                                           | channel admin + viewer member                                         |                  |                                            |       |
| DSEC-28 | P1  | security   | Export injection safety — malicious member name is HTML-escaped in PDF and quote-wrapped in CSV               | 1. Seed a session whose member name / admin note contains the payloads above. 2. On dept-attendance, Export PDF for that org and inspect the rendered report window (DOM + no script execution). 3. Export CSV and open the file in a spreadsheet app.                                                                                                                                                                                                                                                                                                                                                                                       | In the PDF, every header and cell is passed through escapeHtml (& < > " escaped) so the payload renders as inert text — NO script executes, no injected DOM. In the CSV, fields are quote-wrapped by the server export. VERIFY whether leading =,+,-,@ are neutralized against spreadsheet formula injection; if a raw formula cell survives, log it as a finding (potential CSV-injection gap) rather than passing silently.                                                                                                                                                                                                                                                            | ops-console browser as HQ SUPERVISOR/ADMIN                            |                  |                                            |       |
| DSEC-29 | P0  | security   | Ops export guard chain — CSRF required and OrgManager token cannot reach the HQ ops route                     | 1. As the ops SUPERVISOR/ADMIN, POST /ops/deptchat/attendance/export WITHOUT a valid CSRF token. 2. Repeat with a valid CSRF token. 3. Using a provider org-MANAGER token (no admin*users row), attempt the same /ops/deptchat/* export and summary/incidents routes. 4. Confirm the reverse: an ops-only token attempting the org-manager /attendance/org/\_ routes.                                                                                                                                                                                                                                                                        | The no-CSRF POST is rejected by CsrfGuard (the /ops/deptchat controller chain is JwtAuthGuard → CsrfGuard → AdminGuard → DeptChatV2Guard). With CSRF it proceeds to the role check. The org-manager token is rejected by AdminGuard (different trust tier — admin_users, not org_members) — a provider manager can NEVER reach the HQ ops surface, and vice-versa. Guards are layered, never substituted.                                                                                                                                                                                                                                                                                | HQ ops SUPERVISOR/ADMIN + provider org-manager                        |                  |                                            |       |
| DSEC-30 | P0  | security   | Suspended delegated MANAGER loses both dept-chat and manager surfaces immediately                             | 1. As the delegated manager, confirm the workspace + manager surfaces (org/summary, pending, create-channel) load. 2. Flip the manager's org_members.status to non-active. 3. Re-open the workspace / re-authenticate. 4. Attempt GET /department/channels and any manager-only route.                                                                                                                                                                                                                                                                                                                                                       | With status<>'active', DeptChatAccessGuard Path 2 AND OrgManagerGuard Path 2 both stop matching → 403 ('dept_chat_org_membership_required' for the workspace, 'org_manager_access_required' for manager routes). The suspended manager loses BOTH tiers immediately; the guards re-read the DB so the existing token cannot keep access alive.                                                                                                                                                                                                                                                                                                                                           | delegated manager + admin to flip status                              | D4-c             |                                            |       |
| DSEC-31 | P2  | error      | Export/summary with blank or malformed org id is guarded (button disabled + backend rejects)                  | 1. With the Org id field blank (or whitespace only), observe the Load/Export CSV/Export PDF buttons. 2. Type whitespace, confirm buttons stay disabled. 3. Enter a non-UUID string and click Load summary / Export. 4. Observe the on-screen message.                                                                                                                                                                                                                                                                                                                                                                                        | All three buttons are disabled while !orgId.trim() (blank/whitespace), and load/export early-return without a request. A malformed (non-UUID) org id sent to the backend yields a handled error surfaced as 'Failed: …' / 'Export failed: …' — no crash, no unhandled promise, no half-open report window, and no data for an unrelated org.                                                                                                                                                                                                                                                                                                                                             | ops-console browser as HQ SUPERVISOR/ADMIN                            |                  |                                            |       |
